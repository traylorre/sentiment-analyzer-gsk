# Feature Specification: X-Ray Exclusive Tracing

**Feature Branch**: `1219-xray-exclusive-tracing`
**Created**: 2026-02-14
**Status**: Draft (Round 20)
**Input**: Full X-Ray trace coverage across entire sentiment-analyzer-gsk system. Consolidate all custom logging, custom correlation IDs, and missing trace propagation onto X-Ray exclusively. Single exception: one non-X-Ray canary to validate X-Ray itself is operational.
**Round 2 Emergent Issues**: (1) SSE Lambda RESPONSE_STREAM mode closes X-Ray segment before streaming completes — orphaned subsegments, (2) asyncio.new_event_loop() breaks X-Ray context propagation in async-to-sync bridge, (3) SendGrid uses urllib not httpx — NOT auto-patched by X-Ray SDK, (4) Powertools Tracer vs raw xray_recorder inconsistency across Lambdas causes silent exception loss in traces.
**Round 3 Emergent Issues**: (1) `begin_segment()` is a documented no-op in Lambda — the "Two-Phase Architecture" proposed in Round 2 is **INVALID**, (2) Powertools `@tracer.capture_method` silently mishandles async generators — falls through to sync wrapper, (3) `EventSource` API does not support custom HTTP headers per WHATWG spec — trace header propagation for SSE requires `fetch()` + `ReadableStream`, (4) Clients can force 100% X-Ray sampling via `Sampled=1` header — cost amplification attack vector, (5) X-Ray has no native "guaranteed capture on error" mode — sampling decisions made before request outcome is known, (6) X-Ray silently drops data at 2,600 segments/sec region limit — no alert on data loss, (7) X-Ray SDK `AsyncContext` loses context across event loop boundaries — must use default `threading.local()` context.
**Round 4 Emergent Issues**: (1) Audit reveals 2 Lambda functions (SSE Streaming, Metrics) have zero CloudWatch error alarms — operators never alerted to failures that X-Ray traces could diagnose, (2) 7 custom metrics emitted without alarms (StuckItems, ConnectionAcquireFailures, EventLatencyMs, MetricsLambdaErrors, HighLatencyAlert, PollDurationMs, AnalysisErrors) — failures invisible to operators, (3) X-Ray Groups only operate on already-sampled traces — at production sampling <100%, error monitoring via trace data misses unsampled errors; CloudWatch metrics required for 100% error alarming, (4) ADOT auto-instrumentation (`AWS_LAMBDA_EXEC_WRAPPER`) conflicts with Powertools Tracer — double-patches botocore, duplicates handler wrapping; must use sidecar-only mode, (5) CloudFront removed from architecture (Features 1203-1207) — browser-to-backend trace propagation works without intermediary; re-adding CloudFront would break it (CloudFront treats `X-Amzn-Trace-Id` as restricted header, replaces client value), (6) X-Ray has no native span links — SSE reconnection trace correlation requires annotation-based pattern (`session_id`, `previous_trace_id`), (7) CloudWatch `put_metric_data` failure makes all `treat_missing_data=notBreaching` alarms false-green — canary must verify CloudWatch emission health in addition to X-Ray health; requires separate IAM role and out-of-band alerting.
**Round 5 Emergent Issues**: (1) OTel SDK on SSE Lambda requires explicit configuration of `AwsXRayLambdaPropagator` to read `_X_AMZN_TRACE_ID` environment variable — without this, streaming-phase OTel spans carry different trace IDs than the Lambda runtime's X-Ray facade segment, creating disconnected traces that break SC-010, (2) OTel `TracerProvider` MUST use `AwsXRayIdGenerator` — standard `RandomIdGenerator` produces garbage timestamps in X-Ray trace ID epoch field, causing X-Ray to misindex traces by time window, (3) OTel OTLP exporter endpoint (`localhost:4318`) must be explicitly configured — SDK does not auto-discover the ADOT Extension, (4) `BatchSpanProcessor.force_flush()` MUST be called in the streaming generator's `finally` block — without it, spans buffered between batch intervals are lost when the Lambda execution environment freezes, (5) X-Ray segment document size limit is 64KB per emitted document — large metadata payloads are silently rejected by `PutTraceSegments` API, (6) OTel sampler must be `parentbased_always_on` to honor Lambda runtime's sampling decision — using `always_on` creates orphaned spans for unsampled (`Sampled=0`) invocations, (7) 4 specific OTel Python packages required but not enumerated in spec or requirements files.
**Round 6 Emergent Issues**: (1) OTel trace context MUST be extracted per-invocation (not at module level) — on warm invocations `_X_AMZN_TRACE_ID` changes, but module-level `propagate.extract()` returns the stale first invocation's context, linking ALL subsequent invocations' spans to the wrong trace, (2) Powertools Tracer's `auto_patch=True` globally patches boto3 at import time — during SSE streaming phase, every auto-patched boto3 call creates BOTH an X-Ray subsegment (via X-Ray daemon, unreliable after handler returns) AND an OTel span (via ADOT, reliable), producing duplicate entries in X-Ray, (3) SSE Lambda MUST use `auto_patch=False` to prevent dual-emission — handler-phase AWS SDK calls traced via explicit `@tracer.capture_method` instead of global patching, (4) Existing CloudWatch alarm thresholds not reviewed — audit Section 6.1 identifies `analysis_latency_high` at 25s (42% of 60s timeout) as too generous; Task 17 adds new alarms at 80-90% but doesn't recalibrate existing alarms.
**Round 7 Emergent Issues**: (1) ADOT Lambda Extension has two layer types — collector-only (`aws-otel-collector-*`, ~35MB) and Python SDK (`aws-otel-python-*`, ~80MB); sidecar-only mode MUST use collector-only layer to avoid unused auto-instrumentation code and ~45MB memory waste, (2) OTel Python SDK `BatchSpanProcessor` silently catches ALL exporter exceptions and `force_flush()` returns `True` regardless of export outcome — this is by OTel specification design (exporters must not crash applications) and CANNOT be overridden; ADOT Extension health is monitored exclusively via the canary, (3) `TracerProvider` MUST be module-level singleton — creating per-invocation leaks `BatchSpanProcessor` daemon threads (~1MB each) causing monotonic memory growth until OOM; FR-059's per-invocation requirement applies ONLY to `propagate.extract()`, NOT to `TracerProvider`, (4) OTel span attributes MUST use semantic conventions that ADOT maps to native X-Ray fields (`db.system`, `rpc.service`, `exception.type`) — non-standard names become non-indexed metadata invisible in X-Ray console, (5) ADOT Extension layer ARN MUST be version-pinned in Terraform — unpinned layers can silently introduce breaking collector schema changes on `terraform apply`, (6) SSE Lambda MUST NOT set `OTEL_PROPAGATORS` env var — `set_global_textmap()` unconditionally overwrites it, creating misleading visible-but-inactive configuration.
**Round 9 Emergent Issues**: (1) ADOT Extension has KNOWN ~30% span drop rate when collector context is canceled before HTTP exports complete (GitHub aws-otel-lambda#886, opentelemetry-lambda#224) — force_flush() handles SDK→Extension hop but Extension→X-Ray backend is a KNOWN race condition mitigated by the decouple processor; spec must acknowledge two-hop flush architecture, (2) Spec Round 5 assumption states "default batch timeout of 200ms" — INCORRECT; actual BSP schedule_delay_millis default is 5000ms (5 seconds); FR-073 overrides to 1000ms so runtime is correct but assumption is WRONG, (3) FR-060 does not explicitly prohibit OTel BotocoreInstrumentor().instrument() — future implementer could enable it alongside manual spans recreating double-instrumentation; must be explicitly prohibited, (4) TracerProvider(shutdown_on_exit=True) default registers atexit handler that races with ConcurrentMultiSpanProcessor thread pool on environment recycling (opentelemetry-python#4461); FR-065 must specify shutdown_on_exit=False, (5) X-Ray trace propagation delay is "generally 30 seconds" but non-deterministic; FR-019 canary must use retry-with-backoff (30s then 60s) not single-shot query; BatchGetTraces cannot distinguish "not yet indexed" from "trace lost", (6) FR-049 does not classify put_metric_data errors as transient vs permanent — IAM revocation (the exact failure FR-051 detects) would be retried instead of immediately escalated via FR-050; error taxonomy required, (7) FR-033 assumes SSE Lambda server checks Last-Event-ID for stream resumption — if server ignores it, client-side propagation is hollow; server-side support must be specified, (8) FR-048 requires client to read response X-Amzn-Trace-Id but CORS ExposeHeaders does not include it — hard prerequisite for reconnection trace correlation, (9) ReadableStreamDefaultReader.closed is canonical way to distinguish graceful close from network error — FR-033 reconnection should use different strategies per close type.
**Round 10 Emergent Issues**: (1) Lambda streaming continues after client disconnect — AWS docs confirm streamed responses are NOT interrupted when client disconnects; Python custom runtimes have no standard disconnect detection mechanism (unlike Node.js StreamifyResponse metadata.onAbort); BrokenPipeError on write is the only signal; SSE Lambda continues DynamoDB polling, span creation, and force_flush() for phantom streams, (2) BatchSpanProcessor max_queue_size=512 (FR-073) is INSUFFICIENT — 15-second streaming Lambda at ~15 polls/sec with ~3 spans/poll produces ~675 spans; exceeds 512 queue capacity; BSP silently drops spans via queue.put_nowait() Full exception caught as warning; recalculation required, (3) X-Ray IsPartial flag not checked by canary — X-Ray marks traces as partial (IsPartial=true in TraceSummary) when not all segments received; canary (FR-078) checks trace existence but not completeness; partial traces with missing ADOT streaming spans appear "found," (4) Lambda Function URL streaming breaks silently in VPC — responses buffered instead of streamed; SSE Lambda MUST NOT be deployed in VPC, (5) SSE retry: field not emitted by server — SSE spec (WHATWG HTML Living Standard §9.2) defines server-side reconnection control; without it clients use implementation-defined defaults; fetch()+ReadableStream does NOT auto-parse retry: like EventSource, (6) OTel streaming semantic conventions are still undefined (marked "TODO" in OTel semconv) — project's span-per-poll-cycle convention is project-specific with no standard to align to, (7) Decouple processor auto-configuration only applies to Lambda Layer deployments, NOT container-based custom configs — must be explicitly included in custom collector YAML, (8) AwsXRayLambdaPropagator extracts context even when Sampled=0 (opentelemetry-lambda#1782) — if Active Tracing accidentally disabled, ALL spans silently suppressed, (9) X-Ray centralized sampling fallback to TraceIDRatioBased (1 trace/sec) when sampling API unreachable — dev/preprod 100% sampling silently degrades.
**Round 11 Emergent Issues**: (1) SSE Lambda custom bootstrap does NOT read `Lambda-Runtime-Trace-Id` header or set `_X_AMZN_TRACE_ID` on warm invocations — bootstrap reads `Lambda-Runtime-Aws-Request-Id` but NOT the trace header; on warm starts, `_X_AMZN_TRACE_ID` retains the FIRST invocation's value; FR-059's `propagate.extract()` reads stale env var, linking ALL warm invocation OTel spans to wrong trace (confirmed by code inspection: `src/lambdas/sse_streaming/bootstrap` lines 39-51), (2) Lambda timeout kills runtime process WITHOUT executing generator `finally` blocks — Lambda does NOT send SIGTERM to custom runtime process; runtime is killed directly (SIGKILL after Extension Shutdown phase); FR-055's `force_flush()` in `finally` block does NOT execute; Round 6 assumption about reliable `finally` execution INVALID for timeout scenario; mitigation requires proactive flush using `Lambda-Runtime-Deadline-Ms` before deadline, (3) FR-035 server-side sampling override architecturally infeasible for Lambda Function URLs — API Gateway makes independent sampling decisions when tracing enabled (FR-035 works for API Gateway paths); Lambda Function URLs honor incoming `Sampled=1` directly with NO override mechanism; no WAF header-rewrite capability exists; malicious client can force 100% sampling on SSE Lambda, (4) `OPENTELEMETRY_COLLECTOR_CONFIG_FILE` environment variable not specified in Terraform — ADOT Extension does NOT auto-discover config files at arbitrary paths; without env var, Extension uses embedded default (includes unnecessary components, wastes ~15MB); custom config at `/opt/collector-config/config.yaml` (FR-069) is dead code without the env var, (5) Canary invocation interval never specified — 10+ FR references to "canary interval" without defining frequency; interval determines detection latency (2× interval per FR-020), Lambda timeout requirements (FR-049 CloudWatch health check needs 60-120s ingestion delay), and cost, (6) FR-043 silent failure metrics lack namespace and dimensions — 6 new metrics without unified namespace or FunctionName/FailurePath dimensions; HighLatencyAlert namespace discrepancy (audit: `SentimentAnalyzer/Ingestion`, Task 17: `SentimentAnalyzer/SSE`) still unresolved, (7) ADOT Extension export failure logs not monitored — `"Exporting failed. Dropping data."` from two-hop race (aws-otel-lambda#886) detectable only via CloudWatch Logs; no metric filter specified; provides faster per-invocation detection than canary aggregate approach, (8) Frontend SSE TextDecoder chunk boundary vulnerability — `TextDecoder` without `{stream: true}` produces U+FFFD replacement characters when multi-byte UTF-8 sequences split across ReadableStream chunks; corrupts SSE event data containing non-ASCII characters.
**Round 12 Emergent Issues**: (1) CRITICAL: Post-proactive-flush generator behavior unspecified — FR-093 fires proactive flush at 3000ms remaining but generator continues creating orphaned spans in remaining ~3 seconds until SIGKILL; generator MUST terminate after flush, yielding `event: deadline` SSE event, which converts a potential Lambda timeout (SIGKILL) into a graceful completion (normal Shutdown), giving the ADOT Extension a reliable 2000ms flush window instead of a timeout-triggered one, (2) HIGH: OTel Python core packages (`opentelemetry-api`, `opentelemetry-sdk`, `opentelemetry-exporter-otlp-proto-http`) enforce lockstep `==` pins internally but spec has no version pinning requirement; version drift between deployments possible; pip resolver prevents mixing but explicit pins needed for reproducibility, (3) HIGH: SSE Lambda 512MB memory allocation insufficient with ADOT overhead — Extension ~50MB + OTel SDK ~30MB + runtime ~200MB leaves only ~180MB headroom; cold start at 512MB is ~1200ms vs ~800ms at 1024MB; memory increase to 1024MB required, (4) HIGH: Bootstrap passes `None` for Lambda context parameter — handler CANNOT call `context.get_remaining_time_in_millis()`, making FR-093 unimplementable; bootstrap must read `Lambda-Runtime-Deadline-Ms` header and propagate deadline to handler, (5) MEDIUM: Zero Lambda functions have memory utilization alarms — ADOT memory overhead increases OOM risk without warning, (6) MEDIUM: FR-098 metric filter pattern matches only one of two ADOT export failure log variants — misses "Exporting failed. No more retries left. Dropping data." (retries exhausted), (7) MEDIUM: OTel SDK initialization failures crash Lambda with unclear error messages — operators cannot diagnose misconfiguration from CloudWatch Logs, (8) NOT A RISK: Concurrent Lambda invocations sharing ADOT Extension — CONFIRMED each invocation gets own execution environment with own Extension process; zero cross-invocation span corruption.
**Round 13 Emergent Issues**: (1) CRITICAL: No deployment ordering constraint — AWS docs confirm upstream-to-downstream instrumentation required; deploying canary (Task 11) before instrumentation (Tasks 2-5) produces false failures; removing logging (Tasks 6/7/9) before instrumentation verification creates monitoring blind spot, (2) HIGH: No OTel SDK kill switch — `OTEL_SDK_DISABLED=false` env var needed on ADOT-instrumented Lambdas for emergency disable without container rebuild (available since opentelemetry-python v1.24.0; ADOT Extension still runs at ~50MB but receives zero spans), (3) HIGH: No migration dual-emit period — Tasks 6/7/9 remove logging with no temporal verification gate against X-Ray replacement; minimum 2-week dual-emit with 4 verification gates required, (4) HIGH: No pre-ADOT rollback container image — rollback from ADOT requires container rebuild during incident (~15-30min) vs image tag switch (~2min), (5) HIGH: X-Ray Groups missing `insights_enabled=true` — without it, Groups are query-only and do NOT emit CloudWatch metrics, breaking SC-013, (6) MEDIUM: EventBridge→Lambda permission race — Terraform may create canary EventBridge rule before `aws_lambda_permission`, causing first invocation to fail with AccessDeniedException, (7) MEDIUM: Canary lacks trace completeness ratio metric — checks IsPartial flag per-trace but doesn't detect systemic trace loss (e.g., consistent 10% drop from aws-otel-lambda#886).
**Round 15 Emergent Issues** (2 CRITICAL, 4 HIGH, 4 MEDIUM): (1) CRITICAL: 5 code paths (circuit_breaker, audit, notification, self_healing, fanout) catch exceptions and log but emit NO metric — silent failures invisible to CloudWatch alarms; operators cannot detect DynamoDB persistence failures, SNS publish failures, or batch write partial failures; (2) CRITICAL: metric emission itself can fail (put_metric_data throttling/regional degradation) with no backup detection path — cascading false-green where treat_missing_data=notBreaching resolves alarms to OK while system is failing; (3) HIGH: alarm thresholds miscalibrated — analysis_latency_high at 25s (42% of 60s timeout) causes cold-start false positives, error alarms use absolute counts not percentages, collision_rate_low takes 30 minutes to detect; (4) HIGH: Ingestion and Notification Lambdas have no latency alarms — approaching-timeout degradation invisible; (5) HIGH: dashboard alarm widget shows 6 of 30+ alarms — operators get incomplete picture, no composite alarm for system health; (6) HIGH: SendGrid HTTP calls go through python_http_client → urllib.request → http.client, NOT requests/urllib3 — X-Ray patch_all() only captures via httplib patch, OTel needs URLLibInstrumentor; (7) MEDIUM: canary heartbeat must piggyback on same put_metric_data call path as production metrics — separate health check proves nothing about emission pipeline; (8) MEDIUM: treat_missing_data=notBreaching on application alarms masks CloudWatch Metrics API degradation — must use treat_missing_data=missing to preserve INSUFFICIENT_DATA state; (9) MEDIUM: work order files (fix-*.md) contain only Round 1-6 FRs — 66+ FRs from Rounds 7-14 mapped in HL doc but missing from implementation guides; (10) MEDIUM: collision rate alarm evaluation periods too slow — 6×5min=30min detection for broken dedup, should be 2-3 periods.
**Round 14 Emergent Issues**: (1) CRITICAL: OTel Python SDK `force_flush()` timeout parameter NOT enforced (issue #4568) — call blocks indefinitely; if ADOT Extension crashes, OTLP exporter retries with 10s default timeout × 6 retries per batch; worst-case 24-batch queue blocks for minutes; FR-093's 3000ms pre-deadline window DEFEATED; MUST set `OTEL_EXPORTER_OTLP_TRACES_TIMEOUT` env var, (2) CRITICAL: BSP pops spans from internal deque BEFORE calling exporter — failed exports permanently destroy spans; `force_flush()` returns `True` regardless; code trusting return value as export confirmation gets false confidence, (3) HIGH: No SSE event-level trace correlation — audit Section 6 flags "SSE events to client — Trace IDs not included in SSE event payloads"; HTTP-level trace headers correlate at connection level only; operators cannot trace a specific received sentiment event to its backend DynamoDB poll span, (4) HIGH: ADOT collector config YAML has no pre-deployment validation gate — no JSON Schema exists; `otelcol-contrib validate` reports false errors on ADOT components; must use ADOT-specific binary `validate` subcommand in CI, (5) HIGH: FR-109's four verification gates are abstract with no concrete check definitions — gate (d) "100 manual spot-checks" has no automation; gates must be executable checks, (6) HIGH: ReadableStream error taxonomy missing — Fetch spec defines 4 distinct error types with different reconnection strategies; server MUST send sentinel `event: done` before graceful close to distinguish from network drops, (7) MEDIUM: X-Amzn-Trace-Id triggers CORS preflight — NOT CORS-safelisted; preflight adds 50-200ms on cache miss; must set Access-Control-Max-Age: 7200, (8) MEDIUM: X-Ray 50-annotation-per-segment limit — OTel span attributes mapped to annotations count against same limit; exceeding silently drops; need annotation budget allocation, (9) MEDIUM: treat_missing_data not systematically classified — canary alarms MUST use `breaching`; `notBreaching` on error-only metrics creates false-green when system dies, (10) MEDIUM: Rollback procedure lacks operational formalization — FR-110 preserves image but no trigger criteria or timeline, (11) MEDIUM: ADOT collector startup failures not monitored — distinct error log patterns not captured by metric filters.
**Round 8 Emergent Issues**: (1) SSE Lambda is **container-based** (ECR image with `python:3.13-slim` + custom bootstrap) — Lambda container images CANNOT use Lambda Layers (AWS docs: "You cannot add Lambda layers to container images"); FR-062 "collector-only layer" and FR-063 "version-pinned layer ARN" are **INVALID as written** — ADOT must be embedded in Docker image via multi-stage build from `public.ecr.aws/aws-observability/aws-otel-lambda-extension-amd64`; FR-062/FR-063 REWRITTEN, FR-069/FR-070 added for container-based deployment, (2) OTel `AwsLambdaResourceDetector` does NOT set `service.name` — defaults to `unknown_service`, which X-Ray uses as the service map node name, making the SSE Lambda indistinguishable from any other unconfigured service; `OTEL_SERVICE_NAME` env var is MANDATORY (FR-071), (3) `AwsLambdaResourceDetector` not specified in TracerProvider configuration — missing `cloud.provider`, `cloud.region`, `faas.name` resource attributes on all spans; FR-072 added, (4) `BatchSpanProcessor` default `schedule_delay_millis=5000` and `max_queue_size=2048` are designed for long-running servers, not Lambda — Lambda-tuned configuration required (FR-073), (5) Round 7 assumption about "zero Lambda layers" and "250MB deployment limit" based on WRONG deployment model — SSE Lambda is container-based with 10GB image limit, not ZIP with layer support; assumption INVALIDATED.
**Round 16 Emergent Issues** (2 CRITICAL, 5 HIGH, 3 MEDIUM): (1) CRITICAL: FR-124 creates unified `SilentFailure/Count` metric but NO alarm configuration specified — primary silent failure detection path is unwatched; FR-124 enumerates 5 paths but SC-004 expects 7 (parallel fetcher missing, circuit breaker load/save not disambiguated); (2) CRITICAL: Audit confirms frontend has ZERO X-Ray trace header propagation on fetch() calls — CloudWatch RUM FetchPlugin injects headers pre-fetch and does NOT interfere with ReadableStream (confirmed by source inspection), but injection depends on `addXRayTraceIdHeader: true` config, URL filter, AND CORS Allow-Headers — if any prerequisite fails, header silently dropped; (3) HIGH: Canary "out-of-band alerting channel" (FR-021, FR-050) referenced 10+ times but channel type never specified — if CloudWatch alarm, then CloudWatch degradation kills both monitoring and alert (circular dependency); must be cross-region SNS; (4) HIGH: SNS `AWSTraceHeader` automatic propagation is unverified assumption with no deployment gate — confirmed to work but BREAKS if `rawMessageDelivery: true` on any subscription (source: AWS SNS docs); 9 prior assumptions invalidated in this spec; (5) HIGH: API Gateway `IntegrationLatency` P99 < 5s SLO has no alarm — Lambda-side alarms miss API GW authorization/WAF/integration overhead; Function URLs use different metrics (`Url5xxError`); (6) HIGH: `force_flush()` total duration unbounded when ADOT Extension is HUNG — BSP exports 22 batches sequentially (confirmed by SDK source), each blocking up to 2s OTLP timeout = 44s total; SC-064 tests port-unreachable (fast) but not hung Extension; thread-based hard timeout wrapper required; (7) HIGH: ADOT Extension mid-stream crash produces zero telemetry and zero alerts until next canary (~10min) — OTel SDK silently catches exporter failures by spec design (confirmed by OTel specification); (8) MEDIUM: SSE reconnection creates new trace ID but no X-Ray Group for `session_id`-based session chain queries; (9) MEDIUM: FR-124's 5-path enumeration conflicts with SC-004's 7-path list — `parallel_fetcher_aggregate` and explicit `circuit_breaker_load`/`circuit_breaker_save` missing; (10) MEDIUM: FR-124's "structured log fallback line" format unspecified — FR-125's metric filter requires JSON (`{ $.metric_fallback = true }`) but no FR mandates JSON format for the fallback line; plaintext logs don't match JSON metric filter syntax.
**Round 17 Emergent Issues** (1 CRITICAL, 2 HIGH, 2 MEDIUM): (1) CRITICAL: Manual OTel spans on SSE Lambda have no requirement to call `span.set_status(StatusCode.ERROR)` on exception — ADOT X-Ray exporter ONLY sets X-Ray `fault=true` and populates `cause` field when OTel span StatusCode is ERROR; `span.record_exception()` alone is INVISIBLE to X-Ray (no fault flag, no cause, no Group filter match); affects SC-002, SC-004, SC-019 — every manually-created streaming span that errors without StatusCode.ERROR is a silent failure in X-Ray (source: ADOT X-Ray Exporter `cause.go` — `makeCause()` gated on `StatusCodeError`); (2) HIGH: Canary Lambda's exemption from FR-018 (fail-fast) not formalized — HL document states "sole exception is the canary" but no FR captures this; implementer following FR-018 literally makes canary crash on X-Ray initialization failure, defeating the "who watches the watcher" meta-observability architecture; (3) HIGH: SC-064 tests only port-unreachable ADOT failure mode — FR-139's thread-based hard timeout targets hung Extension (TCP accept, no OTLP response), which is a distinct failure mode where the OS-level connection succeeds but the application blocks indefinitely; port-unreachable fails fast via ECONNREFUSED and does NOT exercise the timeout wrapper; (4) MEDIUM: Canary Lambda is function #7 (new, not in FR-001's 6-Lambda scope) with contradictory requirements — must be instrumented (FR-029 pattern) for operator visibility when X-Ray is healthy, but must be resilient (FR-018 exempt) when X-Ray is down; dual role needs explicit scope; (5) MEDIUM: OTel SpanKind not specified for manual SSE Lambda spans — ADOT maps SpanKind.CLIENT to X-Ray remote subsegment, SpanKind.INTERNAL to local subsegment, SpanKind.SERVER to top-level segment; incorrect SpanKind causes phantom nodes in X-Ray service map, potentially violating SC-028 (source: ADOT X-Ray Exporter `segment.go` — `MakeSegment()` SpanKind routing).
**Round 18 Emergent Issues** (2 CRITICAL, 3 HIGH, 4 MEDIUM): (1) CRITICAL: FR-124 inline text says "five" silent failure paths but FR-142 corrected to 7 and FR-134 creates 7 alarms — implementer following FR-124 creates 5 metrics while FR-134 references 7 alarm ARNs, producing dangling alarms on non-existent metrics (permanent INSUFFICIENT_DATA); (2) CRITICAL: FR-135 Playwright deployment gate passes in controlled test but production cold page loads call fetch() before CloudWatch RUM FetchPlugin patches window.fetch — RUM FetchPlugin monkey-patches synchronously but the AwsRum client constructor itself loads asynchronously via CDN snippet; first fetch() races against RUM initialization with no readiness gate; (3) HIGH: Spans created between FR-093/FR-139 proactive flush and FR-100 generator termination have no guaranteed export path — flush drains BSP queue but new spans enqueued afterward sit in empty BSP until SIGKILL or environment freeze with no second flush; (4) HIGH: FR-144's dual-call requirement (record_exception + set_status) is automatically handled by OTel SDK's `start_as_current_span(record_exception=True, set_status_on_exception=True)` context manager for UNCAUGHT exceptions, but the real gap is CAUGHT exceptions in silent failure try/except blocks where exceptions do not propagate — these require manual dual-call; FR-144 does not distinguish the two cases; (5) HIGH: FR-145 exempts canary from fail-fast on cold start but does not address warm invocations — if TracerProvider initialization fails at module level on cold start, module-level code does NOT re-execute on warm invocations; canary remains permanently un-instrumented with no re-initialization attempt; (6) MEDIUM: FR-109 gate (d) says "100 traces manually spot-checked" (qualitative) while FR-117 says "automated script queries 100 traces" (quantitative) — no precedence defined and expected segment counts per request type undefined; (7) MEDIUM: FR-018 "System MUST fail fast" scoped to FR-001's "all 6 Lambda functions" — FR-145 exempts canary (Lambda #7) but no explicit scope statement defines behavior for FUTURE Lambdas; default should be fail-fast unless explicitly exempted; (8) MEDIUM: FR-132 detects HL document staleness via automated audit but mandates no remediation action — staleness detection that produces only a warning with no enforcement gate allows staleness to persist indefinitely, as demonstrated by the HL document itself being 2 rounds stale despite FR-132 existing; (9) MEDIUM: Zero FRs constrain PII in trace data — X-Ray annotations and metadata can contain user identifiers, email addresses, or sentiment text; no scrubbing requirement exists for span attributes, violating data minimization principles.
**Round 20 Emergent Issues** (2 CRITICAL, 4 HIGH, 5 MEDIUM): (1) CRITICAL: OTel Python SDK `BatchSpanProcessor` has a documented deadlock bug (opentelemetry-python#3886) where the worker thread holds a lock indefinitely — `force_flush()` hangs on the internal condition lock; FR-139's thread-based hard timeout terminates the flush call but spans locked in the BSP queue are permanently unrecoverable; this is a THIRD span-loss vector distinct from Extension hang (FR-139) and Extension drop (FR-074); FR-156 adds diagnostic differentiation; (2) CRITICAL: FR-154 staleness enforcement gate has never been applied — HL document is 1 full round stale (missing 9 FRs FR-147 through FR-155, 9 SCs SC-097 through SC-105, Round 18 Progress Log, Round 18 Risk Assessment entries) with no GitHub issue created as FR-154 mandates; detection-without-enforcement failure PROVEN across Rounds 16-20; FR-157 retroactively enforces with explicit deferral and GitHub issue creation; (3) HIGH: ADOT Lambda Extension cold start latency overhead unmeasured and unconstrained — AWS publishes NO official benchmarks (opentelemetry-lambda#263 confirms); community reports 200ms-2000ms+ at 1024MB; SSE Lambda is customer-facing with no cold start budget; FR-158 mandates measurement with < 2000ms P95 constraint; (4) HIGH: ADOT Extension IAM permissions not enumerated — FR-017 requires "X-Ray permissions" but does not list the 5 actions required (PutTraceSegments, PutTelemetryRecords, GetSamplingRules, GetSamplingTargets, GetSamplingStatisticSummaries); missing GetSampling* causes SILENT fallback to 1 trace/sec; FR-159 enumerates all 5 actions; (5) HIGH: Lambda RESPONSE_STREAM + ADOT Extension lifecycle interaction has NO canonical documentation — spec assumes Extension alive during entire streaming phase but AWS docs do not confirm for streaming mode; if Extension invoke phase ends on response header send, streaming spans lost; FR-160 mandates preprod verification gate; (6) HIGH: Function URL invocations sampled at 100% with parentbased_always_on — Function URL bypasses API Gateway (no parent sampling context), every invocation is a root span at 100% sampling; at $5/million traces, burst traffic creates unbounded cost; FR-161 adds centralized sampling rule and daily cost anomaly alarm; (7) MEDIUM: SC-064 conflates two distinct failure modes (port-unreachable ECONNREFUSED vs hung Extension TCP-accept-no-response) in a single success criterion — split into SC-064a and SC-064b; (8) MEDIUM: treat_missing_data alarm-by-alarm classification not specified — FR-121/FR-131 classify by type but no exhaustive per-alarm mapping exists; FR-162 provides complete classification; (9) MEDIUM: ADOT Extension localhost OTLP endpoint accepts unauthenticated traces — Lambda isolation limits risk but compromised dependency could inject misleading trace data; documented as accepted risk; (10) MEDIUM: X-Ray 30-day retention with no archival strategy — post-incident analysis beyond 30 days loses all trace data; FR-163 adds manual archival procedure; (11) MEDIUM: Work order files (fix-*.md) contain only Round 1-6 FRs — 100+ FRs unmapped in implementation guides; FR-164 mandates synchronization before implementation.

## User Scenarios & Testing *(mandatory)*

### User Story 1 - Operator Traces a Request End-to-End in X-Ray Console (Priority: P1)

An operator investigating a slow or failed request opens the AWS X-Ray console and sees a complete service map and trace from browser through API Gateway, through every Lambda invocation, through every downstream call (DynamoDB, SNS, SendGrid), and back. No gaps. No disconnected segments. The operator never needs to leave X-Ray to understand what happened.

**Why this priority**: This is the core value proposition. Without end-to-end traces, operators must correlate across multiple tools (CloudWatch Logs Insights, custom correlation IDs, dashboard widgets), which costs investigation time during incidents and increases mean-time-to-resolution (MTTR).

**Independent Test**: Can be fully tested by triggering a single user request (e.g., ticker search) and verifying in the X-Ray console that one continuous trace spans from RUM through API Gateway through Dashboard Lambda through DynamoDB. Delivers immediate operator value.

**Acceptance Scenarios**:

1. **Given** a user searches for ticker "AAPL" in the browser, **When** the operator opens X-Ray traces for that time window, **Then** a single trace ID links the browser RUM segment, the API Gateway segment, the Dashboard Lambda segment (with all subsegments for auth, alerts, DynamoDB calls), and the DynamoDB segment.
2. **Given** the Ingestion Lambda processes a batch of articles and publishes to SNS, **When** the Analysis Lambda is invoked by that SNS message, **Then** both Lambda invocations appear under the same X-Ray trace ID with the SNS hop visible as a connecting segment.
3. **Given** the Notification Lambda sends an email via SendGrid, **When** the operator views the trace, **Then** the SendGrid HTTP call appears as an explicitly instrumented subsegment (not auto-instrumented — SendGrid's HTTP transport is not auto-patched) with status code, duration, and recipient count.
4. **Given** a user connects to the SSE streaming endpoint, **When** the operator views the trace, **Then** the connection lifecycle (authentication, connection acquisition, DynamoDB polling, event serialization) appears as named subsegments with timing data.
5. **(Round 3)** **Given** a request that results in an error/fault under any sampling configuration, **When** the operator searches X-Ray for errored traces, **Then** the complete trace is present including all subsegments and annotations — errored requests are never lost to sampling.

---

### User Story 2 - Operator Diagnoses Silent Failure via X-Ray Error Annotations (Priority: P1)

An operator notices stale dashboard data. Instead of grepping CloudWatch Logs, the operator filters X-Ray traces by error/fault status and immediately sees which subsegment failed — circuit breaker persistence, audit trail write, SNS notification publish, time-series fanout partial write, or self-healing item fetch.

**Why this priority**: Equal to P1 because silent failure paths are the most dangerous blind spots. The audit identified 7 silent failure paths where errors are caught, logged, but emit no metric and create no X-Ray subsegment. These paths currently require log-grepping to diagnose.

**Independent Test**: Can be tested by injecting a DynamoDB throttle on the circuit breaker table and verifying that the X-Ray trace shows an error annotation on the circuit breaker subsegment with the throttle details.

**Acceptance Scenarios**:

1. **Given** DynamoDB throttles the circuit breaker table during Ingestion Lambda execution, **When** the circuit breaker load or save fails, **Then** the X-Ray trace contains a subsegment named "circuit_breaker_load" or "circuit_breaker_save" marked as error with the exception details.
2. **Given** the audit trail DynamoDB write fails during ingestion, **When** the operator filters X-Ray traces by fault, **Then** a subsegment named "audit_trail_persist" appears with the ClientError details.
3. **Given** SNS publish fails in the notification publisher, **When** the operator views the trace, **Then** a subsegment named "downstream_notification_publish" is marked as fault with the SNS error.
4. **Given** a BatchWriteItem has unprocessed items after retries during time-series fanout, **When** the operator views the trace, **Then** a subsegment named "timeseries_fanout_batch_write" shows an error annotation with the count of unprocessed items and affected resolutions.
5. **Given** self-healing skips an item due to a fetch failure, **When** the operator views the trace, **Then** a subsegment named "self_healing_item_fetch" is marked as error with the failed source_id and exception.

---

### User Story 3 - Operator Views SSE Streaming Latency and Cache Performance in X-Ray (Priority: P2)

An operator investigating SSE live update latency opens X-Ray and sees per-event latency as subsegment durations and cache hit rate as annotations — without needing CloudWatch Logs Insights pctile() queries.

**Why this priority**: SSE streaming currently has only 1 X-Ray subsegment (stream_status) despite being the most latency-sensitive Lambda. All latency and cache data is in custom structured logs, requiring a separate CloudWatch Logs Insights query tool. Consolidating onto X-Ray provides single-pane observability.

**Architectural constraint (Round 2 finding, revised in Round 3)**: The SSE Lambda uses `RESPONSE_STREAM` invoke mode with Lambda Function URLs. The Lambda runtime's auto-created X-Ray segment closes when the handler returns the generator — but streaming continues after that. All DynamoDB polling, event dispatch, and CloudWatch metric calls happen DURING streaming, AFTER the segment closes.

~~Round 2 proposed creating "independent X-Ray segments" during streaming linked to the original trace ID.~~ **INVALIDATED in Round 3**: The X-Ray SDK's `begin_segment()` is a documented no-op in Lambda. The SDK's `LambdaContext.put_segment()` silently discards segments (source: `aws_xray_sdk/core/lambda_launcher.py:55-59`), and the `FacadeSegment` raises `FacadeSegmentMutationException` on all mutation operations. Independent segments CANNOT be created within Lambda.

**Round 3 corrected approach**: The system MUST use a tracing mechanism with a lifecycle independent of the Lambda runtime's X-Ray segment. This mechanism must create trace spans during response streaming, link them to the original invocation's trace ID, and export them to X-Ray. The mechanism runs as a separate process within the Lambda execution environment (e.g., a Lambda Extension) that survives after the handler returns and has its own shutdown phase for flushing buffered trace data.

**Independent Test**: Can be tested by connecting to the SSE streaming endpoint, waiting for events, and verifying X-Ray traces contain latency annotations and cache hit rate annotations on spans linked to the connection's trace ID.

**Acceptance Scenarios**:

1. **Given** the SSE Lambda processes a DynamoDB change and sends an event to a connected client, **When** the operator views the trace, **Then** a span named "sse_event_dispatch" contains annotations for `event_type`, `latency_ms`, and `is_cold_start`, linked to the original invocation trace ID.
2. **Given** the SSE Lambda polls DynamoDB for sentiment updates, **When** the operator views the trace, **Then** a span named "dynamodb_poll" contains annotations for `item_count`, `changed_count`, and `poll_duration_ms`.
3. **Given** the SSE Lambda cache reaches a periodic logging trigger, **When** the operator views the trace, **Then** the current span contains annotations for `cache_hit_rate`, `cache_entry_count`, and `cache_max_entries`.
4. **Given** a new SSE connection is acquired, **When** the operator views the trace, **Then** a span named "connection_acquire" contains annotations for `connection_id`, `current_count`, and `max_connections`.

---

### User Story 4 - Operator Traces Browser-to-Backend via X-Ray Headers (Priority: P2)

An operator investigating a specific user's slow experience uses the RUM trace ID to find the exact backend trace. The browser's X-Ray trace ID propagates through the frontend API client and SSE proxy into backend Lambdas, creating one continuous trace from browser to database.

**Why this priority**: Currently, CloudWatch RUM captures browser traces and backend Lambdas capture server traces, but they are disconnected. The frontend does not propagate `X-Amzn-Trace-Id` headers, breaking the client-to-server correlation chain.

**Architectural constraint (Round 3 finding)**: The standard `EventSource` API does not support custom HTTP headers (WHATWG HTML Living Standard, Section 9.2). CloudWatch RUM's automatic trace header injection only patches `window.fetch()`, not `EventSource`. To propagate `X-Amzn-Trace-Id` headers on SSE connections, the frontend MUST use `fetch()` with `ReadableStream` consumption instead of `EventSource`. This loses `EventSource`'s built-in auto-reconnection, which must be reimplemented.

**Independent Test**: Can be tested by making a browser API request, extracting the RUM trace ID, and verifying the same trace ID appears in the backend Lambda's X-Ray trace.

**Acceptance Scenarios**:

1. **Given** a browser makes an API call to the Dashboard endpoint, **When** the frontend API client sends the request, **Then** the request includes an `X-Amzn-Trace-Id` header containing the active RUM trace ID.
2. **Given** a browser connects to the SSE endpoint via the Next.js proxy, **When** the proxy forwards the request to the SSE Lambda, **Then** the proxy propagates the `X-Amzn-Trace-Id` header from the incoming request to the upstream SSE Lambda call.
3. **Given** the CORS configuration for both API Gateway and Lambda Function URLs, **When** a browser sends an `X-Amzn-Trace-Id` header, **Then** the CORS preflight response includes `X-Amzn-Trace-Id` in `Access-Control-Allow-Headers`.
4. **(Round 3)** **Given** the frontend SSE client uses `fetch()` + `ReadableStream` instead of `EventSource`, **When** the connection drops, **Then** the client reconnects with exponential backoff, jitter, and `Last-Event-ID` header propagation — matching the reliability guarantees of `EventSource` auto-reconnection.

---

### User Story 5 - Operator Replaces Custom Correlation IDs with X-Ray Trace IDs (Priority: P3)

Operators and automated systems that currently search for custom `{source_id}-{request_id}` correlation IDs in CloudWatch Logs instead use X-Ray trace IDs as the universal correlation key. The custom correlation ID system is removed.

**Why this priority**: The custom correlation system in `metrics.py:get_correlation_id()` creates a parallel tracing universe. Operators must know to search both X-Ray and CloudWatch Logs with different ID formats. Consolidating on X-Ray trace IDs as the single correlation key eliminates this cognitive overhead.

**Independent Test**: Can be tested by processing an article through ingestion, finding the X-Ray trace ID, and verifying that the same trace ID appears in structured log output as the correlation key (replacing the old `{source}-{request_id}` format).

**Acceptance Scenarios**:

1. **Given** the Ingestion Lambda processes an article, **When** it emits structured logs, **Then** the log field `correlation_id` contains the active X-Ray trace ID (format: `1-{hex_timestamp}-{hex_id}`) instead of the custom `{source_id}-{request_id}` format.
2. **Given** the custom `get_correlation_id()` function and `generate_correlation_id()` function, **When** this feature is complete, **Then** both functions are removed and all call sites use the X-Ray trace ID instead.
3. **Given** any Lambda function emitting structured logs, **When** the log entry is written, **Then** the X-Ray trace ID is automatically included as a top-level field so CloudWatch Logs Insights queries can join with X-Ray traces.

---

### User Story 6 - X-Ray Canary Validates Tracing Infrastructure Health (Priority: P3)

A lightweight, non-X-Ray canary periodically verifies that X-Ray itself is operational and that trace data is not being silently lost. This is the single permitted exception to the "X-Ray exclusive" rule — the "watcher of the watcher" that detects X-Ray regional degradation or throttling-induced data loss before it causes cascading alarm blindness.

**Why this priority**: The audit identified that if CloudWatch `put_metric_data` fails, the entire observability system goes dark. Analogously, if X-Ray ingestion fails or is throttled, all the new tracing coverage becomes invisible. A canary that does NOT depend on X-Ray must verify X-Ray is working.

**Independent Test**: Can be tested by running the canary and verifying it reports X-Ray health status via a non-X-Ray channel.

**Acceptance Scenarios**:

1. **Given** the canary runs on its scheduled interval, **When** it submits a known test trace to X-Ray and queries for it, **Then** within the expected propagation window, the trace is retrievable and the canary reports success.
2. **Given** X-Ray ingestion is degraded (simulated by IAM permission revocation in test), **When** the canary's test trace is not retrievable within the expected window, **Then** the canary reports failure via a non-X-Ray channel (CloudWatch metric with `treat_missing_data = breaching`).
3. **Given** the canary itself fails to run, **When** the expected canary metric is absent, **Then** the CloudWatch alarm with `treat_missing_data = breaching` fires, alerting the operator that the watcher is down.
4. **(Round 3)** **Given** the canary submits N test traces per interval, **When** fewer than N are retrievable within the query window, **Then** the canary reports a `trace_data_loss_ratio` metric, enabling detection of partial data loss from X-Ray throttling.

---

### User Story 7 - Metrics Lambda Gains Full X-Ray Instrumentation (Priority: P2)

The Metrics Lambda — which is the monitoring system's core — gains X-Ray instrumentation so that failures in the monitoring system itself are traceable. Currently, the Metrics Lambda has zero X-Ray SDK integration despite having Active tracing enabled in Terraform.

**Why this priority**: The Metrics Lambda is the "monitor of monitors." If it fails, custom metrics stop being emitted, and alarms that depend on those metrics either fire incorrectly or resolve to green (depending on `treat_missing_data`). Having no X-Ray visibility into its execution makes diagnosing these failures impossible.

**Independent Test**: Can be tested by invoking the Metrics Lambda and verifying X-Ray shows subsegments for DynamoDB queries and CloudWatch metric emission.

**Acceptance Scenarios**:

1. **Given** the Metrics Lambda is invoked, **When** it queries DynamoDB for stuck items, **Then** the X-Ray trace contains a subsegment for the DynamoDB query with item count and duration.
2. **Given** the Metrics Lambda emits CloudWatch metrics, **When** the `put_metric_data` call succeeds, **Then** the X-Ray trace contains a subsegment for the CloudWatch API call.
3. **Given** the Metrics Lambda's `put_metric_data` call fails (throttled, permission denied), **When** the operator views the trace, **Then** the subsegment is marked as error with the exception details.

---

### User Story 8 - Guaranteed Error Trace Capture (Priority: P1) *(Round 3 — New)*

An operator investigating a production error is guaranteed to find an X-Ray trace for that errored request. The system ensures that all error/fault traces are captured and queryable, regardless of traffic volume or sampling configuration. Successful requests may be sampled, but errors are always traced.

**Why this priority**: X-Ray sampling decisions are made BEFORE the request completes (at the entry point, the outcome is unknown). Under default sampling (1 request/second + 5%), most errored requests in a high-traffic window would be untraced. For a system that uses X-Ray as its EXCLUSIVE debugging tool, losing error traces makes incidents undiagnosable. This is a P1 because it directly affects the viability of Story 1 and Story 2 during real incidents.

**Independent Test**: Can be tested by configuring sampling, generating 100 requests that result in errors, and verifying that 100% of errored traces are retrievable in X-Ray.

**Acceptance Scenarios**:

1. **Given** the system is running with production sampling rules, **When** a Lambda invocation results in an error/fault, **Then** the complete trace (including all subsegments and annotations) is captured and queryable in X-Ray.
2. **Given** an X-Ray Group configured with the filter `fault = true OR error = true`, **When** errored traces are captured, **Then** the group generates CloudWatch metrics (error count, error rate, latency percentiles), enabling alarm-based monitoring from trace data.
3. **Given** dev/preprod environments, **When** any request is made, **Then** 100% of requests are traced (no sampling), ensuring complete debuggability during development.
4. **Given** the production sampling rate controls cost for successful requests, **When** the monthly X-Ray cost exceeds configured thresholds ($10, $25, $50), **Then** a CloudWatch billing alarm fires.

---

### User Story 9 - Trace Data Integrity Protection (Priority: P2) *(Round 3 — New)*

An operator is alerted when trace data is being silently lost due to X-Ray throttling, daemon failure, or systematic SDK errors. The system treats undetected trace data loss as a first-class failure mode, because an operator who trusts X-Ray traces that aren't there will misdiagnose issues.

**Why this priority**: X-Ray has a hard throughput limit (2,600 segments/second/region). When exceeded, `PutTraceSegments` returns throttled segments in `UnprocessedTraceSegments` — data is silently lost, not queued, no alarm fires. If the system's EXCLUSIVE debugging tool is losing data without alerting, operators have a false sense of complete observability.

**Independent Test**: Can be tested by configuring the canary to track submission-vs-retrieval ratios and verifying that simulated data loss (temporary IAM revocation or throughput saturation) triggers an alert within 2 canary intervals.

**Acceptance Scenarios**:

1. **Given** the X-Ray canary submits N test traces per interval, **When** fewer than N traces are retrievable within the query window, **Then** the canary emits a `trace_data_loss_ratio` metric and the CloudWatch alarm fires when the ratio exceeds the configured threshold.
2. **Given** the system approaches the X-Ray region throughput limit, **When** the canary detects consecutive intervals with data loss, **Then** an operator is alerted to investigate throttling or daemon health.

---

### User Story 10 - Operator Is Alerted to All Failure Modes Regardless of X-Ray Sampling (Priority: P1) *(Round 4 — New)*

An operator is automatically alerted when any Lambda function errors, when latency breaches SLO thresholds, or when critical application metrics indicate degradation — regardless of whether the specific request was X-Ray sampled. CloudWatch alarms on Lambda built-in metrics and custom application metrics provide the 24/7 alerting signal; X-Ray traces provide the diagnostic context when operators investigate.

**Why this priority**: The audit identified 2 Lambda functions (SSE Streaming, Metrics) with zero error alarms and 7 custom metrics emitted without alarms. Without alarms, operators only discover these failures from customer complaints or by staring at dashboards. This directly undermines the value of X-Ray traces — operators don't know to look at X-Ray if nothing alerts them to a problem.

**Scope clarification**: "X-Ray exclusive" applies to **tracing** — the mechanism for distributed request tracing, latency analysis, and dependency visualization. CloudWatch alarms on Lambda built-in metrics (Errors, Duration, Throttles) and custom application metrics are a separate, complementary system for **operational alerting**. Alarms tell operators "something is wrong"; X-Ray traces tell operators "what went wrong and where." Both are required. CloudWatch alarms are NOT duplicative with X-Ray.

**Independent Test**: Can be tested by injecting errors into each Lambda function and verifying CloudWatch alarms fire within the configured evaluation period.

**Acceptance Scenarios**:

1. **Given** the SSE Streaming Lambda errors, **When** the error count exceeds the configured threshold within the evaluation period, **Then** a CloudWatch alarm fires and notifies the on-call operator.
2. **Given** the Metrics Lambda errors, **When** the error count exceeds the configured threshold within the evaluation period, **Then** a CloudWatch alarm fires — alerting operators that the monitoring system itself has failed.
3. **Given** items remain stuck in "pending" status beyond the SLO threshold, **When** the StuckItems metric exceeds threshold, **Then** a CloudWatch alarm fires, alerting operators to stale dashboard data before customers notice.
4. **Given** SSE connection pool exhaustion, **When** ConnectionAcquireFailures exceeds threshold, **Then** a CloudWatch alarm fires, alerting operators that new SSE connections are being rejected.
5. **Given** SSE live update latency breaches the p95 < 3000ms SLO, **When** EventLatencyMs p95 exceeds threshold, **Then** a CloudWatch alarm fires.

---

### User Story 11 - Meta-Observability for Monitoring Infrastructure (Priority: P1) *(Round 4 — New)*

An operator is alerted when the monitoring infrastructure itself fails — whether X-Ray trace ingestion, CloudWatch metric emission, or the canary itself. A broken monitoring system that silently reports "all green" is the single most dangerous failure mode because it prevents detection of all other failures.

**Why this priority**: The audit Section 8.1 identifies CloudWatch `put_metric_data` failure as "the single most dangerous blind spot." If metric emission fails, alarms with `treat_missing_data = notBreaching` resolve to OK (false green), while alarms with `treat_missing_data = breaching` fire correctly but only cover those specific alarms. The X-Ray canary (US6) addresses X-Ray health but not CloudWatch health. The monitoring system needs a unified meta-observability canary that verifies both the tracing plane (X-Ray) and the alerting plane (CloudWatch metrics) are functional.

**Independent Test**: Can be tested by temporarily revoking the application Lambda's CloudWatch permissions and verifying the canary detects the emission failure within 2 intervals.

**Acceptance Scenarios**:

1. **Given** the canary emits a test metric to CloudWatch and queries for it after the ingestion delay, **When** the metric is retrievable, **Then** the canary reports CloudWatch metric pipeline health as healthy.
2. **Given** CloudWatch `put_metric_data` is failing for application Lambdas (simulated via IAM revocation), **When** the canary's test metric query returns empty results, **Then** the canary reports failure via a channel independent of CloudWatch alarms.
3. **Given** all alarms where metric absence indicates failure, **When** the `treat_missing_data` configuration is audited, **Then** 100% of those alarms use `breaching`.
4. **Given** the canary uses a separate IAM role from application Lambdas, **When** application IAM permissions are changed, **Then** the canary retains its ability to emit and query metrics, ensuring it can detect the failure.

---

### Edge Cases

- What happens when the X-Ray daemon is throttled (SDK rate limiting)? Subsegments beyond the sampling rate are dropped, but the Lambda still executes normally. The X-Ray SDK handles throttling internally; the system must not add error-suppression around it (see FR-018).
- What happens when a long-running SSE connection spans multiple X-Ray trace windows (traces have a 5-minute default window)? Each DynamoDB poll cycle within the SSE Lambda creates its own trace. The connection lifecycle subsegments must be scoped to the poll cycle, not the entire connection.
- What happens when the X-Ray canary reports failure but X-Ray is actually healthy (false positive)? The canary query window must account for X-Ray's eventual consistency delay (typically 5-30 seconds). The canary MUST NOT trigger on a single missed query — require consecutive failures.
- What happens when CloudWatch RUM sampling is reduced in production (10% sampling)? Only 10% of browser sessions generate RUM trace IDs. For the remaining 90%, the browser-side API client MUST still generate a valid `X-Amzn-Trace-Id` header so backend traces are created regardless of RUM sampling. Note: this applies to the browser-side API client only; the server-side SSE proxy (see next edge case) has different behavior.
- What happens when the SSE proxy (server-side) does not have an incoming `X-Amzn-Trace-Id` header? The server-side proxy MUST NOT generate a synthetic trace ID. Let the Lambda runtime assign the trace ID. Generating server-side trace IDs without proper SDK context would produce invalid trace context. The boundary is: browsers generate trace IDs (via RUM SDK); server-side proxies forward but never originate them.
- What happens when X-Ray annotation limits are reached? X-Ray allows a maximum of 50 annotations per subsegment, with values up to 2,048 characters. The annotations defined in FR-006 through FR-009 total fewer than 15 per subsegment. If future extensions approach the limit, non-essential annotations must be moved to X-Ray metadata (non-indexed, no limit).
- What happens to CloudWatch alarms, Log Insights saved queries, or dashboards that reference the old custom correlation ID format or the structured log fields being removed? Any downstream consumers of the removed systems (latency_logger fields, cache_logger fields, `correlation_id` in the old format) MUST be audited and updated as part of this feature. Failing to update them would cause silent breakage.
- What is the relationship between X-Ray sampling and the "any user request" tracing claim? X-Ray uses sampling (default: 1 request/second + 5% of additional requests). SC-001 applies to sampled requests. Dev/preprod uses 100% sampling. Production sampling must guarantee error trace capture (see FR-034) while controlling cost for successful requests.
- **(Round 2)** What happens when the SSE Lambda's X-Ray segment closes before streaming completes? The Lambda runtime creates a segment on invocation and closes it when the handler returns the generator. But `RESPONSE_STREAM` mode means the bootstrap continues polling the generator for chunks AFTER the handler returns. All boto3 calls during streaming (DynamoDB polls, CloudWatch metric emission) happen after segment closure. ~~The system MUST create independent segments during streaming, linked to the original trace ID.~~ **INVALIDATED in Round 3**: `begin_segment()` is a no-op in Lambda (see Assumption 7 invalidation). The system MUST use a tracing mechanism with an independent lifecycle (see FR-026 revised).
- **(Round 2)** What happens when `asyncio.new_event_loop()` is used in the async-to-sync bridge? The default `threading.local()` context storage DOES correctly propagate X-Ray context through `asyncio.new_event_loop().run_until_complete()` on the same thread. However, the X-Ray SDK's `AsyncContext` (which uses `TaskLocalStorage`) loses context across event loop boundaries. The system MUST NOT configure `AsyncContext` (see FR-037).
- **(Round 2)** What happens with X-Ray annotation type validation? The X-Ray SDK accepts `str`, `int`, `float`, and `bool` as annotation values. Any other type (including `None`, `list`, `dict`) is silently dropped with only a `log.warning()`. All annotations defined in FR-006 through FR-009 use valid types (confirmed: `latency_ms` is int, `cache_hit_rate` is float, `is_cold_start` is bool). Implementers MUST NOT pass `None` values — use a sentinel value or omit the annotation.
- **(Round 2)** What happens when raw `@xray_recorder.capture` is used instead of Powertools `@tracer.capture_method`? Raw xray_recorder does NOT automatically capture exceptions as subsegment errors. If an exception is raised inside a `@xray_recorder.capture`-decorated function, the subsegment is closed but not marked as error/fault. Powertools Tracer does this automatically. The spec's FR-005 requirement (mark subsegments as error on exception) is NOT satisfied by raw xray_recorder alone — either use Powertools Tracer or add manual exception capture.
- **(Round 3)** What happens when `@tracer.capture_method` is applied to an async generator function? The decorator does NOT check `inspect.isasyncgenfunction()` (confirmed: zero matches in Powertools codebase). It falls through to `_decorate_sync_function()`, which wraps the generator CREATION (near-zero time) rather than ITERATION. The subsegment opens and closes instantly, capturing no meaningful timing. Additionally, the decorator destroys the async generator's type signature (`inspect.isasyncgenfunction()` returns `False` on the wrapped function). System MUST NOT use `@tracer.capture_method` on async generators (see FR-031).
- **(Round 3)** What happens when the frontend uses `EventSource` for SSE connections? The `EventSource` API does not support custom HTTP headers (WHATWG HTML Living Standard, Section 9.2). The constructor accepts only a URL and optional `withCredentials`. CloudWatch RUM's automatic trace header injection patches `window.fetch()` but NOT `EventSource`. Trace headers CANNOT be propagated via `EventSource`. The frontend MUST use `fetch()` + `ReadableStream` for SSE connections that require trace propagation (see FR-032).
- **(Round 3)** What happens when a malicious client sends `Sampled=1` in the `X-Amzn-Trace-Id` header? X-Ray respects the incoming `Sampled` field as the authoritative sampling decision when it is already decided (not `?`). A client that always sends `Sampled=1` forces 100% server-side sampling, inflating costs at $5 per million traces. Server-side sampling rules MUST override client-supplied decisions (see FR-035).
- **(Round 3)** What happens when X-Ray throughput exceeds the region limit (2,600 segments/second)? `PutTraceSegments` returns HTTP 429 `ThrottledException`. Throttled segments appear in the `UnprocessedTraceSegments` response array — data is silently lost, not queued, not retried. The X-Ray daemon buffers and batches, providing some burst capacity, but sustained throughput above the limit results in permanent data loss with no alarm or notification.
- **(Round 3)** What happens when the SSE Lambda adds the ADOT Lambda Extension for independent lifecycle tracing? Lambda Extensions add cold start overhead (typically 50-200ms for ADOT) and memory overhead (~40-60MB). For the SSE Lambda with a 15-second streaming lifecycle, the relative impact is small. The extension's INIT phase runs concurrently with the Lambda runtime's INIT, partially masking the overhead.
- **(Round 3)** What happens when traffic grows beyond cost-effective 100% sampling? At current traffic levels (<1M requests/month), 100% sampling costs <$5/month. If traffic grows to 100M requests/month, 100% sampling costs ~$500/month. The X-Ray cost budget alarm (FR-038) provides early warning. The scaling path is tail-based sampling via an external OpenTelemetry Collector with a tail sampling processor, which makes sampling decisions AFTER spans complete — always keeping error traces while dropping a percentage of successful ones. This is documented as a future upgrade path, not a current requirement.
- **(Round 4)** What happens if CloudFront is re-introduced to the architecture? CloudFront treats `X-Amzn-Trace-Id` as a restricted header and replaces the client-sent value with its own trace ID. If CloudFront is added between browser and API Gateway, the RUM-generated trace context from the browser would be lost. The mitigation would be to rely on CloudFront's own X-Ray edge segment rather than browser-originated trace IDs. Source: AWS CloudFront documentation — `X-Amzn-Trace-Id` is listed as a restricted header that cannot be overridden via custom headers policies.
- **(Round 4)** What happens when ADOT auto-instrumentation is accidentally enabled on the SSE Lambda via `AWS_LAMBDA_EXEC_WRAPPER=/opt/otel-handler`? ADOT's `AwsLambdaInstrumentor` wraps the handler entry point (conflicting with Powertools `@tracer.capture_lambda_handler`), and `BotocoreInstrumentor` patches botocore (conflicting with X-Ray SDK patching from Powertools). Every boto3 call produces duplicate spans/subsegments in X-Ray. The env var MUST NOT be set on any Lambda that uses Powertools Tracer.
- **(Round 4)** What happens when the OTel `service.name` resource attribute and Powertools `POWERTOOLS_SERVICE_NAME` differ on the SSE Lambda? X-Ray service map aggregates nodes by service name. Mismatched names cause the same Lambda to appear as two disconnected nodes — one for handler-phase subsegments (Powertools) and one for streaming-phase spans (ADOT). Both names MUST be identical for a unified service map.
- **(Round 4)** What happens when an SSE connection drops and the client reconnects with `fetch()` + `ReadableStream`? The new `fetch()` call gets a new trace ID from RUM. The old trace (showing the connection drop) and new trace (showing the reconnection) are disconnected in X-Ray. X-Ray does not support native OpenTelemetry span links — link data stored in segment metadata is not surfaced in the X-Ray console or queryable via API. The system uses annotation-based correlation: a stable `session_id` annotation on all SSE traces enables querying all traces for a logical session, and a `previous_trace_id` annotation on reconnection traces provides explicit backward reference.
- **(Round 4)** What happens when X-Ray sampling rate is <100% in production and an error occurs on an unsampled request? The error is captured by the CloudWatch Lambda `Errors` metric (100% fidelity, independent of X-Ray sampling), but the X-Ray trace for that specific errored request does not exist — unsampled traces (`Sampled=0`) are permanently lost because data is never sent to the X-Ray service. Operators use CloudWatch alarms for alerting (100% error detection) and X-Ray traces for diagnosis (available for sampled requests). At current traffic levels with 100% sampling, this gap does not manifest.
- **(Round 4)** What is the relationship between X-Ray Groups and X-Ray sampling? X-Ray Groups are post-ingestion filters that only operate on already-sampled, already-ingested traces. A Group with filter `fault = true` generates CloudWatch metrics only from faulted traces that were sampled and ingested. If a request errors but was not sampled (`Sampled=0`), no X-Ray data exists and no Group will ever see it. Groups are a monitoring tool for sampled trace data, not a mechanism to ensure error capture. This is why CloudWatch alarms on Lambda built-in metrics (which capture 100% of invocations regardless of X-Ray) are required alongside X-Ray Groups.
- **(Round 5)** What happens when the OTel SDK's `TracerProvider` uses the default `RandomIdGenerator` instead of `AwsXRayIdGenerator`? The first 4 bytes of generated trace IDs are random instead of a valid unix timestamp. When the ADOT exporter converts to X-Ray format (`1-{first_8_hex}-{remaining_24_hex}`), the epoch field contains a garbage timestamp. X-Ray indexes traces by time window using this embedded timestamp — traces with invalid epochs may not appear in time-range queries, may be placed in wrong time buckets, or may be rejected entirely. All OTel `TracerProvider` instances on Lambdas that export to X-Ray MUST use `AwsXRayIdGenerator`. Source: `opentelemetry-sdk-extension-aws` package — `AwsXRayIdGenerator` embeds `int(time.time())` in the first 4 bytes.
- **(Round 5)** What is the X-Ray segment document size limit? Each emitted segment or subsegment document is capped at **64KB** by the `PutTraceSegments` API. Documents exceeding this limit are silently rejected and appear in `UnprocessedTraceSegments` in the API response — no error is thrown at the SDK level. The X-Ray SDK mitigates this by emitting completed subsegments as independent documents (with `parent_id` referencing the parent segment). However, a single subsegment with very large metadata (full HTTP response bodies, deep stack traces, large error messages) can still exceed 64KB. All metadata payloads MUST be bounded — truncate error messages to 2048 characters and never attach full response bodies as metadata. Source: AWS X-Ray API Reference, `PutTraceSegments` — "Each segment document can be up to 64 kB."
- **(Round 5)** What happens when the SSE Lambda's execution environment freezes without calling `force_flush()` on the OTel `TracerProvider`? The `BatchSpanProcessor` holds buffered spans in memory with a configurable batch timeout (FR-073 tunes to 1000ms; default is 5000ms — see FR-084 for correction of the original 200ms assumption). When the execution environment freezes after streaming completes, any spans created in the final batch interval that haven't been batched yet are suspended in memory. On the NEXT invocation (when the environment thaws), these stale spans from the PREVIOUS invocation are exported — carrying previous trace IDs that confuse operators (spans from invocation N appearing during invocation N+1's processing). Calling `force_flush(timeout_millis=5000)` in the generator's `finally` block ensures all spans reach the ADOT Extension's local OTLP receiver (localhost, sub-millisecond) before the environment freezes.
- **(Round 5)** What happens when the ADOT Extension's SHUTDOWN timeout is insufficient to flush all buffered spans to X-Ray? The Extension receives SHUTDOWN only when the execution environment is being destroyed (idle timeout, ~5-15 minutes of inactivity), NOT after each invocation. During SHUTDOWN, the ADOT collector calls `ForceFlush()` on all exporters with a default timeout of 2 seconds (configurable up to 10 seconds via `AWS_LAMBDA_EXTENSION_SHUTDOWN_TIMEOUT_MS`). If the X-Ray export takes longer, remaining spans are dropped. This is low risk because: (a) `force_flush()` in FR-055 pushes spans to the ADOT Extension's local OTLP receiver on each invocation (fast, localhost), (b) the ADOT batch processor exports to X-Ray incrementally during streaming (1000ms batch timeout per FR-073), (c) SHUTDOWN only occurs on environment destruction, not routine operation.
- **(Round 5)** What happens when the OTel SDK uses `always_on` sampler instead of `parentbased_always_on` on the SSE Lambda? When the Lambda runtime sets `Sampled=0` in `_X_AMZN_TRACE_ID` (unsampled request), the `AwsXRayLambdaPropagator` extracts this flag. With `parentbased_always_on`, the sampler respects the parent's decision and suppresses span creation — correct behavior. With `always_on`, the sampler ignores the parent's decision and creates spans regardless. These spans are exported to X-Ray via ADOT, but X-Ray has no corresponding Lambda facade segment (since the request was unsampled). The result is orphaned spans that appear as root-level traces with no parent, creating noise in the X-Ray console and inflating costs for unsampled traffic.
- **(Round 6)** What happens on warm invocations when OTel context is extracted at module level instead of per-invocation? The `_X_AMZN_TRACE_ID` environment variable changes on every Lambda invocation (the runtime updates it before calling the handler). If `propagate.extract(carrier={})` is called at module level (cold start only), the returned context captures the FIRST invocation's trace ID. All subsequent warm invocations' streaming spans link to this stale trace ID — creating cross-invocation trace contamination. This is invisible in cold-start-heavy dev testing but manifests in production under sustained load where warm invocations dominate. The `AwsXRayLambdaPropagator.extract()` method reads `os.environ` on EVERY call (no caching), so calling it at handler entry correctly provides the current invocation's context.
- **(Round 6)** What happens when Powertools Tracer's `auto_patch=True` is active on the SSE Lambda during the streaming phase? Powertools' auto-patching instruments boto3/botocore at module import time via the X-Ray SDK's `patch()` mechanism. These patches are GLOBAL — they affect ALL boto3 calls regardless of execution phase. During SSE streaming, a DynamoDB query creates: (a) an X-Ray subsegment via auto-patching (transmitted to X-Ray daemon, which may or may not accept data after handler returns — undocumented by AWS for RESPONSE_STREAM), AND (b) an OTel span via manual instrumentation (transmitted to ADOT Extension, reliable). If the X-Ray daemon continues accepting data during streaming, operators see DUPLICATE entries for each AWS SDK call. If the daemon stops, only OTel spans appear. The inconsistency is unpredictable. Setting `auto_patch=False` and using explicit `@tracer.capture_method` on handler-phase functions eliminates the dual-emission during streaming while preserving handler-phase tracing.
- **(Round 6)** What happens when the SSE Lambda's execution environment is destroyed without a SHUTDOWN event (e.g., during infrastructure updates or spot reclamation)? The ADOT Extension does not receive SHUTDOWN, so its final flush does not execute. Spans in the ADOT pipeline are lost. The `BatchSpanProcessor`'s in-process buffer is also lost. The canary (FR-019) detects the data loss on subsequent intervals. This is a rare edge case — standard Lambda lifecycle always includes SHUTDOWN before destruction.
- **(Round 6)** What happens when the ADOT Extension's OTLP receiver is not ready during Lambda cold start? During cold start, the ADOT Extension and Lambda runtime initialize concurrently. The ADOT Extension's OTLP receiver on `localhost:4318` may not be listening when the first handler-phase spans are created. The `OTLPSpanExporter` fails to connect. The `BatchSpanProcessor` buffers the spans and retries on the next scheduled export (1000ms later per FR-073). By then, the ADOT Extension's receiver is ready. Handler-phase spans are delayed by up to 200ms but are NOT lost. No action required — this is handled by the existing retry/buffer mechanism.
- **(Round 6)** What happens when existing CloudWatch alarm thresholds use different calibration than new alarms? Task 17 adds new latency alarms at 80-90% of Lambda timeout. But existing alarms (e.g., `analysis_latency_high` at 25s for 60s timeout = 42%) were set with different methodology. During an incident, the new SSE latency alarm fires at 14s/15s (93% of timeout) while the existing Analysis alarm doesn't fire until 25s/60s (42% of timeout). The inconsistency creates uneven alerting — some Lambdas alert early, others alert late. All thresholds MUST use consistent calibration.
- **(Round 7)** What happens when the ADOT Extension process crashes during SSE streaming? The OTel SDK's `BatchSpanProcessor._export()` catches all exceptions via `except Exception` and logs them. The `OTLPSpanExporter` retries once on `ConnectionError`, then the error propagates to the `BatchSpanProcessor` which swallows it. The SSE Lambda continues streaming normally — the client receives a complete response. However, zero trace data exists for the remainder of that stream. The operator has no per-invocation detection mechanism; the canary (FR-019/FR-036) detects trace absence at aggregate level. This is a known, accepted gap mitigated by the fact that ADOT communicates via localhost (connection failure = Extension process death, which is logged by the Lambda runtime).
- **(Round 7)** What happens when the ADOT Extension's default collector configuration includes unnecessary components (metrics pipeline, health check endpoint, zpages extension)? Each component consumes memory and CPU without value in sidecar-only mode. A custom stripped-down collector configuration (`OPENTELEMETRY_COLLECTOR_CONFIG_FILE`) containing only the OTLP HTTP receiver and AWS X-Ray exporter reduces the Extension's memory footprint from ~50MB to ~35MB, freeing headroom within the 512MB allocation.
- **(Round 7)** Why `BatchSpanProcessor` instead of `SimpleSpanProcessor` for the SSE Lambda? `SimpleSpanProcessor` exports each span synchronously — every DynamoDB query span blocks on an OTLP HTTP POST to localhost before the next query executes, adding per-span latency to the streaming response. `BatchSpanProcessor` buffers spans and exports asynchronously every 1000ms (per FR-073; default is 5000ms — corrected from original 200ms assumption, see FR-084), requiring `force_flush()` at stream end (FR-055). For latency-sensitive streaming, `BatchSpanProcessor` is the correct choice — the 1000ms batch interval is negligible relative to the 15-second streaming lifecycle, and async export prevents span creation from blocking the response.
- **(Round 7)** What happens when `TracerProvider` is mistakenly created per invocation instead of as a module-level singleton? Each `TracerProvider()` instantiation spawns a new `BatchSpanProcessor` daemon thread and opens a new OTLP HTTP connection to ADOT. Previous instances are not garbage-collected (daemon threads hold references). After N warm invocations, the execution environment has N orphaned threads consuming ~N MB of memory. Eventually, Lambda hits its 512MB memory limit and OOM-terminates the environment. This is why FR-065 clarifies that FR-059's per-invocation requirement applies ONLY to `propagate.extract()`, not to infrastructure objects.
- **(Round 7)** What happens with the ADOT Extension + RESPONSE_STREAM lifecycle interaction? In RESPONSE_STREAM mode, the Lambda invocation is "complete" only when the generator is exhausted and the response stream closes — NOT when the handler returns the generator. The ADOT Extension receives the INVOKE completion notification after streaming finishes. The `force_flush()` in the generator `finally` block (FR-055) pushes the final spans to ADOT's OTLP receiver before INVOKE completion. The Extension then has time to export to X-Ray in the post-INVOKE processing window before environment freeze. This lifecycle is compatible because ADOT is designed for Lambda — it waits for the INVOKE response before starting its post-processing.
- **(Round 7)** What is the distinction between the ADOT Python SDK layer and the ADOT collector-only layer? Two layer types exist: (1) `aws-otel-python-amd64-*` includes both the OTel Collector binary AND Python auto-instrumentation wrapper (`/opt/otel-instrument`) — ~80MB unzipped, (2) `aws-otel-collector-amd64-*` includes only the OTel Collector binary — ~35MB unzipped. For sidecar-only mode (FR-046: no `AWS_LAMBDA_EXEC_WRAPPER`), the collector-only layer is correct. Using the Python SDK layer wastes ~45MB and includes unused auto-instrumentation code that could be accidentally activated. Source: ADOT Lambda Layer ARN repository — separate per-region ARN mappings for each layer type.
- **(Round 8)** What happens when FR-062/FR-063 specify Lambda Layers for a container-based Lambda? Lambda container images CANNOT use Lambda Layers — the `layers` attribute is only valid for ZIP deployments. Terraform will either error (if using `aws_lambda_function` with `image_uri` and `layers` simultaneously) or silently ignore the layer configuration. The SSE Lambda (`python:3.13-slim` + custom bootstrap), Analysis Lambda (`public.ecr.aws/lambda/python:3.13`), and Dashboard Lambda (`public.ecr.aws/lambda/python:3.13`) are ALL container-based. ADOT deployment for the SSE Lambda requires embedding the collector binary in the Docker image. Source: AWS Lambda Container Image docs — "You cannot add Lambda layers to container images."
- **(Round 8)** What happens when `OTEL_SERVICE_NAME` is not set on the SSE Lambda? The `AwsLambdaResourceDetector` populates `cloud.*` and `faas.*` attributes but does NOT set `service.name`. The OTel SDK defaults `service.name` to `unknown_service`. The ADOT X-Ray exporter uses `service.name` as the node name in the X-Ray service map. All SSE Lambda invocations appear under a single `unknown_service` node — indistinguishable from any other unconfigured Lambda. SC-028 becomes unachievable. Operators cannot identify the SSE Lambda in the service map without searching by trace ID. Source: `opentelemetry-sdk-extension-aws` — `AwsLambdaResourceDetector` implementation; AWS X-Ray OTLP integration docs.
- **(Round 8)** What happens when `BatchSpanProcessor` uses default configuration (`schedule_delay_millis=5000`) on the SSE Lambda? The background worker thread wakes every 5 seconds to check for pending spans. During a 15-second streaming session producing ~50-200 spans, the worker exports approximately 3 batches (at 5s, 10s, 15s). With `force_flush()` in the generator `finally` block, the remaining spans are drained synchronously. Functionally correct, but the 5-second delay means a mid-stream crash loses up to 5 seconds of spans (vs 1 second with tuned configuration). The 2048-entry queue buffer also wastes ~200KB of memory unnecessarily. For Lambda, smaller/faster is better.
- **(Round 9)** What happens when the ADOT Extension's collector context is canceled before HTTP exports to X-Ray complete? This is a KNOWN issue (GitHub aws-otel-lambda#886, opentelemetry-lambda#224) with measured ~30% span drop rate in worst-case scenarios. The root cause: the collector's internal context is canceled when the Lambda Extension calls Next API, but the X-Ray HTTP exporter may still have in-flight requests. Error pattern: `"context canceled"` during export followed by `"Exporting failed. Dropping data."` The decouple processor (Lambda-specific batch processor in ADOT) mitigates this by decoupling span reception from export — it holds the Extension from calling Next until all queued data is exported. However, under high concurrency or when `span.end()` is the very last operation before function exit, a race exists between the collector's shutdown and the HTTP export completion. The spec's `force_flush()` (FR-055) handles the SDK→Extension hop (fast, localhost). The Extension→X-Ray backend hop is the vulnerable path. The canary (FR-019/FR-036) provides aggregate detection. Per-invocation detection is NOT possible because the span loss occurs in the Extension process, not the SDK. Source: https://github.com/aws-observability/aws-otel-lambda/issues/886
- **(Round 9)** What happens when `TracerProvider` is created with the default `shutdown_on_exit=True`? Python's `atexit` module registers a handler that calls `TracerProvider.shutdown()`. In Lambda, the interpreter does not exit between warm invocations, so `atexit` does not fire between invocations. However, when the execution environment is recycled (idle timeout), the interpreter finalization sequence runs, triggering `atexit` handlers. In Python 3.13+, `ConcurrentMultiSpanProcessor.shutdown()` shuts down the thread pool executor. If the shutdown races with a pending export (e.g., a span was queued just before recycling), a `RuntimeError: cannot schedule new futures after shutdown` is raised (opentelemetry-python#4461). The `shutdown_on_exit=False` parameter prevents this race by skipping the `atexit` registration. Since the Lambda runtime calls `force_flush()` per FR-055, the `atexit` handler is redundant. Source: https://github.com/open-telemetry/opentelemetry-python/issues/4461
- **(Round 9)** What happens when OTel `BotocoreInstrumentor().instrument()` is called on the SSE Lambda alongside Powertools Tracer (even with `auto_patch=False`)? `BotocoreInstrumentor` patches `botocore.client.BaseClient._make_api_call` with OTel span creation. Powertools Tracer's `@tracer.capture_method` also creates X-Ray subsegments around handler-phase AWS SDK calls. During the handler phase, each AWS SDK call would produce BOTH an OTel span (via BotocoreInstrumentor) AND an X-Ray subsegment (via capture_method). During the streaming phase, only OTel spans are created (correct). The BotocoreInstrumentor does NOT auto-activate in sidecar-only mode — it must be explicitly called. FR-060 prohibits `auto_patch` but does not prohibit `BotocoreInstrumentor`. Without explicit prohibition, a future implementer could import and call it thinking it's the "OTel way" to instrument AWS SDK calls.
- **(Round 9)** What happens when the X-Ray canary queries BatchGetTraces before the trace is indexed? The X-Ray API cannot distinguish "not yet indexed" from "trace does not exist." Both scenarios result in the trace ID appearing in `UnprocessedTraceIds` or being absent from the `Traces` response array. A single query at 30 seconds will produce false negatives under load. The canary must retry with backoff (query at 30s, retry at 60s) to account for eventual consistency. Two consecutive failures across canary intervals remains the alarm threshold. Source: https://aws.amazon.com/xray/faqs/ — "Trace data sent to X-Ray is generally available for retrieval and filtering within 30 seconds"
- **(Round 9)** What happens when CloudWatch `put_metric_data` fails with an IAM error (AccessDeniedException) vs a transient error (ThrottlingException)? The canary (FR-049) currently treats all failures uniformly. IAM/credential errors (AccessDeniedException, NotAuthorized, InvalidClientTokenId) indicate a permanent permission change — the exact failure mode FR-051's separate IAM role is designed to detect — and require immediate out-of-band escalation. Transient errors (ThrottlingException, InternalFailure, ServiceUnavailable) should be retried with exponential backoff before escalating. Without this classification, an IAM revocation is silently retried for multiple canary intervals before escalating, losing critical response time. Source: https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/API_PutMetricData.html
- **(Round 9)** What happens when the SSE Lambda server ignores the `Last-Event-ID` request header on reconnection? FR-033 specifies client-side `Last-Event-ID` propagation for stream resumption, but if the server does not check this header and resume from the corresponding position, the client receives a fresh stream from the beginning. This causes duplicate events (already-processed events re-sent) and missed events (events emitted between disconnect and reconnect start if server resets). The client must handle idempotently (deduplicate by event ID). Without server-side support, the "stream resumption" guarantee in FR-033 is hollow.
- **(Round 9)** What happens when the frontend needs to read the `X-Amzn-Trace-Id` response header for FR-048's `previous_trace_id` capture? Per the CORS specification, browsers can only read response headers listed in `Access-Control-Expose-Headers`. The SSE Lambda Function URL's CORS configuration currently does not include `x-amzn-trace-id` in `ExposeHeaders`. Without it, the browser JavaScript cannot read the response trace ID, making FR-048's reconnection trace correlation impossible. This is a hard prerequisite, not optional.
- **(Round 9)** What happens when Safari processes small SSE frames from a fetch()+ReadableStream connection? Safari may buffer small streaming responses differently than Chrome/Firefox, potentially batching multiple small SSE frames before delivering them to the ReadableStream reader. This causes perceived latency spikes where events arrive in bursts rather than individually. Workaround: server sends padding bytes or explicit flush signals. Source: sveltejs/kit#10315
- **(Round 9)** What is the canonical way to distinguish graceful server close from network error in a fetch()+ReadableStream SSE connection? `ReadableStreamDefaultReader.read()` returns `{done: true}` on graceful close and throws `TypeError` on network error. The `reader.closed` Promise fulfills on graceful close and rejects on error. FR-033's reconnection logic should use `retry:` value (from SSE protocol) or short fixed delay for graceful close (expected Lambda timeout), and exponential backoff with jitter for network errors. Uniform backoff on all close types unnecessarily delays reconnection after expected server-initiated closes. Source: WHATWG Streams Standard — ReadableStreamDefaultReader.closed
- **(Round 9)** What happens if AWS Account-level Transaction Search is enabled? If the account migrates to CloudWatch-native tracing, traces stored in `/aws/spans` are NOT queryable via X-Ray's `BatchGetTraces` API — only traces stored via `PutTraceSegments` to the legacy X-Ray segment store are retrievable. The canary's trace verification (FR-019) uses `BatchGetTraces`. An account-level settings change could break the canary without any code change. The canary should verify that its test traces are retrievable via the API it uses, not assume the storage backend. Source: https://repost.aws/questions/QUUl7bopxdQii5vVCNZ3FtPg
- **(Round 10)** What happens when the SSE Lambda continues streaming after the client disconnects? AWS docs confirm: "Streamed responses are not interrupted or stopped when the invoking client connection is broken. Customers are billed for the full function duration." The SSE Lambda continues DynamoDB polling, CloudWatch metric emission, and OTel span creation after client disconnects. `force_flush()` runs in the `finally` block for a phantom stream. Python custom runtimes have NO standard mechanism to detect client disconnect (unlike Node.js `StreamifyResponse` which provides `metadata.onAbort`). The only signal is `BrokenPipeError` raised when the generator attempts to write to the closed response stream. Spans created after disconnect annotate "successful" streaming with no client receiving the data. The canary should account for elevated "successful but undelivered" traces in its health assessment. Source: https://docs.aws.amazon.com/lambda/latest/dg/configuration-response-streaming.html
- **(Round 10)** What happens when `BatchSpanProcessor` queue fills during high-throughput SSE streaming? `BatchSpanProcessor.emit()` uses `queue.put_nowait()` which raises `queue.Full` exception, silently caught and logged as a WARNING. Spans are DROPPED, not queued or retried. At FR-073's original `max_queue_size=512`, a 15-second streaming Lambda producing ~15 DynamoDB polls/second with ~3 spans per poll generates ~675 spans per invocation. This EXCEEDS the 512 queue capacity. If `force_flush()` (which drains the queue synchronously) does not keep pace with span production between batch intervals, ~163 spans are silently lost. The drop is only visible as a WARNING log line — no metric, no exception, no span attribute. Source: OTel Python SDK source code — `BatchSpanProcessor.on_end()` method.
- **(Round 10)** What happens when custom ADOT collector config omits the decouple processor? The ADOT Lambda Layer's default configuration automatically includes the decouple processor when the batch processor is used. However, this auto-configuration ONLY applies to Lambda Layer deployments — NOT to container-based deployments with custom collector YAML (FR-064/FR-069). If the custom collector config defines a pipeline without explicitly listing `decouple` in the processors chain, span drops revert to the known ~30% rate (aws-otel-lambda#886) because the collector context is canceled before HTTP exports complete. Cross-reference: FR-075 (decouple processor requirement), FR-064 (container-based deployment), FR-069 (custom config). Source: https://github.com/open-telemetry/opentelemetry-lambda/blob/main/collector/processor/decoupleprocessor/README.md
- **(Round 10)** What happens when Active Tracing is accidentally disabled on a Lambda function? `AwsXRayLambdaPropagator` reads `_X_AMZN_TRACE_ID` environment variable, which is ALWAYS set by the Lambda runtime regardless of Active Tracing status. With Active Tracing disabled, the trace ID still populates but with `Sampled=0`. The propagator extracts this context (open issue opentelemetry-lambda#1782 — the propagator does not distinguish between "legitimately unsampled" and "tracing disabled"). The `parentbased_always_on` sampler honors the parent's `Sampled=0` flag and suppresses ALL span creation. Result: zero spans emitted, zero errors logged, complete silent tracing failure. Cross-reference: FR-052 (Active Tracing requirement). Source: https://github.com/open-telemetry/opentelemetry-lambda/issues/1782
- **(Round 10)** What happens when X-Ray subsegment count exceeds 100 per segment (Powertools Tracer)? The X-Ray SDK has a default subsegment streaming threshold of 100. When exceeded, completed subsegments are automatically sent to the X-Ray daemon out of band. For non-streaming Lambdas with many boto3 calls, this is benign. For the SSE Lambda handler phase (before streaming begins), the count should be well under 100. However, if `auto_patch=True` were accidentally re-enabled (violating FR-060), streaming-phase boto3 calls would create X-Ray subsegments via the X-Ray daemon (unreliable after handler returns), counting against the 100-subsegment threshold and triggering automatic streaming of partially-complete subsegments. This reinforces the criticality of FR-060 (`auto_patch=False`). Source: https://docs.aws.amazon.com/xray/latest/devguide/xray-troubleshooting.html
- **(Round 10)** What happens when X-Ray centralized sampling rules are temporarily unavailable? The OTel X-Ray remote sampler falls back to a `TraceIDRatioBased` sampler with reservoir borrowing (1 trace per second). In dev/preprod environments where 100% sampling is required (FR-034/FR-035), this fallback silently drops to 1 request/second sampling during X-Ray service degradation. The fallback is a safety mechanism (not a bug), but the drop from 100% to ~0.01% (at 10K req/s) is dramatic and invisible without explicit monitoring. No metric or alarm fires for the sampling mode change. Source: https://aws-otel.github.io/docs/getting-started/remote-sampling/
- **(Round 11)** What happens when the SSE Lambda hits its timeout limit during streaming? Lambda does NOT send SIGTERM to the custom runtime's bootstrap process. The process is killed directly. Python's `finally` blocks do NOT execute when the process is SIGKILL'd. `GeneratorExit` is NOT raised. FR-055's `force_flush()` does NOT run. Un-flushed `BatchSpanProcessor` spans (up to 1000ms worth per FR-073's batch interval) are permanently lost. The ADOT Extension receives a Shutdown event with `shutdownReason: "TIMEOUT"` and has a 2000ms grace period to export already-received spans. Spans that never left the SDK (still in BSP queue) cannot be recovered. Mitigation: proactive flush when remaining time < threshold (see FR-093). Source: AWS Lambda Runtime Environment Lifecycle — Extensions receive Shutdown with reason TIMEOUT; runtime process is killed.
- **(Round 11)** What happens when the SSE Lambda's custom bootstrap does NOT set `_X_AMZN_TRACE_ID` from the `Lambda-Runtime-Trace-Id` response header between warm invocations? The Lambda service sets `_X_AMZN_TRACE_ID` in the process environment at cold start (INIT phase). On warm invocations, the Lambda Runtime API returns an updated `Lambda-Runtime-Trace-Id` header with the new invocation's trace context, but the process environment variable is NOT automatically updated by the Lambda service. The custom bootstrap MUST explicitly set `os.environ['_X_AMZN_TRACE_ID']` from this header before calling the handler. Without this, `AwsXRayLambdaPropagator.extract()` reads stale context from the FIRST invocation on EVERY subsequent warm invocation. The X-Ray SDK's `LambdaContext._refresh_context()` reads the same env var. This is a SILENT failure — no error, no warning, just wrong trace IDs on every warm invocation span. Source: AWS Lambda Custom Runtime Contract — bootstrap must set `_X_AMZN_TRACE_ID` from `Lambda-Runtime-Trace-Id` header.
- **(Round 11)** What happens when a malicious client sends `Sampled=1` in `X-Amzn-Trace-Id` to a Lambda Function URL? The Lambda service honors the incoming sampling decision. Unlike API Gateway (which makes its own sampling decision when tracing is enabled), Function URLs have no intermediary tracing layer. The client forces 100% sampling on the SSE Lambda. At 1000 concurrent connections with 15-second sessions (4 invocations/minute/connection), this produces ~172M traces/month at ~$860/month. Mitigation for API Gateway paths: API Gateway's own tracing integration makes independent decisions. Mitigation for Function URL paths: application-level sampling logic, WAF rate limiting, or placing API Gateway in front. CloudFront would strip the header (it treats `X-Amzn-Trace-Id` as restricted) but is not in the current architecture.
- **(Round 11)** What happens when the ADOT Extension's custom collector config file exists at `/opt/collector-config/config.yaml` but `OPENTELEMETRY_COLLECTOR_CONFIG_FILE` is not set? The Extension ignores the file and uses its compiled-in embedded default configuration. The embedded default includes: OTLP HTTP receiver on 4318, AWS X-Ray exporter, decouple processor. However, it also includes unnecessary components (gRPC receiver on 4317, potentially metrics pipeline) that consume ~15MB of extra memory. The custom config (with stripped-down components per FR-064) is effectively dead code. The file's presence in the container image provides no benefit without the env var. Source: ADOT Lambda Extension — config file path is only read from `OPENTELEMETRY_COLLECTOR_CONFIG_FILE` env var.
- **(Round 11)** What happens when `TextDecoder` processes a ReadableStream chunk that ends in the middle of a multi-byte UTF-8 character? Without `TextDecoder({stream: true})`, the decoder treats each chunk independently. An incomplete byte sequence at the chunk boundary (e.g., the first 2 bytes of a 3-byte UTF-8 character) is replaced with U+FFFD (replacement character). On the next chunk, the remaining byte(s) are also invalid (starting mid-sequence) and produce another U+FFFD. The original character is permanently lost, replaced by 2 replacement characters. For SSE event data containing sentiment analysis results with non-ASCII characters (article titles, international content), this silently corrupts the data. `TextDecoder({stream: true})` retains incomplete sequences across decode calls, correctly reconstructing multi-byte characters split across chunks. Source: WHATWG Encoding Standard — TextDecoder `stream` option.
- **(Round 12)** What happens when the SSE Lambda's generator terminates after the proactive flush (FR-093/FR-100) but before the Lambda timeout? The Lambda handler returns normally (not a timeout), so the ADOT Extension receives a NORMAL Shutdown event with `shutdownReason: "spindown"` (not `"timeout"`). The Extension gets the standard 2000ms grace period to flush buffered spans to X-Ray. This is strictly MORE reliable than waiting for timeout, because normal Shutdown has no SIGKILL risk for the Extension process. FR-100's graceful termination converts what would be a crash into a clean exit, improving span export reliability from "best-effort Extension Shutdown after runtime SIGKILL" to "guaranteed Extension Shutdown after normal completion."
- **(Round 12)** What happens when OTel core packages are at different versions in the Docker image? The `opentelemetry-sdk` package hard-pins `opentelemetry-api==<exact-version>`. `pip install` fails with `ResolutionImpossible` error, preventing the Docker build from succeeding. This means version mismatch is a build-time failure, not a runtime failure — but without explicit `==` pins in `requirements.txt`, different `pip install` runs on different dates could resolve to different lockstep versions, making builds non-reproducible. FR-101 makes this constraint explicit.
- **(Round 12)** What happens when the bootstrap provides the deadline but the handler doesn't use it? FR-093 becomes unimplementable. The generator streams until Lambda timeout, the runtime is SIGKILL'd, the `finally` block doesn't run, and spans from the final BSP batch interval (~1000ms per FR-073) are permanently lost. FR-103 is a hard prerequisite for FR-093.
- **(Round 12)** What happens when Lambda memory is increased to 1024MB but ADOT + application code consumes more than expected? The 85% memory alarm (FR-104) fires at ~870MB, giving operators advance warning before OOM. Lambda OOM-terminates the execution environment at 1024MB. The Lambda `REPORT` log line includes `Max Memory Used` for post-incident analysis. Without FR-104, the first indication of memory pressure is an OOM kill with no advance warning.
- **(Round 12)** What happens when the ADOT Extension log message format changes in a future version? FR-105's substring match pattern (`"Exporting failed" "Dropping data"`) is resilient to minor format changes (field reordering, additional fields) but would miss a completely rewritten message. Version pinning (FR-070 digest pin) prevents silent ADOT upgrades. When upgrading ADOT, the metric filter pattern must be verified against the new version's log output. The log messages originate from upstream OTel Collector `exporterhelper` package and have been stable since v0.67 (2022). Source: `open-telemetry/opentelemetry-collector` — `exporter/exporterhelper/internal/retry_sender.go`.
- **(Round 13)** What happens when remediation tasks are deployed out of phase order — specifically, when the canary (Task 11) is deployed before any instrumentation task (Tasks 2-5)? The canary submits its OWN test traces and queries for them. If no application instrumentation is deployed, the canary's self-generated test traces are found successfully, so it reports healthy. But application traces don't exist — the canary validates itself, not the system. This is a false-green scenario where the canary says "tracing works" but zero real application traces exist. FR-107's Phase 6 ordering prevents this by requiring instrumentation deployment before canary deployment.
- **(Round 13)** What happens when `OTEL_SDK_DISABLED` is accidentally set to `true` in production? All OTel tracing silently stops. The `TracerProvider` returns no-op tracers — zero spans created, zero export errors, zero log entries from the OTel SDK. The ADOT Extension process continues running (consuming ~50MB memory) but receives no spans on its OTLP receiver. The canary (FR-019) detects absence of application traces within 2 canary intervals (10 minutes per FR-048 at 5-minute interval). The Lambda continues functioning normally — `OTEL_SDK_DISABLED` affects ONLY observability, not application logic. This is by design: the kill switch trades observability for stability during incidents where tracing is suspected of causing problems. Rollback: set `OTEL_SDK_DISABLED=false` via Terraform and apply.
- **(Round 13)** What happens when a rollback from ADOT is needed but no pre-ADOT container image exists in ECR? The team must modify the Dockerfile to remove ADOT components, rebuild the container image, push to ECR, update Terraform `image_uri`, and apply. This takes 15-30 minutes during an active incident, requiring CI/CD pipeline access and Docker build infrastructure availability. FR-110's pre-ADOT baseline image reduces rollback to a single Terraform variable change (~2 minutes), eliminating CI/CD dependency during incidents.
- **(Round 13)** What happens when X-Ray Groups are created without `insights_enabled = true`? The Groups function correctly as query-time filters in the X-Ray console — operators can search and view filtered trace lists. However, the Groups do NOT emit CloudWatch metrics. SC-013's requirement ("X-Ray Group with error/fault filter generates CloudWatch metrics") fails silently — no error, no warning, no indication that metrics are not being emitted. Alarms configured against Group-derived CloudWatch metrics never fire because the underlying metrics never exist. The `insights_enabled` attribute defaults to `false` in both the AWS API and Terraform, making this an opt-in configuration that is easy to miss.
- **(Round 13)** What happens when the EventBridge rule for the canary fires before `aws_lambda_permission` is created during initial `terraform apply`? EventBridge attempts to invoke the Lambda and receives `AccessDeniedException`. EventBridge retries with exponential backoff (built-in retry policy). The `aws_lambda_permission` resource is typically created within seconds by Terraform, so the first retry succeeds. However, the initial failure creates noise: an error entry in EventBridge's dead-letter queue (if configured) or a silently lost invocation (if no DLQ). More critically, operators monitoring the initial deployment may see a "canary failed" alert that self-resolves, creating false urgency. FR-112's `depends_on` eliminates this entirely.
- **(Round 13)** What happens when trace completeness drops below 95% but individual traces are not marked IsPartial? This indicates systemic trace loss where entire traces — not just segments within a trace — are being dropped. Possible causes: (a) ADOT Extension process crash during streaming (all spans from that invocation are lost), (b) X-Ray `PutTraceSegments` throttling at the 2,600 segments/sec region limit, (c) BSP queue overflow (mitigated by FR-086's 1500 queue size), (d) Extension→X-Ray backend race (aws-otel-lambda#886 at ~30% worst-case). The per-trace IsPartial check (FR-087) catches incomplete traces but NOT missing traces (a trace that never arrives has no IsPartial flag to check). FR-113's completeness ratio metric catches both incomplete and missing traces by comparing submission count to retrieval count.
- **(Round 14)** What happens when the ADOT Extension process crashes during `force_flush()`? The OTel OTLP HTTP exporter's `DEFAULT_TIMEOUT` is 10 seconds with up to 6 retries per HTTP POST. With ceil(1500/64)=24 batches at worst-case, each blocking for up to 60s on timeout, total potential block is ~24 minutes. Since `force_flush()` timeout parameter is NOT enforced (OTel issue #4568), the call blocks indefinitely. FR-093's 3000ms pre-deadline flush becomes a multi-minute block. The Lambda is SIGKILL'd while `force_flush()` is still processing. Mitigation: `OTEL_EXPORTER_OTLP_TRACES_TIMEOUT=2` caps per-request timeout, limiting worst-case to ~15 seconds. Source: OTel Python SDK — `BatchSpanProcessor._export()` loop; OTLP HTTP exporter timeout; issue #4568.
- **(Round 14)** What happens when the BSP's internal export fails silently during `force_flush()`? Spans are popped from the deque BEFORE being passed to `self._exporter.export(batch)`. If export raises an exception, it is caught by `except Exception` and the loop continues. Failed batch spans are PERMANENTLY destroyed — already removed from queue. `force_flush()` returns `True` regardless, indicating "queue drained" not "export succeeded." Against localhost ADOT, export failure is extremely unlikely, but during Extension crash/hang recovery, this behavior means proactive flush can drain the queue while zero spans reach ADOT. Source: OTel Python SDK `_shared_internal/__init__.py` lines 180-195.
- **(Round 14)** What happens when an OTel span on the SSE Lambda has more than 50 attributes that map to X-Ray annotations? X-Ray silently drops annotations beyond the 50-per-segment limit with `log.warning()`. With FR-006 (~6 annotations), FR-007 (~5), FR-008 (~3), FR-009 (~4), plus OTel semantic conventions (~5-10), total is ~23-28 per span — within the 50 limit. However, ad-hoc debugging annotations during incident investigation could exceed 50, and the silent drop creates a debugging trap. Source: AWS X-Ray API — PutTraceSegments annotation limit; SDK warning behavior.
- **(Round 14)** What happens when the browser's CORS preflight cache expires for X-Amzn-Trace-Id? After `Access-Control-Max-Age` expiry (Chrome: 7200s, Firefox: 86400s, Safari: 600s), the next fetch() with X-Amzn-Trace-Id triggers a preflight OPTIONS request (50-200ms). For SSE connections (15s+), negligible. For Dashboard Lambda API calls, first request after expiry pays the cost. Safari's 10-minute max means Safari users experience frequent preflights. Source: MDN Access-Control-Max-Age; Fetch Living Standard.
- **(Round 14)** What happens when a ReadableStream SSE connection terminates with `done: true` but the server did NOT send a sentinel `event: done` first? The client cannot distinguish server-intentional close from clean network drop (TCP FIN). Both produce `{ done: true, value: undefined }`. Without sentinel: treating all `done: true` as graceful masks network issues; treating as error adds unnecessary backoff to graceful closes. Sentinel resolves ambiguity: presence of `event: done` in buffer before `done: true` = graceful; absence = abnormal. Source: WHATWG Fetch Standard; Streams Standard §4.2.3.
- **(Round 14)** What happens when `treat_missing_data = "notBreaching"` is set on the canary health alarm and the canary Lambda dies? The canary stops emitting health metrics. `notBreaching` treats missing data as within threshold — alarm resolves to OK. The "watcher of the watcher" is dead and the alarm says "healthy." `treat_missing_data = "breaching"` is the ONLY correct setting for canary alarms: absence of heartbeat IS the failure. Source: AWS CloudWatch Developer Guide — treat_missing_data.
- **(Round 14)** What happens when ADOT collector config is syntactically valid YAML but references an unsupported component? The collector fails at config validation phase with `Error: failed to get config: unknown receiver type`. ADOT Extension logs error and exits. Lambda invocation proceeds without telemetry (Extension is sidecar — failure doesn't crash Lambda). All subsequent invocations on this execution environment also lack telemetry. Only canary detects trace absence (~10 minutes). CI validation with `validate` subcommand catches this before deployment. Source: OTel Collector config loading; ADOT Extension failure behavior.
- **(Round 14)** What happens during dual-emit when X-Ray SDK and OTel SDK produce traces for the same request? X-Ray SDK produces native segment documents with `subsegment` nesting. ADOT-converted OTel spans produce flat segments with `parent_id` references. X-Ray service map correctly links both (shared trace ID). Console visualization differs (nesting depth varies). FR-109 verification gates must use SEMANTIC comparison (same operations, annotations, error flags) not structural comparison (byte-identical documents). Source: X-Ray segment document format; ADOT X-Ray exporter output.
- **Cascading false-green during CloudWatch regional degradation** (Round 15): put_metric_data fails → custom metrics stop → alarms with treat_missing_data=notBreaching resolve to OK → dashboard shows all-green while system is failing. Mitigation: canary heartbeat with treat_missing_data=breaching + Logs metric filter backup.
- **Canary heartbeat through different code path** (Round 15): If canary uses a separate CloudWatch client or separate put_metric_data call, it proves CloudWatch API reachability but NOT that the application's metric emission pipeline is healthy. Canary MUST piggyback on same call.
- **Silent failure metric emission itself failing** (Round 15): Adding put_metric_data to catch blocks creates a second failure point — the metric emission for the silent failure can itself fail silently. Defense: structured log fallback with metric_fallback=true marker for Logs metric filter.
- **Percentage-based error alarm at low traffic** (Round 15): If Lambda runs 2 invocations in 5 minutes and 1 fails, that's 50% error rate — alarm fires. At 1000 invocations, 5 errors = 0.5% — alarm does not fire. Need minimum invocation count guard: IF(invocations > 10, error_rate, 0).
- **SendGrid python_http_client call chain** (Round 15): sendgrid → python_http_client.Client._make_request() → urllib.request.urlopen() → http.client.HTTPSConnection. X-Ray patch_all() patches httplib which IS http.client — so it DOES capture SendGrid calls, but only as generic "http.client" subsegments without SendGrid-specific metadata.
- **Composite alarm INSUFFICIENT_DATA suppression** (Round 15): If underlying alarms use treat_missing_data=notBreaching, they never enter INSUFFICIENT_DATA — composite alarm for "all metrics missing" never fires. Underlying alarms MUST use treat_missing_data=missing.
- **Work order implementer scope blindness** (Round 15): Developer implementing Task 5 from fix-sse-subsegments.md sees 16 FRs. HL doc assigns 52 FRs (including 2 CRITICAL: FR-092 warm-invocation trace ID, FR-093 deadline-aware flush). 36 FRs silently omitted, including corrections that invalidate earlier FRs' assumptions.
- **Alarm widget showing stale count** (Round 15): Dashboard alarm widget configured with static alarm list — new alarms added by Terraform not automatically included. Widget must use alarm name prefix pattern, not explicit ARN list.
- **CloudWatch RUM initialization race** (Round 16): If application code calls `fetch()` before RUM's web client completes initialization (INIT phase), the `X-Amzn-Trace-Id` header is NOT injected. The SSE connection is typically established early in the page lifecycle. Mitigation: SSE connection establishment MUST wait for RUM readiness (e.g., `window.cwr_ready` or a RUM initialization callback) before calling `fetch()`.
- **SNS rawMessageDelivery breaks trace propagation** (Round 16): If any subscription on the Ingestion→Analysis SNS topic has `rawMessageDelivery: true`, the `AWSTraceHeader` system attribute is stripped and cross-Lambda trace propagation silently breaks. The SNS subscription appears healthy — messages deliver — but traces are disconnected. FR-137's verification gate catches this.
- **Cross-region SNS IAM specificity** (Round 16): The canary's execution role for FR-136 needs `sns:Publish` for the cross-region topic ARN. A wildcard region in the policy (`arn:aws:sns:*:*:canary-alert`) creates unnecessarily broad permissions. The policy MUST pin to the specific backup region ARN.
- **Hung vs. crashed ADOT Extension** (Round 16): TCP connect to a crashed Extension fails in <100ms (ECONNREFUSED). TCP connect to a hung Extension succeeds but reads block until per-request timeout (FR-114, 2s). FR-139's thread wrapper handles both: crashed Extension completes fast (~2s for all 22 batch connect failures); hung Extension triggers the 2,500ms hard timeout. Without FR-139, only the crashed case is bounded.
- **ADOT Extension OOM** (Round 16): If the Extension process is OOM-killed by the Lambda sandbox (Extension shares Lambda's memory allocation per FR-102), the OTLP exporter sees ECONNREFUSED (not hung). force_flush() fails fast per SC-064. But the OOM signal indicates insufficient memory headroom — FR-104's memory alarm at 85% should fire BEFORE Extension OOM. If it doesn't, the 1024MB allocation (FR-102) needs re-evaluation.
- **API Gateway IntegrationLatency includes cold start** (Round 16): During deployments (new container image rollout), Lambda cold starts spike temporarily. FR-138's `IntegrationLatency` P99 alarm must tolerate deployment-time spikes: 2 evaluation periods × 5 minutes = 10-minute window prevents single-period false alarms during rolling deployment.
- **X-Ray annotation requirement for session chain queries** (Round 16): FR-141's X-Ray filter `annotation.session_id = "{value}"` requires `session_id` to be an X-Ray ANNOTATION (indexed), not metadata (non-indexed). FR-008 specifies it as an annotation, so this works. But if future FR amendments move it to metadata for space savings (FR-120 annotation budget), session queries break.
- **Audit trail DLQ for compliance** (Round 16, OUT OF SCOPE): FR-124 adds alerting for audit trail persistence failures, but alerting alone doesn't solve the compliance gap — failed audit events are permanently lost. A DLQ or retry mechanism for audit writes is outside X-Ray tracing scope but flagged as a dependent feature requirement. The X-Ray trace subsegment (SC-004 item 3) provides forensic evidence of the failure but not data recovery.
- **OTel SpanKind service map distortion** (Round 17): Manual OTel spans on SSE Lambda must use correct SpanKind — `SpanKind.CLIENT` for outbound calls (DynamoDB, CloudWatch), `SpanKind.INTERNAL` for internal processing (poll cycle, event formatting). ADOT X-Ray exporter maps `SpanKind.CLIENT` to remote subsegment (appears as separate node in service map), `SpanKind.INTERNAL` to local subsegment (appears within the Lambda node). Using `SpanKind.CLIENT` for internal processing creates phantom nodes in the X-Ray service map. Using `SpanKind.INTERNAL` for DynamoDB calls hides them from the service map's dependency view, violating SC-028. Source: ADOT X-Ray Exporter `segment.go` — `MakeSegment()` SpanKind routing.
- **Canary dual-mode operation** (Round 17): The canary Lambda has a unique architectural constraint: it must be INSTRUMENTED (per FR-029/FR-146) for operator visibility when X-Ray is healthy, AND RESILIENT (per FR-145) when X-Ray is down. No other Lambda in the system has this dual requirement. If FR-145's error handling catches too broadly, the canary might mask legitimate bugs in its own code. If too narrowly, it might crash on unexpected X-Ray failure modes. The error handling MUST be scoped to initialization only (TracerProvider setup, ADOT Extension connection) — runtime exceptions in canary business logic (API calls, metric emission) should propagate normally per FR-018.
- **record_exception() without set_status(ERROR) invisibility** (Round 17): The OTel `span.record_exception(exception)` API adds an exception event to the span with type, message, and stacktrace attributes. However, the ADOT X-Ray exporter's `makeCause()` function — which converts exception events to X-Ray's `cause` field — is GATED on `StatusCode.ERROR`. Calling `record_exception()` without `set_status(StatusCode.ERROR)` means the exception event EXISTS on the OTel span but is NEVER translated to the X-Ray segment document. The segment has `fault: false`, `cause: null`. The exception is permanently invisible in X-Ray, including to Group filters, console search, and CloudWatch metrics derived from Groups. This is the most insidious failure mode because the developer believes they handled the error (they recorded it) but X-Ray sees a healthy span. Source: ADOT X-Ray Exporter `cause.go`.
- **RUM initialization race on cold page load** (Round 18): CloudWatch RUM's FetchPlugin patches `window.fetch` synchronously once the AwsRum client constructor completes, but the constructor itself may be loaded asynchronously via a CDN snippet (`<script>` with `async` or `defer`). On a cold page load, application JavaScript can call `fetch()` before the AwsRum constructor fires, resulting in an unpatched `window.fetch` that sends NO `X-Amzn-Trace-Id` header. The SSE connection established in this window has no frontend-to-backend trace correlation. This is distinct from FR-135's deployment gate (which tests in a controlled Playwright environment where page load order is deterministic) — production users with cold caches experience non-deterministic script loading order. Mitigation: application code MUST NOT call fetch() until RUM is confirmed initialized (FR-148).
- **Post-flush span creation window** (Round 18): Between the proactive flush (FR-093 at 3000ms remaining, FR-139 at 2500ms hard timeout) and the generator's final `event: deadline` yield + return (FR-100), there is a window where new spans MAY be created (e.g., exception handling spans, cleanup operation spans). These spans are enqueued in the BSP AFTER the flush has drained the queue. No second flush occurs. The BSP's schedule_delay_millis=1000ms (FR-073) means these post-flush spans wait for the next scheduled export — but the Lambda may be killed (SIGKILL) or frozen before that export fires. These spans are silently lost. The window is small (~100ms for generator cleanup) but during error scenarios (exception handling creates diagnostic spans) it can be meaningful.
- **Canary TracerProvider state on warm invocation after cold-start failure** (Round 18): If TracerProvider initialization fails at module level during a cold start (e.g., ADOT Extension not running), the canary continues per FR-145 but module-level code does NOT re-execute on subsequent warm invocations. The TracerProvider remains in its failed/uninitialized state for ALL warm invocations until the execution environment is recycled. This means a transient ADOT Extension hiccup during cold start permanently disables canary tracing for the lifetime of that execution environment — potentially hours. The canary's alerting functions continue (FR-145) but its own trace visibility (FR-146) is lost without any re-initialization attempt.
- **FR-093 timing cascade margin analysis** (Round 18): FR-093 triggers proactive flush at 3000ms remaining. FR-139 wraps flush in a 2500ms hard timeout. If flush completes at 2500ms (worst case), 500ms remains for generator cleanup (yield `event: deadline`, return). Generator cleanup is lightweight (~10ms: one SSE event format + yield). The 500ms margin is sufficient but depends on FR-139's timeout being strictly enforced. If the thread-based timeout has jitter (thread scheduling latency under high CPU contention), the margin shrinks. At extreme contention (100ms thread scheduling delay), effective margin is 400ms — still sufficient for generator cleanup but worth documenting as a boundary condition.
- **BSP deadlock + FR-139 thread wrapper interaction** (Round 20): When BSP deadlocks (opentelemetry-python#3886), `force_flush()` hangs on the BSP's internal condition variable lock. FR-139's thread wrapper terminates the flush call after 2500ms — identical behavior to Extension hang. But the SPAN FATE differs: in Extension hang, spans were dequeued from BSP and held by the exporter (lost in exporter memory). In BSP deadlock, spans remain in the BSP deque but the lock is never released — they cannot be exported, retried, or even counted. The diagnostic log (FR-156) helps operators differentiate, but the practical outcome (span loss) is the same. A BSP deadlock also blocks all subsequent `on_end()` calls (new spans cannot be enqueued), meaning the deadlock's impact extends beyond the current flush to all future spans until the Lambda execution environment is recycled.
- **ADOT cold start exceeds customer experience threshold** (Round 20): SSE Lambda with embedded ADOT Extension at 1024MB may have cold starts exceeding 2000ms. For the SSE endpoint, cold start directly adds to the time before the first SSE event is delivered to the user. If cold start is 2500ms and first DynamoDB poll takes 200ms, the user waits 2700ms for the first stock price update — potentially exceeding acceptable UX thresholds. Mitigation options include provisioned concurrency (cost — ~$15/month per provisioned instance), larger memory (diminishing returns above 1769MB which allocates a full vCPU per AWS Lambda pricing), or ADOT version optimization. FR-158 mandates measurement to replace this assumption with data.
- **ADOT Extension invoke phase end during streaming** (Round 20): AWS Lambda Extensions API defines three lifecycle phases: INIT, INVOKE, SHUTDOWN. For buffered responses, the INVOKE phase ends when the runtime sends the response. For streaming responses, the "response" is continuous — the initial HTTP 200 headers are sent immediately, then the body streams over 15+ seconds. If the Extension's invoke phase ends when the initial response headers are sent (before streaming begins), the Extension may enter a pre-Shutdown state where it prioritizes flushing existing data over accepting new spans. This would manifest as intermittent span loss concentrated in the LATER seconds of streaming — observable in X-Ray as traces with early spans present but late spans missing. FR-160's verification gate detects this pattern by checking span retention across the full streaming duration.
- **Function URL burst traffic X-Ray cost spike** (Round 20): A traffic burst creating 10 million Function URL invocations in a single day at 100% sampling (parentbased_always_on with no parent context) costs $50 in X-Ray traces — exceeding FR-038's monthly $50 cost guard in a single day. At 100 million invocations (possible during a viral event or DDoS), the cost is $500/day. FR-161's daily anomaly alarm detects this, but the alarm evaluates at 5-minute intervals with a 6-hour rolling window — up to 6 hours of uncapped cost accumulation before the alarm fires. The X-Ray centralized sampling rule change (via console) takes effect within 5 minutes. The gap between cost accumulation start and alarm fire is the residual cost risk.
- **X-Ray trace archival at scale rate limits** (Round 20): The `BatchGetTraces` API has a limit of 5 traces per request with a rate limit of 5 requests/sec. For an incident affecting 100,000 traces, archival takes: 100,000 ÷ 5 ÷ 5 = 4,000 seconds (≈67 minutes). For 1 million traces, archival takes ~11 hours and may exceed the remaining 30-day retention window if the incident is discovered late. Operators must estimate trace volume via `GetTraceSummaries` count BEFORE starting archival and prioritize the most critical time windows.
- **ADOT Extension unauthenticated OTLP endpoint** (Round 20): The ADOT Extension accepts OTLP trace data on localhost:4318 without authentication (confirmed by ADOT documentation). Lambda's execution environment provides network isolation at the sandbox level — only code within the same Lambda invocation can reach the endpoint. However, a compromised third-party dependency (e.g., supply chain attack on a pip package) running within the Lambda could inject arbitrary trace data into X-Ray, creating misleading service map topology, false error annotations, or even PII violations (bypassing FR-155). The practical risk is LOW (requires compromised dependency with network access) but should be considered in the threat model.
- **Silent IAM permission degradation for centralized sampling** (Round 20): If `xray:GetSamplingRules` permission is removed from the Lambda execution role (e.g., during an IAM policy update), the ADOT Extension's awsproxy fails silently and the OTel sampler degrades from centralized sampling to `TraceIDRatioBased` at 1 trace/sec. This produces dramatically fewer traces in X-Ray but NO error in Lambda logs, NO CloudWatch alarm, and NO canary detection (the canary's own test traces still succeed because the canary uses Powertools Tracer, not OTel SDK). The degradation is invisible until an operator notices reduced trace volume. FR-159 prevents this by enumerating all required actions, but IAM drift after initial deployment remains a risk.

## Requirements *(mandatory)*

### Functional Requirements

**Backend Lambda X-Ray Subsegments**

- **FR-001**: System MUST add X-Ray tracing to all SSE Streaming Lambda operations: global stream handling, config stream handling, DynamoDB polling, connection acquisition, connection release, and event dispatch. Because the SSE Lambda uses `RESPONSE_STREAM` invoke mode, the Lambda runtime's auto-created segment closes before streaming begins. Tracing during streaming MUST use a mechanism with an independent lifecycle that does not depend on the Lambda runtime's X-Ray segment being open (see FR-026 revised).
- **FR-002**: System MUST add X-Ray subsegments to all 7 silent failure paths: (1) circuit breaker load, (2) circuit breaker save, (3) audit trail persistence, (4) downstream notification SNS publish, (5) time-series fanout batch write, (6) self-healing item fetch, (7) parallel fetcher error aggregation.
- **FR-003**: All service calls made by the Metrics Lambda (DynamoDB queries, CloudWatch API calls) MUST appear as auto-instrumented subsegments in X-Ray traces. Currently, the Metrics Lambda has zero X-Ray SDK integration despite Active tracing being enabled at the infrastructure level.
- **FR-004**: System MUST add explicit X-Ray subsegments to the Metrics Lambda for DynamoDB queries and CloudWatch `put_metric_data` calls.
- **FR-005**: System MUST mark X-Ray subsegments as error/fault when the wrapped operation catches an exception, including the exception type and message as metadata.

**X-Ray Annotations and Metadata**

- **FR-006**: System MUST replace custom structured latency logging in `latency_logger.py` with X-Ray subsegment annotations containing: `event_type`, `latency_ms`, `is_cold_start`, `is_clock_skew`, `connection_count`.
- **FR-007**: System MUST replace custom structured cache logging in `cache_logger.py` with X-Ray subsegment annotations containing: `cache_hit_rate`, `cache_entry_count`, `cache_max_entries`, `trigger`, `is_cold_start`.
- **FR-008**: System MUST include X-Ray annotations on connection lifecycle subsegments: `connection_id`, `current_count`, `max_connections`.
- **FR-009**: System MUST include X-Ray annotations on DynamoDB polling subsegments: `item_count`, `changed_count`, `poll_duration_ms`, `sentiment_type`.

**Correlation ID Consolidation**

- **FR-010**: System MUST replace the custom `get_correlation_id()` function with X-Ray trace ID retrieval from the active segment.
- **FR-011**: System MUST replace the custom `generate_correlation_id()` function with X-Ray trace ID retrieval.
- **FR-012**: System MUST include the X-Ray trace ID as a top-level field in all operational structured log entries (error logs, warning logs, info-level operational events) so CloudWatch Logs Insights queries can join log entries to X-Ray traces. Note: FR-022 and FR-023 remove tracing-specific structured logs; this requirement applies to the operational logs that remain.

**Cross-Service Trace Propagation**

- **FR-013**: System MUST verify that Active X-Ray tracing is enabled on both the Ingestion Lambda and the Analysis Lambda, and that the SNS subscription between them supports automatic trace context propagation via the `AWSTraceHeader` system attribute. The result is that both Lambda invocations appear under the same trace ID with zero manual MessageAttributes configuration.
- **FR-014**: System MUST propagate the `X-Amzn-Trace-Id` header from the frontend SSE proxy (Next.js API route) to the upstream SSE Lambda call when the header is present on the incoming request.
- **FR-015**: System MUST add `X-Amzn-Trace-Id` to the CORS `Access-Control-Allow-Headers` list for both API Gateway and Lambda Function URLs.
- **FR-016**: The frontend API client MUST include the `X-Amzn-Trace-Id` header on all outgoing requests when the browser has an active trace context. In test environments where RUM sampling is set to 100%, every API request from the browser MUST carry this header.

**Infrastructure Alignment**

- **FR-017**: All Lambda execution roles MUST have sufficient permissions to emit X-Ray trace segments and telemetry records. Currently, only 2 of 6 Lambda roles have explicit X-Ray write permissions; the remaining 4 (Ingestion, Analysis, Dashboard, Metrics) MUST be aligned.
- **FR-018**: X-Ray instrumentation errors MUST propagate to the Lambda runtime unhandled, resulting in Lambda invocation failure visible in standard error metrics. No error-suppression or fallback logic around tracing calls is permitted.

**X-Ray Canary (Watcher of the Watcher)**

- **FR-019**: System MUST implement a canary that periodically submits a batch of test traces to X-Ray and queries for their retrieval, reporting health via a non-X-Ray channel (CloudWatch metric). The canary MUST track both presence (did my test trace arrive?) and completeness (did all N of my test traces arrive?) to detect partial data loss from throttling.
- **FR-020**: System MUST configure the canary's health metric alarm with `treat_missing_data = breaching` so the alarm fires if the canary itself stops running.
- **FR-021**: The canary MUST be the sole non-X-Ray tracing and correlation mechanism in the system. All other custom tracing logs (latency_logger, cache_logger), custom correlation IDs, and custom log-based trace correlation MUST be consolidated onto X-Ray. Standard operational logging (error messages, audit records) and CloudWatch metrics (billing alarms, auto-scaling triggers, SLO dashboards) are not in scope and remain unchanged.

**Removal of Replaced Systems**

- **FR-022**: System MUST remove the custom latency structured logging from `latency_logger.py`. After X-Ray subsegment annotations replace its tracing function, the module and all its call sites MUST be deleted. No adapter or wrapper is permitted — the X-Ray annotations ARE the replacement.
- **FR-023**: System MUST remove the custom cache structured logging from `cache_logger.py`. After X-Ray annotations replace its tracing function, the module and all its call sites MUST be deleted.
- **FR-024**: System MUST remove the custom `get_correlation_id()` and `generate_correlation_id()` functions after X-Ray trace ID retrieval replaces them.

**SSE Streaming Trace Context (Round 2, revised in Round 3)**

- **FR-025** [Round 3 — Revised]: The SSE Lambda MUST preserve trace context across the async-to-sync bridge boundary. The system MUST rely on the default `threading.local()` context storage (which correctly propagates through `asyncio.new_event_loop().run_until_complete()` on the same thread). The system MUST NOT configure the X-Ray SDK's `AsyncContext`, which uses `TaskLocalStorage` that loses context across event loop boundaries (known bugs: aws-xray-sdk-python #164, #310, #446).
- **FR-026** [Round 3 — Replaced]: ~~During SSE streaming, the system MUST create independent X-Ray segments for each poll cycle.~~ **INVALIDATED**: `begin_segment()` is a no-op in Lambda (`LambdaContext.put_segment()` silently discards, `FacadeSegment` raises `FacadeSegmentMutationException`). **Replacement**: The SSE Lambda MUST use a tracing mechanism with an independent lifecycle for operations that execute during response streaming (after the handler returns the generator). This mechanism MUST: (a) create trace spans during active streaming, (b) link spans to the original invocation's trace ID for unified visualization, (c) export spans to X-Ray, (d) function independently of the Lambda runtime's facade segment lifecycle. The mechanism MUST run as a separate process (Lambda Extension) with its own INIT and SHUTDOWN phases, ensuring trace data is flushed even when the Lambda execution environment freezes.
- **FR-027** [Round 3 — Replaced]: ~~Auto-patched boto3 calls during streaming MUST execute within independently created segments.~~ **Replacement**: All service calls during SSE response streaming (DynamoDB queries, CloudWatch metric emission) MUST be captured within traced spans. These calls occur after the Lambda handler returns, when the X-Ray SDK's facade segment is closed. The independent lifecycle tracing mechanism from FR-026 MUST capture these calls so they appear in X-Ray as part of the connection's trace.

**SendGrid Explicit Instrumentation (Round 2 — Emergent)**

- **FR-028**: The SendGrid email sending operation in the Notification Lambda MUST be wrapped in an explicit X-Ray subsegment. The SendGrid SDK uses `urllib`/`python-http-client` for HTTP transport, which is NOT auto-patched by the X-Ray SDK. The subsegment MUST capture: HTTP status code, request duration, and error details on failure.

**Tracer Standardization (Round 2, updated in Round 3)**

- **FR-029** [Round 3 — Updated]: All non-streaming Lambda functions (Ingestion, Analysis, Dashboard, Notification, Metrics) MUST use a consistent X-Ray instrumentation approach that automatically captures exceptions as subsegment errors. The SSE Streaming Lambda MUST use the independent lifecycle tracing mechanism from FR-026, which has different instrumentation requirements due to the RESPONSE_STREAM segment lifecycle constraint. Both approaches MUST produce traces that appear in the same X-Ray console with unified trace IDs.
- **FR-030**: The system MUST eliminate double-patching of HTTP/AWS SDK clients. Currently, the Dashboard Lambda calls both explicit `patch_all()` and initializes a Tracer with `auto_patch=True` (the default), causing boto3 and requests to be patched twice. After standardization, each Lambda MUST have exactly one patching mechanism.

**Async Generator Tracing Safety (Round 3 — New)**

- **FR-031**: The system MUST NOT apply `@tracer.capture_method` or any equivalent function-wrapping decorator to async generator functions. The Powertools Tracer dispatcher does not check `inspect.isasyncgenfunction()` and silently routes async generators to the synchronous wrapper, which captures only generator creation time (near-zero) rather than iteration time. All async generator tracing MUST use manual subsegment or span context managers within the generator body. Source: `aws_lambda_powertools/tracing/tracer.py` — `isasyncgenfunction()` is never called in the dispatch chain.

**Frontend SSE Trace Propagation (Round 3 — New)**

- **FR-032**: The frontend SSE client MUST use `fetch()` with `ReadableStream` consumption instead of the `EventSource` API for connections that require trace header propagation. The `EventSource` API does not support custom HTTP headers per the WHATWG HTML Living Standard (Section 9.2). Using `fetch()` enables CloudWatch RUM's automatic `X-Amzn-Trace-Id` header injection (when `addXRayTraceIdHeader: true` is configured in the RUM http telemetry) and provides a mechanism for manual header attachment.
- **FR-033**: Since the `EventSource` API's built-in auto-reconnection is lost when switching to `fetch()` + `ReadableStream` (FR-032), the frontend SSE client MUST implement equivalent reconnection logic: exponential backoff with jitter on connection failure, `Last-Event-ID` header propagation for stream resumption, and connection state management matching the reliability guarantees of the replaced `EventSource`.

**Sampling Strategy (Round 3 — New)**

- **FR-034**: The system MUST configure X-Ray sampling rules per environment: 100% sampling (reservoir=1, fixed_rate=1.0) in dev/preprod for complete debuggability, and a documented production rate that balances cost with trace availability. The system MUST configure an X-Ray Group with filter expression `fault = true OR error = true` that automatically generates CloudWatch metrics (ApproximateErrorCount, FaultCount, latency percentiles) from errored traces, enabling error rate monitoring and alarming directly from trace data.
- **FR-035**: The system MUST configure server-side X-Ray sampling rules at the API Gateway and Lambda level that make independent sampling decisions, regardless of client-supplied `Sampled=1` in the `X-Amzn-Trace-Id` header. A malicious client MUST NOT be able to force 100% sampling and inflate X-Ray costs. The server-side sampling configuration is the authoritative source of truth for sampling decisions.

**Trace Data Integrity (Round 3 — New)**

- **FR-036**: The X-Ray canary (FR-019) MUST additionally track the ratio of submitted test traces to successfully retrieved test traces across consecutive intervals. When the retrieval ratio drops below a configured threshold (indicating systematic data loss from throttling, daemon failure, or SDK errors), the canary MUST emit a dedicated `trace_data_loss` CloudWatch metric that triggers an alarm.
- **FR-037**: The system MUST NOT manually configure the X-Ray SDK's `AsyncContext` on any Lambda function. The default `threading.local()` context storage correctly propagates through `asyncio.new_event_loop().run_until_complete()` calls on the same thread. Manually configuring `AsyncContext` uses `TaskLocalStorage`, which stores context on `asyncio.Task` objects — new tasks in a new event loop have no inherited context, breaking the async-to-sync bridge pattern used by the SSE Lambda. Known open issues: aws-xray-sdk-python #164 (concurrent async subsegments cause `AlreadyEndedException`), #310 (concurrent asyncio tasks + X-Ray in Lambda), #446 (context not propagated with `run_in_executor`).

**Cost Guard (Round 3 — New)**

- **FR-038**: The system MUST have CloudWatch billing alarms for X-Ray costs at thresholds of $10, $25, and $50 per month. At 100% sampling, X-Ray costs scale linearly at $5 per million traces recorded plus $0.50 per million traces retrieved/scanned. The free tier covers 100,000 traces recorded and 1,000,000 retrieved per month.

**Scope Clarification (Round 4 — New)**

- **FR-039**: "X-Ray exclusive" applies to **tracing** — the mechanism for distributed request tracing, latency analysis, and dependency visualization. CloudWatch alarms on Lambda built-in metrics (Errors, Duration, Throttles) and custom application metrics are a separate, complementary system for operational alerting. Alarms tell operators "something is wrong"; X-Ray traces tell operators "what went wrong and where." Both systems are required for complete observability. CloudWatch alarms are NOT duplicative with X-Ray and MUST NOT be removed as part of the X-Ray consolidation effort. Standard operational logging (error messages, audit records) also remains unchanged per FR-021.

**Operational Alarm Coverage (Round 4 — New)**

- **FR-040**: All 6 Lambda functions MUST have CloudWatch error alarms on the `AWS/Lambda` `Errors` metric. Currently, SSE Streaming Lambda and Metrics Lambda have zero error alarms (audit Section 4.3).
- **FR-041**: All 6 Lambda functions MUST have CloudWatch latency alarms on the `AWS/Lambda` `Duration` metric at the p95 statistic. Currently, Ingestion Lambda, Notification Lambda, SSE Streaming Lambda, and Metrics Lambda are missing latency alarms (audit Section 9.3).
- **FR-042**: The following custom metrics currently emitted without alarms MUST each have a CloudWatch alarm configured: `StuckItems` (items stuck in "pending" >5min), `ConnectionAcquireFailures` (SSE connection pool exhaustion), `EventLatencyMs` (SSE live update latency p95 > 3000ms SLO), `MetricsLambdaErrors` (monitoring system errors), `HighLatencyAlert` (ingestion latency >30s), `PollDurationMs` (DynamoDB poll duration), `AnalysisErrors` (analysis processing errors). Source: audit Section 4.2.
- **FR-043**: The 7 silent failure paths identified in audit Section 8 MUST emit dedicated CloudWatch metrics in addition to X-Ray subsegments (FR-002, FR-005). X-Ray subsegments provide trace context for debugging; CloudWatch metrics provide 100% occurrence capture for alarming regardless of X-Ray sampling configuration. The required metrics are: `CircuitBreakerPersistenceFailure` (paths 1-2), `AuditEventPersistenceFailure` (path 3), `DownstreamNotificationFailure` (path 4), `TimeseriesFanoutPartialFailure` (path 5), `SelfHealingItemFetchFailure` (path 6), `ParallelFetcherErrors` (path 7).
- **FR-044**: The CloudWatch dashboard alarm status widget MUST display ALL configured alarms. The audit identified that the current widget shows only 6 of 30+ alarms (audit Section 6.3), giving operators a false sense of "all green" when non-displayed alarms may be firing.
- **FR-045**: All CloudWatch alarms monitoring metrics where absence indicates failure (heartbeat metrics, canary metrics, pipeline throughput metrics such as `NewItemsIngested`) MUST use `treat_missing_data = breaching`. Alarms monitoring error count metrics where zero data points means "no errors occurred" MUST use `treat_missing_data = notBreaching`. The configuration MUST be audited and corrected for all existing alarms.

**ADOT Coexistence Constraints (Round 4 — New)**

- **FR-046**: The SSE Lambda MUST NOT enable ADOT auto-instrumentation via the `AWS_LAMBDA_EXEC_WRAPPER=/opt/otel-handler` environment variable. ADOT MUST function solely as an OTLP receiver sidecar (Lambda Extension) that accepts spans on `localhost:4318` and exports them to X-Ray. ADOT auto-instrumentation wraps the handler entry point (conflicting with Powertools `@tracer.capture_lambda_handler` from FR-029) and patches botocore (conflicting with X-Ray SDK patching from Powertools `auto_patch=True`), causing duplicate handler wrapping and duplicate spans for every boto3 call.
- **FR-047**: The OTel `service.name` resource attribute configured for ADOT span export on the SSE Lambda MUST match the `POWERTOOLS_SERVICE_NAME` environment variable used by Powertools Tracer. X-Ray service map aggregates nodes by service name. Mismatched names cause the same Lambda to appear as two disconnected nodes in the service map — one for handler-phase subsegments and one for streaming-phase spans.

**SSE Reconnection Trace Correlation (Round 4 — New)**

- **FR-048**: All SSE connection traces MUST include a stable `session_id` annotation that persists across reconnections, enabling operators to query X-Ray by `session_id` to find all traces for a logical SSE session. On reconnection (new trace ID due to new `fetch()` call), the trace MUST additionally include a `previous_trace_id` annotation referencing the prior connection's X-Ray trace ID and a `connection_sequence` annotation (incrementing integer starting at 1). X-Ray does not support native span links (OpenTelemetry link data stored in segment metadata is not queryable via X-Ray console or API), so annotation-based correlation is the required pattern.

**Meta-Observability (Round 4 — New)**

- **FR-049**: The observability canary (extending FR-019) MUST additionally verify CloudWatch metric emission health on each canary interval. The canary emits a known test metric via `put_metric_data`, waits for the CloudWatch ingestion delay (typically 60-120 seconds), and then queries for it via `get_metric_statistics`. If the test metric is not retrievable within the expected window, the canary reports CloudWatch metric pipeline failure. This check is in addition to the X-Ray trace submission/retrieval check from FR-019.
- **FR-050**: The canary MUST report CloudWatch metric pipeline failure via a channel independent of CloudWatch alarms. If CloudWatch is degraded, a CloudWatch alarm on the canary's own metric may also fail to fire. The canary MUST use an out-of-band alerting mechanism (e.g., SNS direct publish to email/SMS, external monitoring API) for meta-observability failures. Source: AWS Well-Architected Operational Excellence Pillar — "never alert on failure using the same system that failed."
- **FR-051**: The canary Lambda MUST use a separate IAM role from all application Lambdas. If an IAM policy change revokes X-Ray or CloudWatch permissions from application Lambda roles, the canary MUST retain its permissions to detect and report the failure. This isolation ensures the canary is not affected by the same failure mode it is designed to detect.

**OTel-to-X-Ray Trace Context Bridging (Round 5 — New)**

- **FR-052**: The SSE Lambda's OTel SDK MUST be configured with the `AwsXRayLambdaPropagator` (from `opentelemetry-propagator-aws-xray`) as the global text map propagator. This propagator reads the `_X_AMZN_TRACE_ID` Lambda environment variable (updated by the Lambda runtime on each invocation) and extracts the trace ID, parent span ID, and sampling decision into the OTel context. Without this propagator, streaming-phase OTel spans carry auto-generated trace IDs that differ from the Lambda runtime's X-Ray facade segment, creating disconnected traces that violate SC-010 and SC-023. The propagator MUST be configured before any OTel spans are created during the streaming phase. Source: `opentelemetry-propagator-aws-xray` package — `AwsXRayLambdaPropagator.extract()` reads `os.environ.get("_X_AMZN_TRACE_ID")`.
- **FR-053**: The SSE Lambda's OTel `TracerProvider` MUST be configured with `AwsXRayIdGenerator` (from `opentelemetry-sdk-extension-aws`). This ID generator embeds the current unix timestamp in the first 4 bytes of generated trace IDs, matching X-Ray's trace ID format requirement (`1-{unix_epoch_hex}-{unique_hex}`). The default OTel `RandomIdGenerator` produces random bytes in the epoch field, causing X-Ray to misindex traces by time window. Note: when linking to an existing Lambda trace via FR-052, the trace ID comes from `_X_AMZN_TRACE_ID` (already valid). `AwsXRayIdGenerator` matters for any new root spans (e.g., canary test traces) and is a safety net for correct X-Ray interoperability.
- **FR-054**: The SSE Lambda MUST configure the OTel OTLP exporter to send spans to the ADOT Extension's local HTTP endpoint (`http://localhost:4318/v1/traces`). This MUST be configured via the `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318` environment variable in Terraform or programmatically via the exporter constructor. The HTTP/protobuf exporter (`opentelemetry-exporter-otlp-proto-http`) is required — the gRPC exporter adds unnecessary binary dependency overhead in Lambda.

**OTel Span Lifecycle Management (Round 5 — New)**

- **FR-055**: The SSE Lambda's streaming generator MUST call `TracerProvider.force_flush()` in a `finally` block at the end of iteration, with a timeout of at least 5 seconds. The OTel `BatchSpanProcessor` buffers spans in memory and exports them to the ADOT Extension in batches (default batch timeout: 200ms). When the Lambda execution environment freezes after streaming completes, any spans buffered in the final batch interval are suspended in memory. Without `force_flush()`, these spans survive the freeze and are exported on the NEXT invocation — carrying stale trace IDs from the previous invocation, confusing operators and polluting traces. The `force_flush()` target is the local ADOT Extension OTLP receiver (localhost, sub-millisecond latency), not the remote X-Ray API.
- **FR-056**: The SSE Lambda's OTel `TracerProvider` MUST use the `parentbased_always_on` sampler (the OTel SDK default). This sampler reads the sampling decision from the parent context extracted by FR-052's propagator. When the Lambda runtime sets `Sampled=0` in `_X_AMZN_TRACE_ID`, the sampler suppresses span creation and export, preventing orphaned traces in X-Ray. When `Sampled=1`, all streaming-phase spans are created and exported normally. The system MUST NOT override the sampler to `always_on`, which would ignore the parent's sampling decision and create spans for unsampled invocations.

**OTel SDK Dependencies (Round 5 — New)**

- **FR-057**: The SSE Lambda's deployment package MUST include the following OTel Python packages for the streaming-phase instrumentation: (1) `opentelemetry-sdk` — core SDK including `TracerProvider`, `BatchSpanProcessor`, (2) `opentelemetry-sdk-extension-aws` — `AwsXRayIdGenerator` for X-Ray-compatible trace ID generation, (3) `opentelemetry-propagator-aws-xray` — `AwsXRayLambdaPropagator` for reading `_X_AMZN_TRACE_ID`, (4) `opentelemetry-exporter-otlp-proto-http` — OTLP HTTP exporter for sending spans to the ADOT Extension. These packages are ONLY required on the SSE Lambda — non-streaming Lambdas use Powertools Tracer (FR-029) which has no OTel dependency.

**Segment Document Size Guard (Round 5 — New)**

- **FR-058**: All X-Ray subsegment metadata payloads (both X-Ray SDK and OTel span attributes exported via ADOT) MUST be bounded to prevent exceeding the 64KB `PutTraceSegments` document size limit. Error messages MUST be truncated to 2,048 characters. HTTP response bodies MUST NOT be attached as metadata. Stack traces MUST be truncated to the first 10 frames. Documents exceeding 64KB are silently rejected by the X-Ray API (appearing in `UnprocessedTraceSegments` with no SDK-level error), causing permanent trace data loss with no alarm.

**Warm Invocation Trace Context Safety (Round 6 — New)**

- **FR-059**: The SSE Lambda's OTel trace context MUST be extracted from `_X_AMZN_TRACE_ID` at the start of each handler invocation, NOT at module level. On warm invocations, the Lambda runtime updates `_X_AMZN_TRACE_ID` to reflect the new invocation's trace ID, but module-level code executes only once (on cold start). Module-level `propagate.extract()` captures the first invocation's trace context and reuses it for ALL subsequent warm invocations, linking every streaming-phase OTel span to the wrong trace ID. The per-invocation extraction MUST occur inside the handler function, before any OTel spans are created. Source: AWS Lambda execution model — environment variables are updated by the runtime between invocations; module-level code runs only on INIT.

**SSE Lambda Auto-Patching Constraint (Round 6 — New)**

- **FR-060**: The SSE Lambda MUST initialize Powertools Tracer with `auto_patch=False` to prevent Powertools' global boto3/botocore patching from creating duplicate X-Ray subsegments alongside OTel spans during the streaming phase. With `auto_patch=True` (the default), Powertools patches `botocore.client.BaseClient._make_api_call` at import time. These patches persist globally and intercept ALL boto3 calls — including those made during the streaming phase after the handler returns. During streaming, each DynamoDB query and CloudWatch `put_metric_data` call creates BOTH an X-Ray subsegment (via the auto-patch, sent to the X-Ray daemon whose lifecycle during RESPONSE_STREAM is undocumented) AND an OTel span (via manual instrumentation, sent to ADOT). This dual-emission produces duplicate entries in X-Ray. Handler-phase AWS SDK calls MUST be traced via explicit `@tracer.capture_method` decorators rather than auto-patching.

**Alarm Threshold Calibration (Round 6 — New)**

- **FR-061**: Task 17 (alarm coverage) MUST review ALL existing CloudWatch alarm thresholds and align them with the 80-90% of timeout threshold standard applied to new alarms. The audit (Section 6.1) identified `analysis_latency_high` at 25s (42% of the Analysis Lambda's 60s timeout) as too generous — this threshold would not fire until nearly half the timeout is consumed, leaving insufficient margin for investigation and remediation before timeout failures begin. Existing alarms MUST be recalibrated to match the threshold methodology applied to new alarms. This review covers all pre-existing alarms, not only the one explicitly called out in the audit.

**ADOT Extension Operational Lifecycle (Round 7 — New)**

- **FR-062**: ~~The SSE Lambda's ADOT Lambda Extension MUST use the collector-only layer distribution.~~ **REWRITTEN in Round 8**: The SSE Lambda is container-based (ECR image with `python:3.13-slim` + custom bootstrap) — Lambda container images CANNOT use Lambda Layers. The ADOT collector MUST be embedded in the SSE Lambda's Dockerfile via multi-stage build using the collector-only container image from `public.ecr.aws/aws-observability/aws-otel-lambda-extension-amd64`. The collector-only image contains the OTel Collector binary that runs the OTLP receiver and X-Ray exporter pipeline, without bundled Python auto-instrumentation code (~35MB vs ~80MB). The Dockerfile MUST copy the collector binary to `/opt/extensions/collector` and the default configuration to `/opt/collector-config/config.yaml`. See FR-069 for deployment details and FR-070 for version pinning. Source: AWS Lambda Container Image docs — "You cannot add Lambda layers to container images"; ADOT ECR Public Gallery — `public.ecr.aws/aws-observability/`.
- **FR-063**: ~~The ADOT Extension layer ARN in Terraform MUST be pinned to a specific version.~~ **REWRITTEN in Round 8**: The ADOT container image reference in the SSE Lambda's Dockerfile MUST be digest-pinned (not tag-referenced). See FR-070 for requirements. The digest determines the embedded OTel Collector binary version, which defines the collector configuration schema, available receivers/exporters, and wire protocol compatibility. An unpinned tag (`:latest` or `:v0.40.0`) could silently introduce breaking changes when the Docker image is rebuilt. Unlike Lambda Layer ARNs which are region-specific, container images from ECR Public Gallery are region-agnostic. Source: OCI Distribution Specification — content-addressable image references.
- **FR-064**: The SSE Lambda MUST verify that the ADOT Extension's collector pipeline includes: (1) an OTLP HTTP receiver on port 4318, and (2) an AWS X-Ray exporter. The ADOT Extension's default collector configuration for Lambda includes this pipeline. If the default configuration needs modification (e.g., to strip unnecessary components for reduced memory footprint, or to add custom processors for attribute filtering), a custom collector configuration MUST be provided via the `OPENTELEMETRY_COLLECTOR_CONFIG_FILE` environment variable pointing to a configuration file in the Lambda deployment package. Unnecessary collector components (metrics pipelines, Prometheus receivers, zpages extensions) MUST be removed from custom configurations to minimize memory and CPU overhead within the 512MB allocation.

**TracerProvider Singleton Lifecycle (Round 7 — New)**

- **FR-065**: The SSE Lambda's OTel `TracerProvider` MUST be instantiated exactly once at module level (during Lambda INIT phase) and reused across all subsequent invocations on that execution environment. The `TracerProvider`, `BatchSpanProcessor`, and `OTLPSpanExporter` are invocation-independent infrastructure — they manage the export pipeline to the ADOT Extension. FR-059's per-invocation trace context extraction requirement applies ONLY to `AwsXRayLambdaPropagator.extract()` (reading the current `_X_AMZN_TRACE_ID` environment variable), NOT to `TracerProvider` instantiation. Creating a new `TracerProvider` per invocation spawns a new `BatchSpanProcessor` daemon thread (~1MB per thread) and OTLP HTTP connection, orphaning previous instances and causing monotonic memory growth that eventually triggers Lambda OOM termination on warm execution environments. Source: OTel Python SDK — `BatchSpanProcessor.__init__()` starts a daemon thread via `threading.Thread(daemon=True)`.

**OTel Export Failure Detection (Round 7 — New)**

- **FR-066**: The OTel Python SDK's `BatchSpanProcessor` silently catches all exporter exceptions (via `except Exception` in internal `_export()` method) and `force_flush()` returns `True` regardless of whether the underlying export succeeded or failed. This behavior is mandated by the OTel specification — exporters MUST NOT crash the application — and CANNOT be overridden via configuration. There is no built-in fail-fast mode. The SSE Lambda MUST NOT rely on `force_flush()` return value or exceptions to detect ADOT Extension failures. Instead, ADOT Extension health MUST be monitored via: (1) the X-Ray canary's end-to-end trace verification (FR-019/FR-036), which detects trace absence at aggregate level, and (2) Lambda Extension lifecycle logs (the runtime logs Extension registration and deregistration events, making Extension crashes visible in CloudWatch Logs). The practical mitigation is that the ADOT Extension runs on localhost with sub-millisecond latency — connection failures indicate the Extension process has crashed, not a transient network issue. Source: OTel Python SDK v1.39.1 — `BatchProcessor._export()` at `opentelemetry/sdk/_shared_internal/__init__.py`; OTel Specification, SDK Exporters section — "The SDK MUST NOT throw unhandled exceptions."

**OTel Span Semantic Conventions for X-Ray (Round 7 — New)**

- **FR-067**: Streaming-phase OTel spans MUST use semantic conventions that the ADOT X-Ray exporter maps to native X-Ray segment fields: (1) DynamoDB query spans MUST set `db.system = "dynamodb"` and `aws.dynamodb.table_names = ["{table_name}"]` attributes for X-Ray to render them as typed DynamoDB subsegments in the service map, (2) CloudWatch `put_metric_data` spans MUST set `rpc.service = "CloudWatch"` and `rpc.method = "PutMetricData"` for X-Ray to render them as typed CloudWatch subsegments, (3) error spans MUST record exceptions via the OTel span's `record_exception()` method (which sets `exception.type`, `exception.message`, and `exception.stacktrace` attributes per OTel semantic conventions), enabling X-Ray to populate the `cause` field with structured error data. Non-standard attribute names are stored as X-Ray metadata (non-indexed, non-searchable via X-Ray console or API filter expressions), rendering them invisible in default views and eliminating their value for operator debugging. Source: OTel Semantic Conventions for Database (https://opentelemetry.io/docs/specs/semconv/database/) and RPC (https://opentelemetry.io/docs/specs/semconv/rpc/); ADOT X-Ray exporter attribute mapping at `opentelemetry-collector-contrib/exporter/awsxrayexporter`.

**Propagator Configuration Hygiene (Round 7 — New)**

- **FR-068**: The SSE Lambda MUST NOT set the `OTEL_PROPAGATORS` environment variable in its Terraform configuration. The OTel Python SDK reads `OTEL_PROPAGATORS` at module import time and constructs the initial global propagator from it (default: `tracecontext,baggage` for W3C trace context). FR-052 requires `set_global_textmap(AwsXRayLambdaPropagator())` which unconditionally overwrites whatever `OTEL_PROPAGATORS` configured. Setting both creates misleading configuration — the environment variable appears active in Terraform/Lambda console but is silently overridden at runtime, causing operator confusion during debugging when the visible configuration does not match actual behavior. The `set_global_textmap()` call in application code is the single source of truth for propagator selection. Source: OTel Python SDK v1.39.1 — `opentelemetry/propagate/__init__.py`, module-level `environ.get(OTEL_PROPAGATORS)` parsing and `set_global_textmap()` override function.

**Container-Based ADOT Deployment (Round 8 — New)**

- **FR-069**: The SSE Lambda's Dockerfile MUST include the ADOT collector binary via multi-stage build from the official AWS ADOT Lambda Extension container image. The collector binary MUST be placed at `/opt/extensions/collector` and the default collector configuration at `/opt/collector-config/config.yaml`. Lambda discovers extensions by scanning `/opt/extensions/` during INIT — this is a Lambda service behavior independent of the runtime base image. The SSE Lambda uses `python:3.13-slim` with a custom bootstrap (not the AWS Lambda base image), which is fully compatible with Lambda Extensions. The multi-stage pattern `COPY --from=<adot-image> /opt/extensions/ /opt/extensions/` is the canonical approach for container-based Lambda Extensions. Source: AWS Lambda Container Image docs — "You cannot add Lambda layers to container images"; AWS Lambda Extensions API — extension discovery in `/opt/extensions/`.
- **FR-070**: The ADOT Lambda Extension container image reference in the SSE Lambda's Dockerfile MUST be digest-pinned (e.g., `public.ecr.aws/aws-observability/aws-otel-lambda-extension-amd64@sha256:{digest}`), NOT tag-referenced (`:latest` or `:v0.40.0`). Container image tags are mutable — the publisher can update the image behind a tag without notice. Digest pinning ensures reproducible builds and prevents silent introduction of breaking collector changes (schema changes, removed exporters) when the Docker image is rebuilt. Version updates MUST be explicit Dockerfile changes tracked in version control. Source: OCI Distribution Specification — content-addressable image references; Docker best practices for reproducible builds.

**OTel Resource Configuration (Round 8 — New)**

- **FR-071**: The SSE Lambda MUST set the `OTEL_SERVICE_NAME` environment variable in its Terraform configuration to a value matching `POWERTOOLS_SERVICE_NAME` (per FR-047). The OTel `AwsLambdaResourceDetector` does NOT set the `service.name` resource attribute. Without `OTEL_SERVICE_NAME`, the OTel SDK defaults to `unknown_service`, causing the SSE Lambda to appear as an `unknown_service` node in the X-Ray service map — indistinguishable from any other unconfigured service. This breaks SC-028 (service map correctness). The `OTEL_SERVICE_NAME` env var is read by `Resource.create()` during `TracerProvider` initialization and does not require code changes. Source: OTel Python SDK — `opentelemetry.sdk.resources.Resource.create()` reads `OTEL_SERVICE_NAME`; AWS X-Ray uses `service.name` for service map node naming.
- **FR-072**: The SSE Lambda's `TracerProvider` MUST be configured with `AwsLambdaResourceDetector` to populate resource attributes (`cloud.provider`, `cloud.region`, `faas.name`, `faas.version`, `faas.instance`, `faas.max_memory`) on all emitted spans. These attributes provide Lambda execution context in X-Ray trace metadata, enabling operators to identify which Lambda function, version, and execution environment produced each span. The resource configuration MUST merge `Resource.create()` (which reads `OTEL_SERVICE_NAME`) with `AwsLambdaResourceDetector().detect()` to combine service identity with Lambda metadata. Source: `opentelemetry-sdk-extension-aws` package — `AwsLambdaResourceDetector` in `resource/_lambda.py`.

**BatchSpanProcessor Lambda Configuration (Round 8 — New)**

- **FR-073**: The SSE Lambda's `BatchSpanProcessor` MUST be configured with Lambda-tuned parameters: `schedule_delay_millis=1000` (1 second, down from default 5 seconds), `max_queue_size=256` (down from default 2048), `max_export_batch_size=64` (down from default 512). The defaults are designed for long-running server processes with high span throughput. The SSE Lambda produces a bounded number of spans per streaming session (~50-200 over 15 seconds). Reduced parameters consume less memory (smaller queue buffer), export more frequently (1s vs 5s), and reduce the span count at risk if `force_flush()` is not reached. Configuration MUST be via `OTEL_BSP_SCHEDULE_DELAY`, `OTEL_BSP_MAX_QUEUE_SIZE`, and `OTEL_BSP_MAX_EXPORT_BATCH_SIZE` environment variables in Terraform for visibility, or via constructor parameters in application code. Source: OTel Python SDK — `BatchSpanProcessor` defaults and `OTEL_BSP_*` environment variable support.

**Two-Hop Flush Architecture Acknowledgment (Round 9 — New)**

- **FR-074**: The spec acknowledges that span export from the SSE Lambda to X-Ray involves TWO asynchronous hops: (1) SDK → ADOT Extension via OTLP HTTP on localhost:4318 (fast, sub-millisecond, reliable — handled by FR-055's `force_flush()`), and (2) ADOT Extension → X-Ray backend via HTTPS (slow, measured in hundreds of milliseconds, subject to KNOWN race condition documented in aws-otel-lambda#886 and opentelemetry-lambda#224). The decouple processor in the ADOT Lambda collector mitigates the second hop by holding the Extension from signaling completion until queued data is exported. However, under worst-case timing (span.end() as the very last operation before function exit), the collector context may be canceled before HTTP exports complete, resulting in span loss with error pattern "context canceled" + "Exporting failed. Dropping data." Per-invocation detection of this loss is NOT possible because it occurs in the Extension process. The canary (FR-019/FR-036) provides aggregate detection. The system MUST NOT add fallback logic or retry mechanisms around this — per FR-018, the Extension's internal failures are the Extension's responsibility. The ADOT Extension's decouple processor is the accepted mitigation, not a fallback.
- **FR-075**: The ADOT Extension's collector configuration for the SSE Lambda MUST include the `decouple` processor in the pipeline to prevent the Lambda-specific span drop race condition. The decouple processor separates span reception from export, ensuring the Extension does not call the Next API until all queued spans are exported to the X-Ray backend. Without the decouple processor, the Extension signals invocation completion while HTTP exports are still in-flight, and the execution environment freezes before exports complete. The decouple processor is included in the ADOT Lambda Extension's default configuration — a custom `collector.yaml` MUST preserve it if FR-064's configuration modifications remove it. Source: https://github.com/open-telemetry/opentelemetry-lambda/blob/main/collector/processor/decoupleprocessor/README.md

**OTel SDK Lambda Safety (Round 9 — New)**

- **FR-076**: The SSE Lambda MUST NOT call `BotocoreInstrumentor().instrument()` or any OTel auto-instrumentation for AWS SDK clients. The SSE Lambda's instrumentation architecture is: (a) handler-phase AWS SDK calls traced via explicit Powertools `@tracer.capture_method` decorators (per FR-060), (b) streaming-phase AWS SDK calls traced via manual OTel span creation with semantic conventions (per FR-067). Enabling `BotocoreInstrumentor` would patch `botocore.client.BaseClient._make_api_call` globally, creating OTel spans for EVERY AWS SDK call — including handler-phase calls that are already traced by Powertools, producing duplicate entries in X-Ray. The prohibition on `auto_patch=True` (FR-060) prevents Powertools' X-Ray SDK patching but does NOT prevent OTel botocore instrumentation, which is a separate mechanism. Both MUST be prohibited.
- **FR-077**: The SSE Lambda's `TracerProvider` MUST be instantiated with `shutdown_on_exit=False`. The default `shutdown_on_exit=True` registers a Python `atexit` handler that calls `TracerProvider.shutdown()` during interpreter finalization. In Lambda, `atexit` handlers fire during execution environment recycling (not between warm invocations). In Python 3.13+, the `ConcurrentMultiSpanProcessor.shutdown()` can race with a pending export, causing `RuntimeError: cannot schedule new futures after shutdown`. Since FR-055 mandates `force_flush()` at end of each streaming session, the `atexit` handler is redundant. Setting `shutdown_on_exit=False` eliminates the race with zero functional impact. Source: opentelemetry-python#4461 — `ConcurrentMultiSpanProcessor` shutdown race condition.

**Canary Robustness (Round 9 — New)**

- **FR-078**: The X-Ray canary (FR-019) MUST implement retry-with-backoff for trace retrieval. After submitting test traces, the canary queries `BatchGetTraces` at 30 seconds, then retries at 60 seconds if traces are not found. X-Ray's propagation delay is "generally 30 seconds" per the AWS FAQ but is NOT a deterministic SLA — under regional load, indexing may take longer. The `BatchGetTraces` API cannot distinguish "not yet indexed" from "trace permanently lost" — both result in the trace ID appearing in `UnprocessedTraceIds` or being absent from the `Traces` response array. The canary MUST treat these cases identically and only report failure after the final retry. The 2-consecutive-interval alarm threshold (FR-020) provides the outer detection boundary. Source: https://aws.amazon.com/xray/faqs/
- **FR-079**: The canary (FR-049) MUST classify CloudWatch `put_metric_data` errors into two categories and handle them differently: (a) **Transient errors** (`ThrottlingException`, `InternalFailure`, `ServiceUnavailable`, `InternalServiceError`) — retry with exponential backoff (max 3 retries), then report failure if all retries exhausted, (b) **Permanent IAM/credential errors** (`AccessDeniedException`, `NotAuthorized`, `InvalidClientTokenId`) — trigger IMMEDIATE out-of-band alert via FR-050 with zero retries. IAM revocation is the EXACT failure mode that FR-051's separate IAM role is designed to detect. Without error classification, an IAM revocation on application Lambda roles is silently retried for multiple canary intervals, losing critical response time. The canary's own metric emission uses its separate IAM role (FR-051) and is unaffected by application role changes — but the canary's CloudWatch HEALTH CHECK (which verifies application metrics are flowing) must detect when application roles lose permissions. Source: https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/API_PutMetricData.html
- **FR-080**: The canary SHOULD support a secondary out-of-band alerting channel (external webhook endpoint such as PagerDuty or Opsgenie HTTPS API) in addition to SNS direct publish (FR-050). During simultaneous AWS regional failures affecting both CloudWatch AND SNS, the canary has no alerting path with SNS alone. Industry best practice from AWS outage post-mortems (e.g., 2017 S3 outage) is to use at least two independent alerting channels. This is a SHOULD (not MUST) because simultaneous CloudWatch + SNS regional failure is a low-probability event, but production deployments handling critical workloads benefit from this redundancy.

**Frontend SSE Server-Side Requirements (Round 9 — New)**

- **FR-081**: The SSE Lambda server MUST check the `Last-Event-ID` request header on connection establishment and resume the event stream from the corresponding position. If the client sends `Last-Event-ID: {id}` (per FR-033's client-side requirement), the server MUST NOT replay events already delivered (events with id <= Last-Event-ID). Without server-side support, FR-033's client-side `Last-Event-ID` propagation is hollow — the client sends the header but the server ignores it, causing duplicate events on reconnection. If server-side stream resumption is not feasible for the current SSE Lambda architecture (e.g., events are ephemeral and not stored), the server MUST respond with a `X-SSE-Resume-Supported: false` header so the client can implement idempotent deduplication.
- **FR-082**: The SSE Lambda Function URL's CORS configuration MUST include `x-amzn-trace-id` in `Access-Control-Expose-Headers`. Per the CORS specification, browsers can only read response headers explicitly listed in `Access-Control-Expose-Headers`. FR-048 requires the client to capture the response `X-Amzn-Trace-Id` header value as `previous_trace_id` for reconnection trace correlation. Without `ExposeHeaders` including this header, the browser JavaScript cannot access it, making FR-048's reconnection trace correlation impossible. This is a hard prerequisite for FR-048, not optional.
- **FR-083**: The frontend SSE client's reconnection logic (FR-033) MUST distinguish between graceful server close and network error using the `ReadableStreamDefaultReader.closed` Promise (fulfills on graceful close, rejects on error). On graceful server close (expected — e.g., Lambda timeout, stream complete): reconnect with the SSE `retry:` field value or a short fixed delay (1-3 seconds). On network error (unexpected — e.g., WiFi drop, server crash): reconnect with exponential backoff plus jitter per FR-033's existing specification. Uniform backoff on all close types unnecessarily delays reconnection after expected server-initiated closes, degrading user experience.

**BSP Default Correction (Round 9 — Assumption Fix)**

- **FR-084**: The spec's Round 5 assumption regarding "default batch timeout of 200ms" is INCORRECT and MUST be corrected. The actual `BatchSpanProcessor` `schedule_delay_millis` default is **5000ms** (5 seconds), confirmed by the OTel Python SDK source code and the OTel Specification. FR-073 correctly overrides this to 1000ms, so runtime behavior is unaffected. The incorrect 200ms figure in the assumption and edge case text (originally added in Round 5) caused downstream reasoning errors about the flush window size — the actual default means spans buffer 25x longer than documented. This FR corrects the assumption; no runtime change is required.

**Client Disconnect Handling (Round 10 — New)**

- **FR-085**: The SSE Lambda's streaming generator MUST catch `BrokenPipeError` (Python's signal that the client has disconnected mid-write) in its event dispatch loop and set the OTel span attribute `client.disconnected=true` on the active span before propagating the exception. Python custom runtimes have NO standard mechanism to detect client disconnect (unlike Node.js `StreamifyResponse` which provides `metadata.onAbort`). `BrokenPipeError` on `response.write()` is the ONLY available signal. When caught: (a) set `client.disconnected=true` annotation on the current streaming-phase span, (b) set span status to `OK` (not `ERROR` — client disconnect is expected behavior, not a server fault), (c) skip remaining DynamoDB polls, (d) proceed to `finally` block for `force_flush()`. Without this handling, post-disconnect spans report successful operations that no client received, inflating success metrics. Source: https://docs.aws.amazon.com/lambda/latest/dg/configuration-response-streaming.html — "Streamed responses are not interrupted or stopped when the invoking client connection is broken."

**BSP Queue Size Correction (Round 10 — Amends FR-073)**

- **FR-086**: FR-073's `max_queue_size` MUST be recalculated based on expected span throughput per invocation and set to >= 2x the expected maximum. Calculation: 15-second streaming session × ~15 DynamoDB polls/second × ~3 spans per poll cycle (db query span + event dispatch span + metric emit span) = ~675 spans per invocation. 2x safety margin = 1,350 minimum. **FR-073 is hereby AMENDED**: `max_queue_size` changes from `512` to `1500`. The `max_export_batch_size` remains at `64` (unchanged) and `schedule_delay_millis` remains at `1000` (unchanged). The 2x margin accounts for: (a) variable poll frequency under load, (b) additional spans from error handling paths, (c) spans created between `force_flush()` calls during batch intervals. The silent drop behavior of `BatchSpanProcessor` (logs WARNING only, no metric, no exception) makes undersized queues particularly dangerous — span loss is invisible without explicit log monitoring. Source: OTel Python SDK — `BatchSpanProcessor.on_end()` uses `queue.put_nowait()`, catches `queue.Full`, logs warning.

**Canary IsPartial Trace Check (Round 10 — New)**

- **FR-087**: The X-Ray canary (FR-078) MUST check the `IsPartial` flag on `TraceSummary` objects returned by `GetTraceSummaries` or `BatchGetTraces` for its test traces. If `IsPartial=true` after the final retry attempt (per FR-078's retry-with-backoff): (a) emit a dedicated CloudWatch metric `canary.trace.partial` with value 1, (b) report the trace verification as **degraded** (not complete success AND not complete failure), (c) include `is_partial=true` in the canary's OTel span attributes. A trace with `IsPartial=true` means not all segments have been received by X-Ray — critical streaming-phase spans from ADOT may be missing while the trace "exists" from the canary's perspective. Without this check, the canary reports false-green when ADOT span export is failing but the Lambda runtime's facade segment arrived successfully. Source: https://docs.aws.amazon.com/xray/latest/devguide/xray-concepts.html — "X-Ray tags traces as incomplete or partial."

**SSE Reconnection Control (Round 10 — New)**

- **FR-088**: The SSE Lambda MUST emit a `retry:` field in the initial SSE frame of each connection with a recommended reconnection interval in milliseconds (e.g., `retry: 3000\n`). The SSE specification (WHATWG HTML Living Standard, Section 9.2) defines this field as the server-controlled mechanism for client reconnection timing. Without it, clients use their own implementation-defined default (varies by browser/library). The `retry:` value MUST be: (a) emitted as the FIRST field in the SSE stream (before any `data:` events), (b) set to 3000ms (3 seconds) as default, (c) configurable via environment variable `SSE_RETRY_MS` for per-environment tuning. Note: since the frontend uses `fetch()+ReadableStream` (not `EventSource`), the `retry:` field is NOT auto-parsed — the frontend must explicitly parse it per FR-089. Source: https://html.spec.whatwg.org/multipage/server-sent-events.html — Section 9.2, "retry" field.
- **FR-089**: The frontend SSE client (FR-033/FR-083) MUST parse the `retry:` field from the server's SSE stream and use its value as the reconnection delay for graceful server closes (overriding FR-083's default 1-3 second range with the server-specified value). Since the frontend uses `fetch()+ReadableStream` (NOT `EventSource`), the SSE protocol fields (`retry:`, `id:`, `event:`, `data:`) are NOT automatically parsed. The client MUST implement SSE field parsing for at least `retry:` and `data:` fields. If no `retry:` field is received, fall back to FR-083's default behavior. This ensures server-controlled reconnection timing without requiring `EventSource` (which cannot send custom headers per FR-032's CORS requirements).

**Explicit Decouple Processor in Custom Config (Round 10 — Strengthens FR-075)**

- **FR-090**: The custom ADOT collector YAML (created per FR-069 for the container-based SSE Lambda) MUST explicitly include the `decouple` processor in the `service.pipelines.traces.processors` list. The ADOT Lambda Layer's automatic decouple processor injection (documented in the decouple processor README: "the OpenTelemetry Lambda Layer automatically configures the decouple processor when the batch processor is used") ONLY applies to Lambda Layer deployments. Container-based deployments with custom collector configurations (FR-064) receive NO automatic processor injection. Without explicit inclusion, the SSE Lambda's span export reverts to the known ~30% drop rate (aws-otel-lambda#886). This FR strengthens FR-075 by making the container-specific requirement explicit. The pipeline order MUST be: `processors: [decouple, batch]` (decouple before batch). Source: https://github.com/open-telemetry/opentelemetry-lambda/blob/main/collector/processor/decoupleprocessor/README.md

**BSP Queue Overflow Observability (Round 10 — New)**

- **FR-091**: The SSE Lambda MUST configure Python logging to capture `BatchSpanProcessor` WARNING-level log messages and emit a structured log entry when span queue overflow occurs. The OTel SDK logs `"Queue is full, dropping span"` at WARNING level when `queue.put_nowait()` raises `Full`. This log line is the ONLY observable signal of span loss (no metric, no exception, no span attribute). The structured log entry MUST include: (a) `span_queue_overflow=true` field for CloudWatch Logs Insights filtering, (b) current queue size, (c) span name being dropped. A CloudWatch Logs metric filter on `span_queue_overflow=true` SHOULD be created to provide alarming on span loss. This is defense-in-depth — FR-086's queue sizing should prevent overflow, but silent data loss requires independent detection.

**Custom Bootstrap Trace Context Propagation (Round 11 — New)**

- **FR-092**: The SSE Lambda's custom bootstrap MUST read the `Lambda-Runtime-Trace-Id` header from the `/runtime/invocation/next` Runtime API response and set `os.environ['_X_AMZN_TRACE_ID']` to its value BEFORE calling the handler function on EVERY invocation (cold and warm). Per the AWS Lambda custom runtime contract, the bootstrap is responsible for propagating trace context from the Runtime API to the function process. Without this, `_X_AMZN_TRACE_ID` retains the cold start value on warm invocations, and FR-059's `AwsXRayLambdaPropagator.extract()` reads stale trace context — silently linking all warm invocation OTel spans to the first invocation's trace ID. The X-Ray SDK's `LambdaContext._refresh_context()` also reads this env var, meaning BOTH the handler-phase (Powertools) and streaming-phase (OTel) traces are broken on warm invocations without this fix. This is a prerequisite for FR-052 and FR-059. Source: AWS Lambda Custom Runtime Contract — "Your function must set `_X_AMZN_TRACE_ID` to the value of the `Lambda-Runtime-Trace-Id` header"; confirmed by code inspection that the current bootstrap at `src/lambdas/sse_streaming/bootstrap` (lines 39-51) reads `Lambda-Runtime-Aws-Request-Id` but omits `Lambda-Runtime-Trace-Id`.

**Deadline-Aware Proactive Span Flush (Round 11 — New)**

- **FR-093**: The SSE Lambda's streaming generator MUST implement a deadline-aware flush mechanism that calls `TracerProvider.force_flush()` BEFORE the Lambda timeout kills the process. The Lambda Runtime API response includes a `Lambda-Runtime-Deadline-Ms` header containing the invocation deadline as a Unix epoch timestamp in milliseconds. The streaming generator MUST check remaining time on each poll cycle and trigger proactive flush when remaining time falls below a configurable threshold (default: 3000ms). This is REQUIRED because Lambda does NOT send SIGTERM to the custom runtime process on timeout — the process is killed directly (SIGKILL after Extension Shutdown phase). Python `finally` blocks do NOT execute on SIGKILL. FR-055's `force_flush()` in the `finally` block handles normal completion and client disconnect (BrokenPipeError) but NOT Lambda timeout. Without proactive flush, up to 1000ms of buffered spans (per FR-073's batch interval) are permanently lost on every timeout. The `finally` block `force_flush()` (FR-055) MUST be retained as defense-in-depth for non-timeout exits; FR-093 adds proactive flush for the timeout path. Source: AWS Lambda Runtime Environment Lifecycle — Lambda does not send SIGTERM before SIGKILL on invocation timeout; Lambda Runtime API — `Lambda-Runtime-Deadline-Ms` header.

**FR-035 Amendment: Function URL Sampling Limitation (Round 11 — Amends FR-035)**

- **FR-094**: FR-035's requirement that "server-side X-Ray sampling rules make independent sampling decisions, regardless of client-supplied Sampled=1" is achievable for API Gateway paths (API Gateway makes its own sampling decision when tracing is enabled) but NOT achievable for Lambda Function URL paths. Lambda Function URLs honor the incoming `Sampled=1` header from the client with no override mechanism — no WAF header-rewrite, no intermediary tracing layer, no server-side sampling override. **FR-035 is hereby SCOPED**: (a) For API Gateway endpoints (Dashboard, Ingestion, Analysis, Notification, Metrics): FR-035 is satisfied by API Gateway's built-in tracing integration, (b) For the SSE Lambda Function URL: the system MUST mitigate client-forced sampling via application-level defense — the SSE Lambda's OTel `parentbased_always_on` sampler (FR-056) respects the parent context's `Sampled` flag, but the Lambda runtime has already created the facade segment; mitigation is limited to cost monitoring (FR-038) and Function URL throttling configuration. The risk is acknowledged: at current traffic levels (<1M/month), worst-case forced sampling costs <$5/month; at scale, Function URL throttling limits concurrent connections. Source: API Gateway X-Ray integration documentation — API Gateway makes independent sampling decisions; Lambda Function URL documentation — no X-Ray sampling override mechanism.

**ADOT Collector Config Environment Variable (Round 11 — New)**

- **FR-095**: The SSE Lambda's Terraform configuration MUST set the `OPENTELEMETRY_COLLECTOR_CONFIG_FILE` environment variable to `/opt/collector-config/config.yaml`. Without this environment variable, the ADOT Extension ignores the custom configuration file placed by FR-069 and uses its compiled-in embedded default. The embedded default includes the decouple processor (safe) but also includes unnecessary components (gRPC receiver on port 4317, potentially metrics pipeline) that consume approximately 15MB of additional memory without value. The custom config (FR-064/FR-090) strips these components and specifies the explicit `[decouple, batch]` pipeline ordering. This FR ties together FR-064 (custom config specification), FR-069 (file placement in container), and FR-090 (decouple processor requirement) with the Terraform configuration that activates the file. Source: ADOT Lambda Extension — configuration file path is read exclusively from the `OPENTELEMETRY_COLLECTOR_CONFIG_FILE` environment variable; no filesystem auto-discovery.

**Canary Invocation Interval and Architecture (Round 11 — New)**

- **FR-096**: The observability canary (FR-019/FR-049) MUST run on a 5-minute invocation interval via EventBridge (CloudWatch Events) scheduled rule. The 5-minute interval balances detection latency (10 minutes worst-case per FR-020's 2-consecutive-interval alarm) with cost efficiency (~8,640 invocations/month). The canary MUST use a two-phase architecture for CloudWatch metric health checks (FR-049): each invocation (a) emits a test metric to CloudWatch AND (b) queries for the PREVIOUS invocation's test metric (emitted 5 minutes ago, well beyond the 60-120 second ingestion delay). This eliminates the need for long Lambda timeouts waiting for CloudWatch ingestion within a single invocation. The canary Lambda timeout MUST be set to 60 seconds (sufficient for X-Ray trace query retry at 30s per FR-078, plus CloudWatch API calls). The canary MUST persist state (previous invocation's test metric identifier) in an SSM Parameter Store parameter — NOT in DynamoDB (canary must not depend on application infrastructure per FR-051's isolation principle). Source: AWS Well-Architected Reliability Pillar — monitoring systems should have minimal dependencies on the systems they monitor.

**FR-043 Amendment: Metric Namespace and Dimensions (Round 11 — Amends FR-043)**

- **FR-097**: FR-043's 6 silent failure CloudWatch metrics MUST use the unified namespace `SentimentAnalyzer/Reliability` with the following dimensions: `FunctionName` (the emitting Lambda function name) and `FailurePath` (one of: `circuit_breaker_load`, `circuit_breaker_save`, `audit_trail_persist`, `notification_publish`, `timeseries_fanout`, `self_healing_fetch`, `parallel_fetcher`). The unified namespace ensures all reliability metrics are queryable in a single CloudWatch namespace without cross-namespace correlation. The `FailurePath` dimension enables per-path alarming (FR-042) and aggregated reliability dashboards. Additionally, the `HighLatencyAlert` metric (FR-042) MUST use the namespace `SentimentAnalyzer/Reliability` — resolving the discrepancy between the audit's `SentimentAnalyzer/Ingestion` and Task 17's `SentimentAnalyzer/SSE` (the metric is emitted by the Ingestion Lambda but represents a system-level reliability signal). All existing custom metrics listed in FR-042 that currently use inconsistent namespaces SHOULD be migrated to `SentimentAnalyzer/Reliability` for consolidation.

**ADOT Export Failure CloudWatch Logs Monitoring (Round 11 — New)**

- **FR-098**: The SSE Lambda's CloudWatch Log Group MUST have a metric filter matching the ADOT Extension's export failure log pattern: `"Exporting failed. Dropping data."`. The metric filter MUST emit a CloudWatch metric `SentimentAnalyzer/Reliability/ADOTExportFailure` with value 1 on each match. A CloudWatch alarm on this metric (threshold: >= 1 in any 5-minute period) provides per-invocation detection of the known span drop race condition (aws-otel-lambda#886). This complements FR-091 (BSP queue overflow detection at the SDK level) and FR-019/FR-036 (canary aggregate detection) by catching span loss at the ADOT Extension level — the "second hop" in the two-hop flush architecture (FR-074). The metric filter pattern MUST also capture the preceding log line containing `"context canceled"` to distinguish Extension shutdown races from other export failures. Source: aws-otel-lambda#886 error pattern: `"context canceled"` followed by `"Exporting failed. Dropping data."`.

**Frontend SSE TextDecoder Streaming Mode (Round 11 — New)**

- **FR-099**: The frontend SSE client (FR-032/FR-089) MUST instantiate `TextDecoder` with the `{stream: true}` option when decoding ReadableStream chunks. Without `stream: true`, the decoder treats each chunk independently, replacing incomplete multi-byte UTF-8 sequences at chunk boundaries with U+FFFD (replacement character). SSE event data may contain non-ASCII characters (article titles, sentiment analysis text with international content). The `stream: true` option retains incomplete byte sequences between `decode()` calls, correctly reconstructing multi-byte characters that span chunk boundaries. This is a one-line configuration change (`new TextDecoder('utf-8', {stream: true})`) with significant correctness implications. Source: WHATWG Encoding Standard — TextDecoder `stream` option: "If true, the decoder will not flush its internal state on each decode() call."

**Post-Proactive-Flush Graceful Termination (Round 12 — New)**

- **FR-100**: After FR-093's proactive `force_flush()` fires (remaining time < 3000ms), the streaming generator MUST yield an `event: deadline\ndata: {"reason": "timeout_imminent", "remaining_ms": <value>}\n\n` SSE event and then terminate normally (return from the generator function). This converts a potential Lambda timeout (runtime SIGKILL, no `finally` blocks) into a graceful Lambda completion (handler returns, ADOT Extension receives NORMAL Shutdown with `shutdownReason: "spindown"`). The ADOT Extension's 2000ms Shutdown phase then exports all buffered spans reliably. Without FR-100, the generator continues creating spans after proactive flush — these post-flush spans are either (a) exported in the next BSP batch interval if time allows, or (b) permanently lost when the runtime is SIGKILL'd at timeout. The frontend client (FR-032) MUST handle `event: deadline` by immediately reconnecting to a fresh invocation via the existing reconnection logic (FR-033), using short delay (not exponential backoff) since this is a server-initiated graceful close per FR-083. Source: AWS Lambda Extensions API — Shutdown event with `shutdownReason` reflects how the invocation ended; normal completion produces `"spindown"`, timeout produces `"timeout"`.

**OTel Python Package Version Pinning (Round 12 — New)**

- **FR-101**: OTel Python core packages (`opentelemetry-api`, `opentelemetry-sdk`, `opentelemetry-exporter-otlp-proto-http`) MUST be pinned to the same version using exact `==` specifier in the SSE Lambda's requirements file. AWS contrib packages (`opentelemetry-propagator-aws-xray`, `opentelemetry-sdk-extension-aws`) MUST use floor pins with major version ceiling (e.g., `>=1.0.2,<2.0.0`). The OTel Python core SDK enforces lockstep `==` pins between packages internally (`opentelemetry-sdk` depends on `opentelemetry-api==<exact-version>`), so pip prevents version mismatches at install time. However, without explicit pins in requirements, different `pip install` runs on different dates resolve to different lockstep versions, making builds non-reproducible. The ADOT collector and OTel SDK communicate via OTLP v1 protocol, which has been stable since OTel proto 1.0.0 and is compatible with all ADOT Collector versions since v0.21.0. Source: PyPI package metadata — `opentelemetry-sdk` declares `opentelemetry-api==1.39.1` (exact pin); OTLP v1 proto path: `opentelemetry.proto.collector.trace.v1`.

**SSE Lambda Memory Allocation Increase (Round 12 — Amends Round 6 Assumption)**

- **FR-102**: The SSE Lambda memory allocation MUST be increased from 512MB to 1024MB. Justification: (a) ADOT Extension resident memory: ~50MB, (b) OTel SDK packages import overhead: ~30MB, (c) Python 3.13 runtime: ~100MB, (d) application code + dependencies: ~100-150MB, (e) BSP queue (1500 spans): ~15MB, (f) DynamoDB response buffers: variable. Total baseline: ~350-400MB, leaving only ~110-160MB headroom at 512MB — insufficient for burst allocations during concurrent DynamoDB responses. At 1024MB: ~624-674MB headroom, and Lambda CPU doubles (CPU scales linearly with memory), reducing cold start from ~1200ms to ~800ms. Monthly cost increase: ~$2-3 at current invocation patterns (well within FR-038's $10 alarm threshold). This amends the Round 6 assumption that "512MB provides sufficient headroom." Source: AWS Lambda pricing — $0.0000166667/GB-second; Lambda CPU allocation documentation.

**Bootstrap Deadline Propagation (Round 12 — New, Prerequisite for FR-093)**

- **FR-103**: The SSE Lambda's custom bootstrap MUST read the `Lambda-Runtime-Deadline-Ms` header from the Runtime API `/runtime/invocation/next` response and make the deadline timestamp accessible to the handler. The deadline value (milliseconds since Unix epoch) MUST be passed either (a) as a `deadline_ms` key in the event data dictionary, or (b) as an environment variable `_LAMBDA_DEADLINE_MS` set before handler invocation. The handler (or streaming generator) computes remaining time as `deadline_ms - current_time_ms`. The standard `context.get_remaining_time_in_millis()` API is unavailable because the bootstrap currently passes `None` for the Lambda context parameter. Without FR-103, FR-093 (proactive deadline-aware flush) is UNIMPLEMENTABLE — the handler has no way to know the execution deadline. Confirmed by code inspection: `src/lambdas/sse_streaming/bootstrap` line 157 calls `handler(event_data, None)`. Source: AWS Lambda Runtime API — `/runtime/invocation/next` response headers include `Lambda-Runtime-Deadline-Ms`.

**Lambda Memory Utilization Alarms (Round 12 — New)**

- **FR-104**: All 6 Lambda functions MUST have CloudWatch memory utilization alarms at 85% of configured memory. The alarm monitors memory usage via CloudWatch Logs metric filter on Lambda `REPORT` log lines (which include `Max Memory Used: <N> MB`). At 85% threshold on 1024MB (SSE Lambda per FR-102), the alarm fires at ~870MB utilization, providing ~154MB headroom warning before OOM. For other Lambdas at their current memory allocations, the threshold is proportional. This is critical for the SSE Lambda where ADOT Extension + OTel SDK consume ~80MB (8% of 1024MB) of the allocation, and DynamoDB polling with large response payloads can cause memory spikes during streaming. Source: AWS Lambda Monitoring documentation — `REPORT` log line format includes `Max Memory Used`.

**FR-098 Amendment: Metric Filter Pattern Completeness (Round 12 — Amends FR-098)**

- **FR-105**: FR-098's CloudWatch Logs metric filter MUST match BOTH ADOT export failure log variants: (a) `"Exporting failed. Dropping data."` (permanent failure or context canceled per aws-otel-lambda#886), and (b) `"Exporting failed. No more retries left. Dropping data."` (retries exhausted). The metric filter pattern MUST be: `"Exporting failed" "Dropping data"` (substring match catching both variants). The original FR-098 pattern `"Exporting failed. Dropping data."` misses the retries-exhausted variant. Both variants are at ERROR log level and include structured fields: `error` (the underlying error string), `dropped_items` (count of lost spans), `data_type` ("traces"). These patterns originate from the upstream OTel Collector `exporterhelper` package (`exporter/exporterhelper/internal/retry_sender.go`) and have been stable since OTel Collector v0.67 (2022). Source: `open-telemetry/opentelemetry-collector` source code — `retry_sender.go` lines ~80-110.

**OTel SDK Initialization Error Attribution (Round 12 — New)**

- **FR-106**: OTel SDK initialization code on the SSE Lambda MUST be wrapped in a try/except block that catches initialization failures, logs a structured error with explicit details (failing component name, specific configuration error, corrective action), and then re-raises the exception. This does NOT violate FR-018's fail-fast principle — the error still propagates unhandled to the Lambda runtime, crashing the function. The purpose is error ATTRIBUTION, not error suppression. Without FR-106, a missing `OTEL_SERVICE_NAME` environment variable produces a generic Python ImportError or AttributeError traceback that does not help operators identify the root cause. The structured error log MUST include: (a) component: e.g., `"TracerProvider"`, `"AwsLambdaResourceDetector"`, `"AwsXRayLambdaPropagator"`, (b) error: the specific exception message, (c) action: e.g., `"Set OTEL_SERVICE_NAME environment variable in Terraform"`. Source: OTel Python SDK — initialization error surface is importlib and constructor exceptions.

**Deployment Ordering and Migration Safety (Round 13 — New)**

- **FR-107**: The 17 remediation tasks MUST be deployed in the following phase order: **Phase 1 (Foundation)**: Task 1 (IAM permissions) — benign if deployed before instrumentation, provides permissions needed by subsequent phases; **Phase 2 (Instrumentation)**: Tasks 2, 3, 4, 5, 13, 14 (Lambda tracing instrumentation) — can be deployed in any order within phase; **Phase 3 (Frontend)**: Task 15 (fetch() migration) — depends on Task 5 SSE ADOT for trace header availability in responses; **Phase 4 (Logging Removal)**: Tasks 6, 7, 9 (remove latency logger, cache logger, correlation ID) — ONLY after Phase 2 is verified in production per FR-109's verification gates; **Phase 5 (Monitoring)**: Tasks 8, 10, 16, 17 (annotations, frontend headers, sampling, alarms) — can be deployed alongside Phase 2 or later; **Phase 6 (Validation)**: Tasks 11, 12 (canary, downstream audit) — ONLY after Phase 2 instrumentation is deployed and emitting traces. Deploying canary before instrumentation produces false-positive failures. Deploying logging removal before instrumentation verification creates a monitoring blind spot where neither old nor new observability is operational. Source: AWS X-Ray distributed tracing best practices — instrument upstream services before downstream consumers.

- **FR-108**: All Lambda functions instrumented with ADOT (SSE Lambda initially; others if migrated in future) MUST include `OTEL_SDK_DISABLED=false` as an explicit Terraform environment variable. This provides an emergency kill switch — setting to `true` disables the OTel SDK's `TracerProvider` (returns no-op tracers) while the ADOT Extension continues running (consuming ~50MB but receiving zero spans and causing zero export errors). The kill switch enables operators to disable tracing without container rebuild or redeployment, using only a Terraform variable change + `terraform apply`. The `OTEL_SDK_DISABLED` environment variable is part of the OTel specification and supported since `opentelemetry-python` v1.24.0. When disabled, zero spans are created, zero spans are exported, and zero tracing-related errors or performance overhead occurs. The Lambda continues functioning normally with no observability. Source: OTel Specification — Environment Variable `OTEL_SDK_DISABLED`.

- **FR-109**: Tasks 6 (remove latency_logger), 7 (remove cache_logger), and 9 (remove correlation ID) MUST NOT be deployed until the replacement X-Ray tracing (Tasks 2-5) has been verified in production for a minimum of 2 weeks with ALL of the following verification gates passing continuously: (a) canary (Task 11) reports healthy for 14 consecutive days with zero alarm transitions, (b) X-Ray service map shows all 6 Lambda nodes with correct service names (zero `unknown_service` nodes), (c) CloudWatch `SentimentAnalyzer/Reliability/ADOTExportFailure` metric emits zero events for 14 consecutive days, (d) at least 100 end-to-end traces have been manually spot-checked in X-Ray console confirming complete trace propagation. If any gate fails during the dual-emit period, logging removal is blocked until the failure is resolved and the 2-week verification clock resets. During the dual-emit period, both the old logging system and new X-Ray tracing emit simultaneously — the old system provides safety net observability while the new system is validated. Source: Observability migration best practices — dual-emit with verification gates.

- **FR-110**: The SSE Lambda's ECR repository MUST maintain a tagged pre-ADOT container image (`pre-adot-baseline`) alongside the ADOT-instrumented production image. This image is the current production image WITHOUT ADOT Extension binary, WITHOUT OTel SDK packages, WITHOUT `OPENTELEMETRY_COLLECTOR_CONFIG_FILE` env var, and WITHOUT `OTEL_SDK_DISABLED` env var. The pre-ADOT image enables rapid rollback via Terraform `image_uri` change + `terraform apply` (~2 minutes) without triggering a CI/CD container build pipeline during an active incident. The image MUST be tagged (not digest-only) for human readability, MUST be excluded from ECR lifecycle policies that remove old images, and MUST be retained for at least 90 days after FR-109's verification gates pass and logging removal is deployed. Source: Container deployment rollback best practices — maintain known-good baseline images.

- **FR-111**: All X-Ray Groups created for this feature (error/fault filter per FR-039, any additional filter groups) MUST be configured with `insights_configuration { insights_enabled = true }` in the Terraform `aws_xray_group` resource. Without `insights_enabled`, X-Ray Groups function only as query-time filters — they do NOT emit CloudWatch metrics. SC-013 requires "an X-Ray Group with error/fault filter generates CloudWatch metrics for errored traces," which is impossible without `insights_enabled = true`. Enabling Insights on Groups incurs no additional cost for basic Group-derived CloudWatch metrics (fault count, error count, response time percentiles). AWS imposes a limit of 25 X-Ray Groups per region; this feature requires 1-2 Groups, well within the limit. Source: AWS X-Ray Groups documentation — `InsightsConfiguration` parameter controls CloudWatch metric emission; `aws_xray_group` Terraform resource.

- **FR-112**: The Terraform configuration for the observability canary's EventBridge scheduled rule MUST include an explicit `depends_on` reference to the `aws_lambda_permission` resource that grants EventBridge invoke permission on the canary Lambda function. EventBridge rules become active immediately upon creation and can fire before the Lambda permission resource is created by Terraform, causing the first canary invocation to fail with `AccessDeniedException`. Without `depends_on`, the resource creation order within a single `terraform apply` is non-deterministic. The `depends_on` ensures the permission exists before the rule becomes active. Source: AWS EventBridge documentation — rules are active immediately upon creation; Terraform `depends_on` meta-argument for explicit ordering.

- **FR-113**: The observability canary MUST emit a `canary.trace.completeness_ratio` CloudWatch metric on each invocation that compares the cumulative count of successfully verified test traces against test traces submitted within a configurable lookback window (default: 1 hour, covering 12 canary invocations at 5-minute intervals). The ratio is calculated as `verified_traces / submitted_traces`. A CloudWatch alarm triggers when the ratio drops below 0.95 (95%) over the lookback window. This metric complements the per-trace IsPartial check (FR-087): IsPartial detects individual incomplete traces, while completeness_ratio detects systemic trace loss where entire traces are missing (e.g., ADOT Extension consistently dropping spans due to aws-otel-lambda#886 race, or X-Ray `PutTraceSegments` throttling). The metric uses namespace `SentimentAnalyzer/Reliability` with dimension `Component=Canary`. Source: Observability canary design patterns — aggregate completeness ratio alongside per-invocation point checks.

**OTLP Exporter Timeout Cap (Round 14 — New, Amends FR-093 Prerequisite Chain)**

- **FR-114**: The SSE Lambda's Terraform environment variables MUST include `OTEL_EXPORTER_OTLP_TRACES_TIMEOUT=2` (2 seconds). This caps the OTel OTLP HTTP exporter's per-request timeout when sending spans to the ADOT Extension's localhost OTLP receiver. Without this cap, the exporter uses a 10-second default timeout with up to 6 retries per HTTP POST. If the ADOT Extension crashes or hangs, `force_flush()` blocks for up to 60s per batch. With ceil(1500/64)=24 batches at max queue capacity, total block time can reach ~24 minutes — catastrophically exceeding FR-093's 3000ms pre-deadline window. At 2-second timeout, worst-case per-batch failure is ~12s (2s×6 retries), but localhost Connection Refused detection is immediate, so realistic worst-case is ~15 seconds total. The Lambda is SIGKILL'd at timeout and the Extension's 2000ms Shutdown phase exports whatever was received. Under normal operation (ADOT healthy), localhost round-trip is sub-millisecond — the 2-second cap has zero performance impact. Source: OTel Specification — `OTEL_EXPORTER_OTLP_TRACES_TIMEOUT`; OTel Python SDK issue #4568 (force_flush timeout not enforced); OTLP HTTP exporter `DEFAULT_TIMEOUT`.

**SSE Event-Level Trace Correlation (Round 14 — New)**

- **FR-115**: Each SSE event emitted by the SSE Lambda during streaming MUST include a `trace_id` field in the JSON `data:` payload containing the X-Ray-format trace ID of the OTel span that produced the event (the `sse_event_dispatch` span from FR-001). This enables operators to trace a specific received sentiment event from the frontend all the way back to the DynamoDB poll and metric emission that generated it. The trace ID MUST be in X-Ray format (`1-{hex_timestamp}-{24_hex_digits}`), not OTel hex format, so operators can paste it directly into the X-Ray console. Bandwidth impact: ~50 bytes per event on ~500-byte JSON payloads. The frontend client SHOULD log received `trace_id` values at debug level for client-side debugging. This closes the audit Section 6 finding: "SSE events to client — Trace IDs not included in SSE event payloads." Source: Audit Section 6 — End-to-End Latency Tracing gap analysis.

**ADOT Collector Config CI Validation (Round 14 — New)**

- **FR-116**: The SSE Lambda's ADOT collector configuration file MUST be validated in CI before Docker image build using the ADOT-specific collector binary's `validate` subcommand. The validation MUST use the ADOT distribution binary (NOT `otelcol` or `otelcol-contrib`, which bundle different component sets and report false errors on ADOT-specific components like `awsxray` exporter). CI validation catches: (a) malformed YAML syntax, (b) unknown receiver/processor/exporter types, (c) invalid pipeline wiring. CI step: `docker run --rm -v $(pwd)/collector-config.yaml:/etc/otel-config.yaml public.ecr.aws/aws-observability/aws-otel-collector:<pinned-version> --config /etc/otel-config.yaml validate`. A `yamllint` step provides additional fast-fail on YAML syntax. Without CI validation, a malformed collector config silently crashes the ADOT Extension on deployment — Lambda invocations proceed without telemetry until the canary detects trace absence (~10 minutes). Source: OTel Collector `validate` subcommand; ADOT component registry.

**Dual-Emit Verification Gate Formalization (Round 14 — Amends FR-109)**

- **FR-117**: FR-109's four verification gates MUST be formalized as executable checks: **(a) Trace Structure Equivalence**: For each Lambda, compare traces from old instrumentation (Powertools/X-Ray SDK) with new instrumentation (OTel/ADOT) for the same request type. Verify all expected spans present, error/fault annotations match, and business annotations (FR-006 through FR-009) appear. Comparison is SEMANTIC (same operations and annotations), NOT structural (segment document format differs between X-Ray SDK and ADOT). **(b) Annotation Parity**: Export distinct annotation keys from X-Ray traces before and after migration; "after" set must be a superset of "before." **(c) Service Map Accuracy**: Verify X-Ray service map shows all 6 Lambda nodes with correct `service.name`, all downstream connections (DynamoDB, SNS, SendGrid), and correct edge types. **(d) Automated Trace Spot-Check**: Script queries 100 most recent traces via `GetTraceSummaries`/`BatchGetTraces` and validates: (i) expected segment count per request type, (ii) no unexpected error/fault flags vs pre-migration, (iii) e2e latency p99 not regressed >20%. Script runs daily during the 2-week verification period with results logged to S3. Source: X-Ray API — GetTraceSummaries, BatchGetTraces.

**ReadableStream Error Classification and Server Sentinel (Round 14 — New, Amends FR-033/FR-083)**

- **FR-118**: The frontend SSE client MUST classify ReadableStream errors into 4 categories with distinct reconnection strategies: **(a) Graceful server close** (`done: true` preceded by `event: done` sentinel in buffer): reconnect immediately with `Last-Event-ID`, no backoff, log INFO; **(b) Abnormal termination** (`done: true` WITHOUT sentinel): reconnect with short delay (1-3s), log WARN, track consecutive occurrences; **(c) Network error** (TypeError from `reader.read()`): exponential backoff (1s, 2s, 4s, 8s... capped 30s with jitter), log ERROR; **(d) Intentional cancellation** (AbortError from `AbortController.abort()`): do NOT reconnect. The SSE Lambda MUST emit `event: done\ndata: {}\n\n` before every intentional close (including after FR-100's `event: deadline` — sequence: yield `event: deadline`, yield `event: done`, return). `TimeoutError` from `AbortSignal.timeout()` is treated as category (c). Source: WHATWG Fetch Standard — TypeError; DOM spec — AbortError/TimeoutError; Streams Standard §4.2.3.

**CORS Preflight Cache for Trace Headers (Round 14 — New, Amends FR-014/FR-016)**

- **FR-119**: All API Gateway and Lambda Function URL CORS configurations that include `X-Amzn-Trace-Id` in `Access-Control-Allow-Headers` MUST also set `Access-Control-Max-Age: 7200` (Chrome maximum; Firefox allows 86400). This caches CORS preflight responses, eliminating the 50-200ms OPTIONS request latency on subsequent requests. Without `Access-Control-Max-Age`, browser default is 5 seconds — virtually every API call incurs preflight. Safari caps at 600s (10 minutes) — acceptable, not worth workaround. CloudWatch RUM does NOT inject `X-Amzn-Trace-Id`; the header is added by application code (FR-015). Source: MDN Access-Control-Max-Age; Fetch Living Standard — CORS preflight cache.

**X-Ray Annotation Budget Allocation (Round 14 — New)**

- **FR-120**: OTel span instrumentation on the SSE Lambda MUST allocate annotation counts within the X-Ray 50-annotation-per-segment limit. Budget: `sse_event_dispatch` (FR-006: 5 + FR-007: 5 = 10), `dynamodb_poll` (FR-009: 4), `connection_acquire/release` (FR-008: 3), `force_flush` (2), OTel semantic conventions (~5-10). Maximum per-span: ~25 of 50 limit. RESERVE: 10 annotations for diagnostic use. Non-indexed attributes (large strings, nested objects) MUST use OTel span attributes mapped to X-Ray metadata, NOT annotations. `aws.xray.annotations` attribute convention MUST NOT be used to force all attributes to annotations. Exceeding 50 silently drops excess with SDK WARNING log only. Source: X-Ray Segment Documents — 50 annotations per segment/subsegment.

**treat_missing_data Systematic Classification (Round 14 — New, Amends FR-045/Task 17)**

- **FR-121**: ALL CloudWatch alarms for this feature MUST use `treat_missing_data` per this classification: **(a) Canary/heartbeat** (FR-019, FR-036, FR-113): `"breaching"` — heartbeat absence IS the failure. **(b) Custom error metrics from metric filters** (FR-043, FR-098/FR-105, FR-123): `"breaching"` — emit only on error; silent log group = system dead. **(c) AWS-native Lambda metrics** (Lambda `Errors`, `Duration`): `"notBreaching"` — AWS publishes zero-value points during idle. **(d) Cost/billing** (FR-038): `"missing"` — billing has variable delay. **(e) Latency percentiles**: `"notBreaching"` or `"ignore"` — no traffic = no data is normal. **(f) Memory utilization** (FR-104): `"notBreaching"` — no invocations = no concern. Source: AWS CloudWatch Developer Guide; Well-Architected Reliability Pillar REL06-BP04.

**Rollback Operational Procedure (Round 14 — Amends FR-110)**

- **FR-122**: Rollback from ADOT instrumentation MUST follow this procedure: **(a) Trigger criteria** — ANY of: SSE Lambda cold start p99 > 3000ms for 3 consecutive 5-minute periods, error rate > 5% for 2 consecutive periods, memory > 90% for 3 consecutive periods, or canary completeness < 80% for 2 intervals. **(b) Immediate mitigation** — Set `OTEL_SDK_DISABLED=true` via Terraform apply (~2 minutes). **(c) Full rollback** (if kill switch insufficient) — Update Terraform `image_uri` to `pre-adot-baseline` and apply (~5 minutes). **(d) Post-rollback verification** — SSE Lambda error rate < 0.1%, cold start p99 < 1500ms, memory < 70%. **(e) Timeline** — Kill switch within 5 minutes; full image rollback within 15 minutes. **(f) Reset** — FR-109's dual-emit clock resets after any rollback. Source: Incident management best practices.

**ADOT Collector Startup Failure Metric Filter (Round 14 — New)**

- **FR-123**: The SSE Lambda's CloudWatch Log Group MUST have a metric filter matching ADOT collector startup failure patterns: `"failed to get config"`, `"cannot unmarshal"`, `"failed to build pipelines"`. The filter emits `SentimentAnalyzer/Reliability/ADOTCollectorStartupFailure` metric. This detects deployment-time config errors that crash the Extension on cold start. Without this filter, bad configs silently disable telemetry — canary detects in ~10 minutes, but this filter detects on first invocation with specific attribution. Complements FR-116 (CI validation) as defense-in-depth. Source: OTel Collector error log patterns; ADOT Extension CloudWatch logging.

- **FR-124** (P0 — CRITICAL): Every code path that catches an exception, logs it, but emits NO metric MUST emit a unified `SilentFailure/Count` CloudWatch metric with `FailurePath` dimension identifying the failure source. Five paths require instrumentation: circuit breaker state persistence, audit trail writes, notification delivery, self-healing item fetch, and time-series fanout partial writes. Each emission MUST include a structured log fallback line containing `metric_fallback: true` for CloudWatch Logs metric filter backup. Source: AWS Well-Architected OPS-08 (unified error metric with dimensions).
- **FR-125** (P0 — CRITICAL): A CloudWatch Logs metric filter MUST exist on each Lambda's log group matching `{ $.metric_fallback = true }` to generate a backup `MetricEmissionFailback/Count` metric. This out-of-band path detects put_metric_data failures without depending on put_metric_data itself. The Logs metric filter alarm MUST use `treat_missing_data = "breaching"`. Source: AWS Well-Architected REL-06, re:Invent 2023 OPS-301.
- **FR-126** (P1 — HIGH): The canary heartbeat metric (`EmissionHeartbeat`) MUST be appended to the SAME `put_metric_data` call as production metrics — not a separate API call. If production metric emission fails, the heartbeat fails with it, proving the emission pipeline is broken. The heartbeat alarm MUST use `treat_missing_data = "breaching"` with 3 consecutive evaluation periods of 60 seconds. Source: Amazon Builder's Library "Implementing health checks."
- **FR-127** (P1 — HIGH): Lambda error alarms MUST use percentage-based thresholds via CloudWatch math expressions: `IF(invocations > 10, (errors / invocations) * 100, 0)`. CRITICAL threshold: >5% error rate sustained for 2 periods. WARNING threshold: >1% error rate sustained for 3 periods. The minimum invocation guard (>10) prevents false positives at low traffic. Existing absolute-count alarms (>3/5min, >5/5min) MUST be replaced. Source: AWS Well-Architected OPS-06, AWS Observability Best Practices.
- **FR-128** (P1 — HIGH): Ingestion Lambda MUST have a latency alarm at P95 < 48,000ms (80% of 60s timeout). Notification Lambda MUST have a latency alarm at P95 < 24,000ms (80% of 30s timeout). SSE Streaming Lambda MUST have a P99 connection setup latency alarm < 5,000ms. Analysis Lambda alarm threshold MUST be recalibrated from 25,000ms (42%) to WARNING at 36,000ms (60%) and CRITICAL at 48,000ms (80%). Source: AWS Well-Architected PERF-04, Lambda Operator Guide.
- **FR-129** (P1 — HIGH): The CloudWatch dashboard MUST display ALL alarms organized into three severity-tiered widgets: (a) CRITICAL alarms (packaging failures, circuit breaker, DLQ depth, budget), (b) WARNING alarms (error rates, latency, collision rates), (c) INFORMATIONAL alarms (cost proxies, volume thresholds). A composite alarm aggregating all CRITICAL alarms MUST be created and displayed in the dashboard header. Alarm widgets MUST use alarm name prefix patterns, not explicit ARN lists, so new alarms are automatically included. Source: AWS CloudWatch Dashboard Best Practices, AWS Well-Architected OPS-08.
- **FR-130** (P2 — MEDIUM): SendGrid HTTP call tracing MUST be verified by confirming that `httplib` patching (X-Ray) or `URLLibInstrumentor` (OTel) captures the `python_http_client → urllib.request → http.client` call chain. An integration test MUST verify that a SendGrid API call produces a trace subsegment/span with the destination URL containing `api.sendgrid.com`. If auto-patching produces only generic `http.client` subsegments, a manual subsegment wrapper MUST be added with SendGrid-specific annotations (endpoint, response status). Source: X-Ray SDK supported libraries, python_http_client source.
- **FR-131** (P2 — MEDIUM): All CloudWatch alarms on application-emitted custom metrics MUST use `treat_missing_data = "missing"` (not `notBreaching`) to preserve INSUFFICIENT_DATA state during CloudWatch Metrics API degradation. Only alarms on AWS-native Lambda metrics (which AWS publishes) and cost/budget alarms may use `notBreaching`. Canary/heartbeat alarms MUST use `breaching`. Source: AWS CloudWatch docs "Configuring how alarms treat missing data."
- **FR-132** (P2 — MEDIUM): A validation check MUST exist that parses the HL remediation checklist task table and each fix-*.md work order file, comparing the FR sets assigned to each task. Any FR present in the HL table but absent from the corresponding work order file MUST be flagged as a synchronization gap. This check MUST run as part of the specification validation workflow. Source: IEEE 830 requirements traceability, ISO 29148.
- **FR-133** (P2 — MEDIUM): Collision rate alarm evaluation periods MUST be reduced: high collision alarm from 3 periods (15min) to 2 periods (10min), low collision alarm from 6 periods (30min) to 3 periods (15min). Error rate alarm thresholds on ingestion and analysis Lambdas MUST be converted from absolute count to percentage-based (FR-127). Source: AWS CloudWatch Alarms Best Practices.

**Silent Failure Alarm Configuration (Round 16 — New, Completes FR-124)**

- **FR-134** (P0 — CRITICAL): A CloudWatch alarm MUST exist on the `SilentFailure/Count` metric (FR-124) for EACH of the 7 `FailurePath` dimension values (FR-142). Alarm configuration: threshold > 0 for any single data point, evaluation period 1×1min, `treat_missing_data = "missing"` (per FR-131 — application-emitted metric). Alarm naming pattern: `{env}-silent-failure-{FailurePath}` for automatic inclusion in dashboard severity widgets (FR-129). An AGGREGATE composite alarm across all FailurePath-specific alarms MUST be created and included in the CRITICAL tier of FR-129's dashboard header. Without this FR, FR-124's metric is emitted but unwatched — the primary silent failure detection path has no alarm despite SC-019 expecting one. Source: AWS CloudWatch Alarms Best Practices; Well-Architected OPS-08.

**Frontend Trace Header Propagation Verification Gate (Round 16 — New, Amends FR-015)**

- **FR-135** (P0 — CRITICAL): A deployment Phase 3 (FR-107 — Frontend) blocking gate MUST verify that `X-Amzn-Trace-Id` headers are present on ALL outbound `fetch()` requests from the browser to: (a) API Gateway endpoints (same-origin), (b) Lambda Function URL SSE endpoint (cross-origin, `fetch()+ReadableStream` from FR-033), (c) any other cross-origin API endpoints. The verification test MUST use Playwright request interception (`page.on('request')`) to inspect outbound headers and confirm valid X-Ray trace ID format. CloudWatch RUM's `FetchPlugin` injects the header pre-fetch via argument mutation and does NOT interfere with ReadableStream response processing (confirmed by source inspection). However, injection depends on: (1) `addXRayTraceIdHeader: true` being configured, (2) the target URL passing RUM's `isUrlAllowed()` filter, (3) CORS `Access-Control-Allow-Headers` including `X-Amzn-Trace-Id` on the target server (FR-014/FR-082/FR-119). If ANY of these prerequisites fails, RUM silently drops the header. The gate MUST test with actual browser requests, not unit-level mocks. No silent fallback — if header injection fails, the gate MUST block deployment and require remediation. Source: aws-rum-web SDK `FetchPlugin.ts` source; CloudWatch RUM Developer Guide — HTTP telemetry configuration.

**Canary Out-of-Band Alerting Channel Specification (Round 16 — New, Specifies FR-021/FR-050)**

- **FR-136** (P1 — HIGH): The canary's out-of-band alerting channel (referenced by FR-021, FR-050, SC-021) MUST be a cross-region SNS topic in a DIFFERENT AWS region from the primary deployment. The canary Lambda calls `sns.publish()` directly to this cross-region topic (not via CloudWatch Alarms) when it detects: (a) X-Ray ingestion failure exceeding threshold, (b) CloudWatch metric emission pipeline failure (`EmissionHeartbeat` missing per FR-126), (c) canary self-execution failure. The SNS topic MUST have at least two subscription types: email distribution list AND webhook integration (PagerDuty, OpsGenie, or equivalent). The canary's IAM execution role MUST include `sns:Publish` permission for the cross-region topic ARN. This channel survives primary-region CloudWatch degradation because: SNS publish is a direct API call (no CloudWatch dependency), the topic is in a different region, and email/webhook delivery is independent of CloudWatch. Source: AWS Well-Architected REL-13 (multi-region monitoring); Amazon Builder's Library "Implementing health checks."

**SNS Trace Propagation Verification Gate (Round 16 — New)**

- **FR-137** (P1 — HIGH): A deployment Phase 2 (FR-107 — Instrumentation) blocking gate MUST verify that X-Ray trace context propagates through SNS from the Ingestion Lambda to the Analysis Lambda. The test: (a) trigger an ingestion event that publishes to SNS, (b) capture the Ingestion Lambda's trace ID, (c) wait with retry-and-backoff (30s then 60s, per X-Ray's eventual consistency), (d) query X-Ray `BatchGetTraces` for the trace ID, (e) confirm BOTH Lambda segments AND the SNS connecting segment appear. Critical prerequisite: the SNS subscription MUST NOT use `rawMessageDelivery: true`, which strips the `AWSTraceHeader` system attribute and breaks propagation. The gate MUST also verify `rawMessageDelivery` is `false` on all Ingestion→Analysis SNS subscriptions via `sns:GetSubscriptionAttributes`. This verification exists because 9 prior assumptions in this spec were invalidated — automatic SNS propagation is too critical to leave unverified. Source: AWS X-Ray Developer Guide — "Tracing SNS event notifications"; SNS Developer Guide — System attributes and raw message delivery.

**API Gateway and Function URL Latency Alarms (Round 16 — New)**

- **FR-138** (P1 — HIGH): API Gateway-fronted Lambda functions (Dashboard, Ingestion, Analysis, Notification) MUST have CloudWatch alarms on API Gateway's `IntegrationLatency` metric at P99 < 5,000ms and on `5XXError` rate at >1% sustained for 2 periods. The SSE Streaming Lambda (Function URL, not API Gateway) MUST have equivalent alarms on Lambda service metrics `Url5xxError`. API Gateway alarms capture the full request lifecycle including authorization, WAF evaluation, and integration latency that Lambda-side duration alarms (FR-128) cannot measure. The Metrics Lambda is EventBridge-triggered and has no HTTP interface — it is covered by Lambda-native metrics only. All alarms use `treat_missing_data = "notBreaching"` per FR-121 classification (AWS-native metrics). Source: AWS API Gateway CloudWatch Metrics documentation; Lambda Function URL metrics; Well-Architected PERF-04.

**force_flush() Hard Timeout Wrapper (Round 16 — New, Amends FR-093)**

- **FR-139** (P1 — HIGH): The SSE Lambda's `force_flush()` call (FR-055, FR-093) MUST be wrapped in a thread-based hard timeout of 2,500ms, leaving 500ms from FR-093's 3,000ms deadline window for generator termination (`event: deadline` + `event: done` + return). The wrapper spawns a daemon thread for `force_flush()`, calls `thread.join(timeout=2.5)`, and proceeds regardless of completion. This bounds total flush duration even when the ADOT Extension is hung (accepting TCP connections but not responding to OTLP requests). Without this wrapper, `BatchSpanProcessor.force_flush()` exports 22 batches sequentially (FR-073 `max_queue_size=1350` ÷ `max_export_batch_size=64`), each blocking up to FR-114's 2s per-request OTLP timeout = 44s total — far exceeding FR-093's 3,000ms budget and causing Lambda timeout instead of graceful `event: deadline` termination. SC-064 MUST be amended to also test the hung-Extension scenario (TCP accept, no response), not just port-unreachable. Spans in unflushed batches are LOST (acceptable — fail fast, NO FALLBACKS). Source: OTel Python SDK `force_flush()` timeout non-enforcement — opentelemetry-python#4568 (open); `BatchSpanProcessor` sequential export implementation; Python `threading.Thread.join(timeout)` documentation.

**ADOT Extension Mid-Stream Crash Detection (Round 16 — New)**

- **FR-140** (P1 — HIGH): The SSE Lambda MUST emit a `SentimentAnalyzer/Reliability/ADOTExtensionUnavailable` CloudWatch metric when the OTLP exporter fails to deliver spans to `localhost:4318` during streaming. Detection: a CloudWatch Logs metric filter on the SSE Lambda log group matching the OTel SDK's exporter WARNING log patterns for connection failure (`"Failed to export"`, `"Connection refused"`, `"Connection reset"`). This provides per-invocation detection within the CloudWatch Logs pipeline latency (~seconds), complementing FR-123's startup failure detection and the canary's aggregate detection (~10 minutes, 2× FR-048 interval). The metric alarm MUST use `treat_missing_data = "missing"` (normal operation produces no metric). When this alarm fires simultaneously with FR-123's startup alarm, it indicates a persistent ADOT configuration problem; when it fires alone, it indicates a mid-stream Extension crash (OOM, collector pipeline failure). Source: OTel Specification — Exporters MUST NOT throw exceptions; ADOT Lambda Extension lifecycle documentation.

**SSE Reconnection Trace Chain Group (Round 16 — New)**

- **FR-141** (P2 — MEDIUM): An X-Ray Group named `sse-reconnections` MUST be created with filter expression `annotation.previous_trace_id BEGINSWITH "1-"` to capture all SSE reconnection events, with `insights_enabled = true` (FR-061 pattern) to generate CloudWatch metrics for system-wide reconnection frequency. An alarm on reconnection rate > 10/minute indicates SSE Lambda instability. The operator workflow for session-level debugging: (1) find initial trace by user/time, (2) extract `session_id` from annotation (FR-008), (3) filter X-Ray traces by `annotation.session_id = "{value}"`, (4) sort results by timestamp to see the full reconnection chain with `previous_trace_id` links. Each reconnection creates a NEW trace ID (new Lambda invocation = new X-Ray facade segment); the `session_id` annotation is the join key across traces. The `previous_trace_id` annotation MUST be set on ALL SSE connections — on first connection, the value is `"none"` (not omitted) to ensure the annotation exists for all traces. Source: AWS X-Ray Groups documentation; X-Ray Filter Expression reference.

**FR-124 Path Enumeration Alignment (Round 16 — Amends FR-124)**

- **FR-142** (P2 — MEDIUM): FR-124's `FailurePath` dimension MUST enumerate exactly 7 distinct values matching SC-004's 7 silent failure paths: `circuit_breaker_load`, `circuit_breaker_save`, `audit_trail`, `notification_delivery`, `self_healing_fetch`, `fanout_partial_write`, `parallel_fetcher_aggregate`. FR-124's current text lists "five paths" with "circuit breaker state persistence" (ambiguous — could be load or save or both) and omits parallel fetcher error aggregation entirely (audit Section 8.7, SC-004 item 7). This amendment ensures FR-124 satisfies SC-004 without interpretation ambiguity. Source: SC-004 (7 paths enumerated); audit Section 8.7.

**Metric Fallback Log Line Format Specification (Round 16 — Amends FR-124)**

- **FR-143** (P2 — MEDIUM): The structured log fallback line required by FR-124 MUST be a single JSON-formatted log line matching the exact structure: `{"metric_fallback": true, "failure_path": "<path>", "metric_name": "SilentFailure/Count", "exception_type": "<type>", "exception_message": "<truncated_to_256_chars>", "timestamp": "<ISO8601>"}`. This line MUST be emitted via a dedicated JSON-emitting call (e.g., `json.dumps()` to stdout or `logging.getLogger().info(json.dumps(...))`), NOT the Lambda's default logging format (which may be plaintext). FR-125's CloudWatch Logs metric filter pattern `{ $.metric_fallback = true }` requires JSON-structured log events — plaintext lines containing the literal string "metric_fallback: true" do NOT match JSON metric filter syntax. Each Lambda MAY use any logging format for all other log lines; only the metric_fallback line requires JSON. Source: CloudWatch Logs Metric Filter pattern syntax — JSON patterns require JSON-formatted log events.

**OTel Manual Span Error Status Requirement (Round 17 — New)**

- **FR-144** (P0 — CRITICAL): ALL manually-created OTel spans on the SSE Lambda MUST call BOTH `span.set_status(StatusCode.ERROR, description)` AND `span.record_exception(exception)` when an exception occurs within the span's scope. The ADOT X-Ray exporter's `makeCause()` function is GATED on `span.Status().Code() == StatusCodeError` — without `set_status(StatusCode.ERROR)`, the X-Ray segment document has `fault: false` and `cause: null`, regardless of whether `record_exception()` was called. This means: (a) X-Ray Group filter expressions `fault = true OR error = true` (FR-034) MISS the error, (b) X-Ray console error/fault filtering (SC-002) MISSES the error, (c) CloudWatch metrics generated from X-Ray Groups do NOT count the error, (d) the operator sees a "healthy" trace for a failed operation. Exception: HTTP-attributed spans (with `http.status_code` >= 400) are mapped to X-Ray error/fault via separate attribute-based logic independent of StatusCode — but manual internal/DynamoDB spans have no HTTP status code fallback. For non-streaming Lambdas, Powertools Tracer `@tracer.capture_method` automatically calls the equivalent of `set_status(ERROR)` on exception (FR-029 handles this). For the SSE Lambda's ~675 manual spans per invocation, EVERY error path MUST explicitly set both. This is the most fundamental requirement for X-Ray error visibility on the SSE Lambda. Source: ADOT X-Ray Exporter `cause.go` — `makeCause()` gated on `StatusCodeError`; OTel Specification — Span API SetStatus; `segment.go` — `fault` flag mapping.

**Canary FR-018 Fail-Fast Exemption (Round 17 — New)**

- **FR-145** (P1 — HIGH): The canary Lambda (Task 11) MUST catch and handle X-Ray SDK, OTel SDK, and ADOT Extension initialization errors WITHOUT crashing. This is the SOLE FORMALIZED exemption from FR-018's fail-fast mandate. Rationale: the canary's purpose is to detect and report X-Ray outages — if the canary itself crashes on X-Ray initialization failure, the entire meta-observability architecture (FR-125, FR-126, FR-136) becomes inoperative. The system loses its ability to detect X-Ray degradation, which is the single most critical gap this spec exists to prevent. The canary MUST: (a) attempt normal X-Ray/OTel initialization, (b) on initialization failure, log a structured error with `{"canary_init_failed": true, "component": "<failed_component>", "error": "<message>"}`, (c) continue executing its CloudWatch health check and SNS alerting functions, (d) report its own initialization failure as a critical finding alongside any X-Ray health findings, (e) emit a `CanaryInitFailure` CloudWatch metric for alarming. The canary's OWN traces are best-effort — visible when X-Ray is healthy, lost when X-Ray is down. Its critical alerting path (FR-136 cross-region SNS) MUST NOT depend on X-Ray. Source: AWS Well-Architected Operational Excellence Pillar — "Monitor the health of your monitoring systems."

**Canary Lambda Instrumentation Scope (Round 17 — New)**

- **FR-146** (P2 — MEDIUM): The canary Lambda (new function #7, not in FR-001's 6-Lambda scope) MUST be instrumented with the same approach as other non-streaming Lambdas (Powertools Tracer per FR-029 pattern). However, all instrumentation initialization MUST be wrapped per FR-145's exemption. FR-001's scope text ("all 6 Lambda functions") is NOT amended — the canary is architecturally distinct as a meta-observability component. The canary's instrumentation serves a different purpose than production Lambda instrumentation: it provides operator visibility into canary execution (useful for debugging canary failures) rather than end-user request tracing. When X-Ray is healthy, canary traces are visible alongside production traces. When X-Ray is down, canary traces are lost but canary alerting continues via FR-136.

**FR-124 Silent Failure Path Enumeration Correction (Round 18 — Amendment)**

- **FR-147** (P0 — CRITICAL): FR-124's inline text MUST be amended from "five distinct silent failure paths" to "seven distinct silent failure paths" to match the authoritative enumeration in FR-142 and SC-092. The seven paths are: (1) `circuit_breaker_load`, (2) `circuit_breaker_save`, (3) `audit_trail`, (4) `notification_delivery`, (5) `self_healing_fetch`, (6) `fanout_partial_write`, (7) `parallel_fetcher_aggregate`. FR-124's original text bundled circuit breaker into one path and omitted parallel_fetcher_aggregate. FR-134 creates 7 alarms referencing these 7 FailurePath dimension values — an implementer following FR-124's unamended text creates 5 metrics, leaving 2 alarm ARNs referencing non-existent metrics (permanent INSUFFICIENT_DATA state on `circuit_breaker_save` and `parallel_fetcher_aggregate` alarms). This is a documentation-only amendment; no behavioral change. Source: FR-142 enumeration; SC-092 dimension list; FR-134 alarm definitions.

**Frontend RUM Readiness Gate (Round 18 — New)**

- **FR-148** (P0 — CRITICAL): The frontend application MUST NOT issue its first `fetch()` call to any instrumented backend endpoint (API Gateway or Lambda Function URL) until CloudWatch RUM's FetchPlugin has completed patching `window.fetch`. The application MUST implement a readiness check that confirms `window.fetch` has been wrapped by the FetchPlugin before initiating any backend communication, including the SSE streaming connection. If RUM initialization has not completed within 5 seconds of page load, the application MUST proceed without the readiness gate and log a structured warning (`{"rum_init_timeout": true, "delay_ms": 5000}`) — this is NOT a fallback (the application does not retry RUM initialization or use alternative tracing), it is a bounded wait to avoid blocking the user experience indefinitely. The 5-second bound is necessary because RUM CDN delivery can fail entirely (CDN outage, ad blocker, CSP policy), and the application must remain functional without RUM — trace headers will be absent but the application works. Detection mechanism: FR-135's Playwright deployment gate verifies the readiness gate exists. FR-085's Playwright SSE test verifies the SSE connection carries the `X-Amzn-Trace-Id` header. Implementation note: The RUM web client SDK does not expose a native readiness event — the readiness check may need to poll for the presence of the `cwr` (CloudWatch RUM) global object or check `window.fetch.toString()` for evidence of shimmer wrapping. Source: aws-rum-web SDK `FetchPlugin.ts` — synchronous monkey-patching via shimmer; CDN snippet loading semantics.

**Post-Proactive-Flush Span Creation Prohibition (Round 18 — Amendment)**

- **FR-149** (P1 — HIGH): After FR-093's proactive flush fires (triggered at `get_remaining_time_in_millis() <= 3000`), the SSE streaming generator MUST NOT create any new OTel spans. All span-creating operations (poll cycle spans, DynamoDB call spans, event formatting spans) MUST be guarded by a `flush_fired` flag that is set atomically when the proactive flush begins. If an exception occurs after flush (e.g., during generator cleanup), the exception MUST be logged via structured JSON (same format as FR-143) but NOT recorded as an OTel span. The generator's final operations after flush MUST be limited to: (a) yielding `event: deadline\ndata: {"reason": "lambda_deadline"}\n\n` per FR-100, (b) returning from the generator function. This amendment closes the window between proactive flush and generator termination where orphaned spans could be created with no export path. Source: OTel Python SDK BatchSpanProcessor — no mechanism for "emergency re-flush" after queue re-population.

**OTel Error Handling Context Manager Pattern (Round 18 — Amendment)**

- **FR-150** (P1 — HIGH): FR-144's dual-call requirement (record_exception + set_status) MUST be implemented via OTel's `start_as_current_span()` context manager with `record_exception=True, set_status_on_exception=True` (the defaults since opentelemetry-python v0.16b0) for ALL spans where exceptions PROPAGATE out of the `with` block. For spans containing try/except blocks that CATCH exceptions without re-raising (i.e., the 7 silent failure paths per FR-142), the caught exception MUST be manually handled with BOTH `span.record_exception(exception)` AND `span.set_status(StatusCode.ERROR, str(exception))` INSIDE the except block BEFORE any other error handling (metric emission, logging, etc.). The context manager's automatic handling does NOT fire for caught exceptions — the `use_span()` internal implementation only calls `record_exception()` and `set_status(ERROR)` in its own except block, which is bypassed when the inner code catches the exception. This distinction is the root cause of FR-144's ambiguity: automatic handling is correct for unexpected errors, but the SSE Lambda's 7 silent failure paths are ALL caught exceptions by design (they must not crash the generator). Every silent failure path MUST have explicit dual-call as the FIRST two lines of its except block. Source: OTel Python SDK `opentelemetry/trace/__init__.py` — `use_span()` implementation; Python language reference — exception propagation in nested try/except.

**Canary Warm-Invocation TracerProvider Re-Initialization (Round 18 — Amendment)**

- **FR-151** (P1 — HIGH): The canary Lambda's handler function MUST check whether TracerProvider is in a valid state on EVERY invocation, not just cold starts. If module-level TracerProvider initialization failed (detected via FR-145's error handling), the handler MUST attempt re-initialization on each subsequent warm invocation. The re-initialization attempt MUST be: (a) guarded by a module-level flag (`_tracer_init_failed = True`), (b) bounded by a 2-second timeout to prevent blocking the canary's primary alerting function, (c) logged as `{"canary_reinit_attempt": true, "success": <bool>}` regardless of outcome. If re-initialization succeeds, the flag is cleared and subsequent invocations use normal instrumentation. If re-initialization fails again, the handler continues with FR-145's degraded mode. This prevents a transient ADOT Extension hiccup during one cold start from permanently disabling canary tracing for the execution environment's lifetime (potentially hours). Source: Python language semantics — module-level code executes once per process; Lambda execution model — warm invocations reuse the same process.

**FR-109/FR-117 Gate Precedence Clarification (Round 18 — Amendment)**

- **FR-152** (P2 — MEDIUM): FR-109's verification gate (d) ("100 traces manually spot-checked for expected annotations") is SUPERSEDED by FR-117's automated trace validation script for deployment gate purposes. FR-109 gate (d) is reclassified as a one-time acceptance test during initial implementation, NOT a recurring deployment gate. FR-117's automated script is the authoritative recurring gate, executing on every deployment per FR-107 Phase 5. The automated script MUST define expected segment counts per request type: (a) Dashboard Lambda: 1 Lambda segment + >=3 DynamoDB subsegments + >=1 httplib subsegment (SendGrid path only), (b) Ingestion Lambda: 1 Lambda segment + >=1 DynamoDB subsegment + >=1 SNS subsegment, (c) Analysis Lambda: 1 Lambda segment + >=2 DynamoDB subsegments, (d) Notification Lambda: 1 Lambda segment + >=1 httplib subsegment, (e) SSE Streaming Lambda: 1 Lambda segment + >=5 subsegments (poll cycles vary), (f) Metrics Lambda: 1 Lambda segment + >=2 DynamoDB subsegments + >=1 CloudWatch subsegment. The expected counts are MINIMUMS — actual counts may be higher depending on code paths exercised. Source: Application architecture — Lambda function responsibilities and downstream dependencies.

**FR-018 Explicit Scope Statement (Round 18 — Amendment)**

- **FR-153** (P2 — MEDIUM): FR-018's fail-fast mandate MUST include an explicit scope statement: "FR-018 applies to ALL Lambda functions in the system EXCEPT those explicitly exempted by a numbered FR. As of Round 18, the sole exemption is FR-145 (canary Lambda). Any future Lambda function added to the system MUST default to FR-018 fail-fast behavior. Exemptions require a new FR with documented rationale for why the Lambda must survive X-Ray initialization failure." This prevents scope ambiguity when new Lambda functions are added to the architecture — without this statement, an implementer could argue that FR-018 applies only to the original 6 Lambdas in FR-001, leaving new functions uninstrumented. Source: Spec architecture — FR-001 scope vs FR-145 exemption; defensive specification practice.

**FR-132 Automated Staleness Remediation Trigger (Round 18 — Amendment)**

- **FR-154** (P2 — MEDIUM): FR-132's automated staleness detection MUST trigger a concrete remediation action, not merely log a warning. When the automated audit detects that the HL remediation checklist is stale (missing rounds, missing SCs, missing risk entries), the system MUST: (a) create a GitHub issue titled "HL Document Staleness: Missing Rounds {N}-{M}" with the specific gaps enumerated, (b) label the issue `doc-staleness` and `priority:high`, (c) block the next `/speckit.specify` round from proceeding until the issue is resolved or explicitly deferred with documented rationale. The current state of the HL document demonstrates that detection-only staleness tracking is insufficient — FR-132 exists in Round 15, yet the HL document is 2 rounds stale by Round 18 with 13 missing risk entries and 39 missing SCs. Detection without enforcement is equivalent to no detection. Source: Observed failure — FR-132 detection mechanism failed to prevent staleness accumulation over Rounds 16-18.

**Trace Data PII Prohibition (Round 18 — New)**

- **FR-155** (P2 — MEDIUM): X-Ray span annotations and metadata MUST NOT contain personally identifiable information (PII) including but not limited to: email addresses, user names, IP addresses, sentiment analysis input text, API keys, or authentication tokens. OTel span attributes that are mapped to X-Ray annotations (indexed, queryable) or metadata (non-indexed but stored) MUST be limited to: trace correlation identifiers (trace_id, session_id, request_id), operational metrics (latency, cache_hit_rate, queue_depth), categorical classifiers (event_type, failure_path, span_kind), and infrastructure identifiers (function_name, region). If a span attribute might contain user-generated content (e.g., error messages that include user input), the value MUST be truncated to a classification label (e.g., "ValueError" instead of "ValueError: invalid email user@example.com"). X-Ray trace data retention is 30 days (non-configurable). This FR does not affect CloudWatch Logs (which have separate data handling) or the application's functional data stores. Source: AWS Shared Responsibility Model — customer responsibility for data classification in trace stores; GDPR Article 5(1)(c) — data minimization principle.

**BSP Deadlock Diagnostic Differentiation (Round 20 — New)**

- **FR-156** (P0 — CRITICAL): The SSE Lambda's `force_flush()` call path MUST be resilient to `BatchSpanProcessor` internal deadlock (opentelemetry-python#3886) AND MUST differentiate the failure cause diagnostically. FR-139's thread-based hard timeout wrapper handles both ADOT Extension hang (external — TCP accept, no OTLP response) and BSP deadlock (internal — worker thread holds `self.condition` lock indefinitely) identically: the flush call is terminated after 2500ms. However, the SPAN FATE differs: in Extension hang, spans were dequeued from BSP and held by the exporter (lost in exporter memory); in BSP deadlock, spans remain in the BSP deque with the lock unreleased (cannot be exported, retried, or recovered). The SSE Lambda MUST emit a structured log after FR-139 timeout that distinguishes the two cases: (a) if the OTLP TCP connection to localhost:4318 was established before timeout, log `{"flush_timeout_cause": "bsp_or_extension_hang", "otlp_connection": "established"}`; (b) if the OTLP TCP connection to localhost:4318 failed with ECONNREFUSED, log `{"flush_timeout_cause": "extension_unavailable", "otlp_connection": "refused"}`. This diagnostic distinction enables operators to differentiate Extension issues (solvable via restart/redeploy) from BSP bugs (solvable via SDK version upgrade or configuration change). This is the THIRD distinct span-loss vector in the spec: (1) Extension hang → FR-139 thread wrapper; (2) Extension drop → FR-074 accepted ~30% loss; (3) BSP deadlock → FR-156 diagnostic differentiation. Source: opentelemetry-python#3886 — `BatchSpanProcessor` deadlock bug; confirmed in Python 3.11+.

**FR-154 Retroactive Staleness Enforcement (Round 20 — Gate)**

- **FR-157** (P0 — CRITICAL): FR-154's staleness enforcement gate MUST be retroactively applied as a Round 20 acceptance criterion. The HL remediation checklist (`docs/x-ray/HL-x-ray-remediation-checklist.md`) is 1 full round stale: (a) Progress Log ends at Round 17 — missing Round 18; (b) cumulative totals show 146 FRs / 96 SCs — actual is 155 FRs / 105 SCs (9 FRs and 9 SCs from Round 18 missing); (c) Work Order table task-to-FR mappings do not include Round 18 FRs (FR-147 through FR-155); (d) Risk Assessment table does not include Round 18 risk entries. Per FR-154(c), the next specification round cannot proceed until the staleness issue is resolved or explicitly deferred. This FR documents the explicit deferral with rationale: HL synchronization is deferred to a dedicated synchronization task (not a specification round) because (i) Round 20's primary purpose is blind spot discovery; (ii) HL synchronization requires updating 5 sections with 9 FRs, 9 SCs, 4 edge cases, 3 assumptions, and 1 Progress Log entry — mechanical work that should not consume specification analysis budget; (iii) a GitHub issue titled "HL Document Staleness: Missing Round 18" MUST be created as part of Round 20 acceptance. The deferral is time-bounded: HL synchronization MUST be completed before implementation begins on any task (FR-107 Phase 0 prerequisite). Source: Observed failure — FR-132 (Round 15) detection and FR-154 (Round 18) enforcement both exist but the HL document they govern is stale through Round 20, proving specification-level enforcement without external tracking (GitHub issue) is insufficient.

**ADOT Cold Start Latency Budget (Round 20 — New)**

- **FR-158** (P1 — HIGH): The SSE Lambda MUST have a measured and documented cold start latency budget that accounts for ADOT Extension overhead. AWS publishes NO official benchmarks for ADOT Lambda Extension cold start impact (confirmed: opentelemetry-lambda#263 states "we currently do not publish expected startup times"). Community reports range from 200ms to 2000ms+ depending on memory allocation and runtime configuration. FR-102 increased SSE Lambda memory to 1024MB based on memory calculations but established no latency constraint. The cold start latency budget MUST be: (a) measured in preprod with the ADOT-embedded Docker image at 1024MB memory; (b) documented as a P95 threshold of < 2000ms for cold start (handler invocation latency, excluding Extension INIT which runs concurrently per Lambda lifecycle); (c) verified as a Phase 1 deployment gate in FR-107's ordering; (d) re-measured when ADOT version, OTel SDK version, or Lambda memory allocation changes. If measured cold start exceeds 2000ms, memory allocation MUST be increased until the budget is met, or an architectural review determines an alternative approach (e.g., provisioned concurrency). This FR does NOT mandate provisioned concurrency as a default — it mandates replacing assumption with measurement. Source: AWS Lambda Performance documentation — cold start latency components; opentelemetry-lambda#263 — ADOT cold start discussions; aws-otel-lambda#228 — community cold start measurements ranging 200ms-4000ms+.

**ADOT Extension IAM Permission Enumeration (Round 20 — New)**

- **FR-159** (P1 — HIGH): Lambda execution roles for ADOT-instrumented Lambdas (SSE Lambda and canary Lambda) MUST include ALL IAM actions required by the ADOT Extension: (a) `xray:PutTraceSegments` — trace data submission; (b) `xray:PutTelemetryRecords` — Extension health telemetry; (c) `xray:GetSamplingRules` — centralized sampling rule retrieval; (d) `xray:GetSamplingTargets` — per-rule sampling target allocation; (e) `xray:GetSamplingStatisticSummaries` — sampling statistics reporting. FR-017 requires "X-Ray IAM permissions" but does not enumerate ADOT-specific actions. The existing `AWSXRayDaemonWriteAccess` managed policy covers ONLY actions (a) and (b). Missing `GetSampling*` actions (c-e) cause the ADOT Extension's `awsproxy` to fail silently, causing the OTel sampler to fall back to `TraceIDRatioBased` at 1 trace/sec — a catastrophic degradation from 100% sampling to ~0.001% with NO error visible to the Lambda function and NO CloudWatch alarm. The IAM policy MUST either use a custom policy statement with all 5 actions or combine `AWSXRayDaemonWriteAccess` with `AWSXrayReadOnlyAccess`. Source: ADOT permissions documentation — aws-otel.github.io/docs/setup/permissions/; AWS X-Ray identity-based policy examples.

**RESPONSE_STREAM + ADOT Extension Lifecycle Verification (Round 20 — New)**

- **FR-160** (P1 — HIGH): The Lambda RESPONSE_STREAM + ADOT Extension lifecycle interaction MUST be verified in preprod before production deployment. The spec's foundational assumption — that the ADOT Extension remains alive and accepts OTLP spans during the entire SSE streaming phase — has NO canonical documentation from AWS. The AWS Lambda Extensions API states "After receiving the function response from the runtime, Lambda returns the response to the client, even if extensions are still running" — written for buffered responses where "function response" is a single event. For streaming, "function response" is continuous. If the Extension interprets response header send as invoke-phase completion and begins pre-Shutdown cleanup, streaming-phase spans may be dropped or lost. The verification gate MUST: (a) deploy the ADOT-instrumented SSE Lambda in preprod with a 15-second streaming duration; (b) create OTel spans at 1-second intervals throughout the streaming phase; (c) query X-Ray for ALL spans including the final span created at second 14; (d) confirm ≥95% of streaming-phase spans appear in X-Ray (allowing for known ~30% edge-case drop from aws-otel-lambda#886, but 95% aggregate across many invocations); (e) this gate blocks FR-107 Phase 2 progression. This is the spec's ONLY foundational architectural assumption that lacks a canonical source. Source: AWS Lambda Extensions API — Extensions lifecycle phases; AWS Lambda Response Streaming documentation — no mention of Extension interaction; absence of documentation is the finding.

**Function URL Sampling Cost Guard (Round 20 — New)**

- **FR-161** (P1 — HIGH): Lambda Function URL invocations MUST have a sampling cost guard that prevents unbounded X-Ray spend in production. With `parentbased_always_on` sampler (FR-056), Function URL invocations have no parent context (bypass API Gateway entirely), making every invocation a root span sampled at 100%. At $5/million traces, 10 million Function URL invocations/day costs $50 — exceeding FR-038's monthly cost guard in a single day. The cost guard MUST include: (a) an X-Ray centralized sampling rule with `ServiceName = "{sse_lambda_function_name}"` and `URLPath = "*"` that limits Function URL sampling to a configurable rate (100% in dev/preprod per FR-034, configurable for production); (b) FR-038's billing alarms MUST include a per-day anomaly detection alarm (not just monthly thresholds) that fires when daily X-Ray cost exceeds 200% of the trailing 7-day average; (c) the production sampling rate MUST be adjustable via the X-Ray console without code deployment. Note: this FR does NOT change the sampler from `parentbased_always_on` to `xray` — the `parentbased_always_on` sampler correctly honors parent decisions for API Gateway-fronted Lambdas. The centralized sampling rule provides the cost control for parentless (Function URL) traffic while preserving 100% sampling for API Gateway traffic that inherits parent decisions. Source: AWS X-Ray pricing — $5.00 per million traces recorded; X-Ray centralized sampling rules; OTel `parentbased_always_on` specification — delegates to root sampler when no parent context exists.

**SC-064 Split — Distinct Failure Mode Verification (Round 20 — Amendment)**

- **FR-156a** (P2 — MEDIUM, informational): SC-064 (Round 14, amended Round 17) is SUPERSEDED by SC-064a and SC-064b in Round 20. The original SC tested two distinct failure modes (port-unreachable and hung Extension) in a single success criterion. These failure modes exercise different code paths: port-unreachable triggers ECONNREFUSED (fast, no thread wrapper), hung Extension requires FR-139's thread wrapper (slow, 2500ms timeout). A single SC testing two code paths produces ambiguous pass/fail — if one mode passes and the other fails, the SC status is undefined. The split ensures each failure mode is independently verifiable.

**Exhaustive treat_missing_data Classification (Round 20 — New)**

- **FR-162** (P2 — MEDIUM): All CloudWatch alarms MUST have an explicitly documented `treat_missing_data` classification. FR-121 (Round 14) classifies by alarm type and FR-131 (Round 15) mandates `missing` for application metric alarms, but neither provides the exhaustive per-alarm mapping needed by implementers. The exhaustive classification is: (a) `treat_missing_data = "breaching"`: canary heartbeat alarms (FR-126 — absence = system down), `no_new_items` alarm (absence = pipeline broken); (b) `treat_missing_data = "missing"`: all application-emitted custom metric alarms (`StuckItems`, `ConnectionAcquireFailures`, `EventLatencyMs`, `SilentFailure/Count` per FR-124, all FR-043 reliability metrics, `MetricsLambdaErrors`, `HighLatencyAlert`, `PollDurationMs`, `AnalysisErrors`), all Lambda error rate alarms (FR-127), all Lambda latency alarms (FR-128), API Gateway `IntegrationLatency` alarms (FR-138), all memory utilization alarms (FR-104); (c) `treat_missing_data = "notBreaching"`: AWS-native cost/billing alarms (FR-038), X-Ray billing alarms (SC-016). Zero alarms use `"ignore"`. Zero alarms rely on the CloudWatch provider default (which varies between console and API creation). Source: CloudWatch alarm documentation — treat_missing_data semantics; AWS Well-Architected REL-06 — alarm configuration best practices.

**X-Ray Trace Archival for Production Incidents (Round 20 — New)**

- **FR-163** (P2 — MEDIUM): Production X-Ray trace data MUST be archivable to S3 before the 30-day retention window expires for any production incident that triggers a post-mortem or escalation. FR-155 documents the 30-day retention as non-configurable. The archival mechanism MUST: (a) be triggered manually by an operator as part of the incident response procedure (FR-122 rollback process); (b) use the X-Ray `GetTraceSummaries` and `BatchGetTraces` APIs to export trace documents for the incident time window; (c) store exported traces in S3 with a 1-year retention policy matching the organization's incident record retention; (d) be documented in the operational runbook alongside FR-122's rollback procedure with rate-limit awareness (`BatchGetTraces` is limited to 5 traces/request at 5 requests/sec — 100,000 traces takes ~67 minutes). This FR does NOT mandate continuous or automated archival (cost prohibitive at scale) — it mandates a documented manual procedure that operators CAN execute when needed for incidents. Source: AWS X-Ray API Reference — GetTraceSummaries, BatchGetTraces rate limits; AWS X-Ray quotas — 30-day trace data retention (non-configurable).

**Work Order File Synchronization Enforcement (Round 20 — New)**

- **FR-164** (P2 — MEDIUM): Work order files (`docs/x-ray/fix-*.md`) MUST enumerate ALL functional requirements mapped to their respective tasks in the HL document's Work Order table. FR-132 (Round 15) detects work order staleness but does not enforce resolution. As of Round 20, fix-*.md files contain only Round 1-6 FRs — 100+ FRs from Rounds 7-20 are mapped in the HL document but absent from individual implementation guides. An implementer using `fix-sse-subsegments.md` (Task 5) as their task guide would see only the original FRs and miss 50+ requirements added in Rounds 7-18. The synchronization MUST: (a) be completed before implementation begins on any task (FR-107 Phase 0 prerequisite, jointly with FR-157); (b) be validated by FR-132's automated check with zero gaps; (c) include FR cross-references with round annotations (e.g., "FR-100 (Round 12)") to provide implementers with requirement provenance. Source: Observed failure — work order staleness has persisted since Round 7 despite FR-132 detection existing since Round 15.

---

### Round 21 Emergent Issues Identified

**13 emergent issues identified (2 CRITICAL, 5 HIGH, 6 MEDIUM)**

1. **CRITICAL**: FR-156's third span-loss vector (BSP deadlock, opentelemetry-python#3886) is FIXED in SDK v1.33.0+ — project uses v1.39.1 which has the rewritten `BatchProcessor` using `collections.deque` + `threading.Event` instead of the old `threading.Condition` with nested lock acquisition. The "three catalogued span-loss vectors" reduces to TWO. FR-156's diagnostic logging remains valuable but the risk profile changes fundamentally.
2. **CRITICAL**: SC-040's verification method ("confirming zero 'Queue is full' log entries") is INVALID on SDK v1.39.1 — the new `BatchProcessor` uses `collections.deque([], max_queue_size)` where `appendleft()` silently drops the oldest span with NO log, NO callback, NO metric. This is by OTel design (issue #4261 — logging in `emit()` causes infinite recursion via logging instrumentation). An operator cannot detect BSP queue overflow through logs alone.
3. **HIGH**: ADOT Extension does NOT restart after crash within the same execution environment — confirmed by AWS Lambda Extensions API documentation. The TracerProvider module-level singleton retains a stale OTLP exporter pointing to localhost:4318, receiving ECONNREFUSED on every export attempt. The OTLP HTTP exporter auto-reconnects when the target becomes reachable (no TracerProvider recreation needed), but since the Extension NEVER restarts, recovery never occurs for the sandbox's lifetime (~5-15 minutes). This is a FOURTH span-loss vector (not previously catalogued) that affects ALL spans on the affected sandbox, not just edge-case timing windows.
4. **HIGH**: No IAM policy drift detection for X-Ray permissions — FR-159 enumerates 5 required IAM actions but no automation detects if permissions are silently removed. AWS has no native "expected vs actual" IAM drift service. AWS Config `iam-policy-change` managed rule is the closest native solution; CloudTrail + EventBridge can detect IAM mutation events.
5. **HIGH**: Canary dead-man detection gap — FR-126's heartbeat alarm (`treat_missing_data=breaching`) detects a stopped canary IF CloudWatch Alarms service is healthy. But if the canary stops running AND primary-region CloudWatch Alarms are degraded simultaneously, the cross-region SNS (FR-136) cannot fire because it depends on the canary Lambda executing. This is a double-failure scenario (EventBridge/Lambda failure + CloudWatch failure) with zero external detection.
6. **HIGH**: PutTraceSegments TPS quota is ~2,500/region (not 2,600 as stated in spec assumptions), AND it is adjustable via Service Quotas (quota code `L-AB6D2D9B`). The spec treats this as a hard, non-adjustable limit. Exceeding it returns `ThrottledException` with permanently lost segments in `UnprocessedTraceSegments`.
7. **HIGH**: OTLP HTTP exporter retry behavior blocks BSP worker thread — retries up to 6 times with exponential backoff (up to 10s total), holding `_export_lock`. During retry sleep, the BSP worker thread is blocked and spans accumulate in the deque. Under ADOT Extension degradation (intermittent 5xx), this amplifies span loss beyond the ~30% edge-case from aws-otel-lambda#886.
8. **MEDIUM**: X-Ray Groups quota (25/region) and Sampling Rules quota (25/region) are BOTH adjustable via Service Quotas — not "non-adjustable" as previously assumed. However, the spec creates multiple Groups (FR-013 error group, FR-141 sse-reconnections) without tracking total count against quota.
9. **MEDIUM**: X-Ray encryption at rest is enabled by default with AWS-managed key — optional CMK available via `PutEncryptionConfig`. Not addressed in spec despite FR-155 covering PII prohibition. Encryption-at-rest configuration should be documented for compliance posture.
10. **MEDIUM**: GetTraceSummaries has a 6-hour maximum time range window per request — FR-163's archival procedure must issue multiple requests with sliding time windows for incidents spanning >6 hours, compounding the rate-limit delay.
11. **MEDIUM**: Centralized X-Ray sampling rules do NOT override ParentBased sampler parent decisions — they only apply at trace roots (no parent context). FR-161's cost guard controls Function URL traffic (rootless) but has NO effect on API Gateway traffic that inherits parent sampling decisions. This is correct behavior but must be explicitly documented to prevent implementer confusion.
12. **MEDIUM**: Lambda Function URL is invisible in X-Ray service map — no separate "Function URL" node exists (unlike API Gateway). Operators lose gateway-level latency breakdown (authorization, WAF evaluation, integration latency). Service map shows the Lambda function directly.
13. **MEDIUM**: ADOT version upgrade procedure needs formalization — container-based ADOT (FR-069/FR-070) requires Dockerfile digest update + rebuild + deploy + cold-start re-measurement (FR-158). No runbook documents this procedure or its required verification gates.

**FR-156 BSP Deadlock Vector Status Amendment (Round 21 — Amendment)**

- **FR-165** (P0 — CRITICAL): FR-156's characterization of BSP deadlock (opentelemetry-python#3886) as an active span-loss vector MUST be amended. The OTel Python SDK v1.33.0 (released 2025) completely rewrote `BatchSpanProcessor` internals — the old implementation used `threading.Condition` with nested lock acquisition patterns that deadlocked under `atexit`/`fork`; the new implementation (used in this project's pinned v1.39.1) uses a `collections.deque` + `threading.Event` (`_worker_awaken`) pattern with no nested locks. FR-156's diagnostic distinction between "bsp_or_extension_hang" and "extension_unavailable" remains architecturally valuable — it differentiates ADOT Extension failures from SDK-internal issues. However, the structured log label `bsp_or_extension_hang` MUST be amended to `extension_hang` because BSP deadlock is no longer a possible cause at the pinned SDK version. The spec's "THREE catalogued span-loss vectors" MUST be reduced to TWO: (1) ADOT Extension drop race (FR-074), (2) ADOT Extension hang (FR-139). FR-156's BSP deadlock vector is RETIRED for SDK v1.33.0+ with the caveat that re-introducing older OTel SDK versions would re-expose this risk. Source: OTel Python SDK `opentelemetry/sdk/_shared_internal/__init__.py` — `BatchProcessor` class (v1.39.1), lines 84-243; `os.register_at_fork` handling at lines 123-126; opentelemetry-python#3886 — fix merged in v1.33.0.

**SC-040 Verification Method Correction (Round 21 — Amendment)**

- **FR-166** (P0 — CRITICAL): SC-040's verification method ("confirming zero 'Queue is full' log entries") MUST be replaced. The SDK v1.39.1 `BatchProcessor.emit()` method uses `collections.deque([], max_queue_size)` where `appendleft()` silently evicts the oldest element when full — there is NO log entry, NO callback, and NO metric on span drop. The `emit()` method explicitly documents this constraint: "Do not add any logging.log statements to this function, they can be being routed back to this emit function, resulting in endless recursive calls" (opentelemetry-python#4261). The replacement verification method MUST: (a) instrument a test wrapper around the BSP's internal `deque` that counts `len(deque)` at each `emit()` call and records the high-water mark; (b) assert that the high-water mark never reaches `max_queue_size` during a standard 15-second streaming session; (c) alternatively, compare total spans created (counted by a test `SpanProcessor` shim) against total spans exported (counted by a test `SpanExporter` shim) — any discrepancy indicates dropped spans. FR-091's capture of BSP WARNING "Queue is full, dropping span" is also INVALIDATED — this log message does not exist in SDK v1.39.1. Source: OTel Python SDK `_shared_internal/__init__.py:200-208` — `emit()` method with deque; opentelemetry-python#4261 — logging recursion prohibition.

**ADOT Extension Non-Recovery Span-Loss Documentation (Round 21 — New)**

- **FR-167** (P1 — HIGH): The spec MUST document ADOT Extension non-recovery as a DISTINCT span-loss scenario (separate from FR-074 drop race and FR-139 hang). When the ADOT Extension process crashes (OOM, segfault, collector pipeline failure) within a Lambda execution environment, the Lambda service does NOT restart it. The Extension is dead for ALL subsequent warm invocations on that sandbox (potentially 5-15 minutes until idle recycling). The TracerProvider module-level singleton's OTLP HTTP exporter automatically retries connections to localhost:4318, receiving `ConnectionError` on each attempt. The BSP catches all exporter exceptions by OTel specification design (`_shared_internal/__init__.py:178-195`), so the Lambda continues executing normally with zero trace export and zero visible errors. Detection: FR-140's `ADOTExtensionUnavailable` CloudWatch Logs metric filter detects the per-invocation failure; the canary (FR-019/FR-036) detects aggregate trace loss. Recovery: only execution environment recycling (idle timeout) or new cold start. The spec's span-loss vector catalogue MUST be updated to THREE vectors: (1) ADOT Extension drop race (FR-074, ~30% edge-case), (2) ADOT Extension hang (FR-139, thread wrapper abort), (3) ADOT Extension crash non-recovery (FR-167, 100% loss on affected sandbox). Unlike vectors 1 and 2 which affect individual flush operations, vector 3 affects ALL spans on the affected sandbox for its remaining lifetime. Source: AWS Lambda Extensions API — Extensions started once during INIT, not restarted on crash; OTel Python SDK `BatchProcessor._export()` exception handling; OTLP HTTP exporter `_export()` retry with `ConnectionError`.

**IAM Policy Drift Detection for X-Ray Permissions (Round 21 — New)**

- **FR-168** (P1 — HIGH): An automated mechanism MUST detect when IAM permissions required by ADOT-instrumented Lambdas (FR-159's 5 actions) are modified or removed. AWS has no native "expected vs actual" IAM policy drift service. The detection MUST use one of: (a) AWS Config `iam-policy-change` managed rule with a CloudWatch Events rule that evaluates Lambda execution role policies against the required 5-action set, or (b) CloudTrail EventBridge rule matching `IAM:PutRolePolicy`, `IAM:DetachRolePolicy`, `IAM:DeleteRolePolicy`, and `IAM:AttachRolePolicy` events for ADOT-instrumented Lambda execution role ARNs, triggering evaluation of whether the 5 required actions remain present. When drift is detected, the mechanism MUST: (a) emit a `SentimentAnalyzer/Reliability/IAMPolicyDrift` CloudWatch metric with `FunctionName` dimension, (b) trigger a CloudWatch alarm, (c) include the specific missing action(s) in the alarm description. This prevents the silent degradation to 1 trace/sec caused by missing `GetSampling*` permissions (FR-159) from persisting undetected. The detection latency for IAM changes via CloudTrail is typically < 15 minutes. Source: AWS Config managed rules — `iam-policy-change`; AWS CloudTrail event reference — IAM events; FR-159 silent fallback analysis.

**Canary Dead-Man External Health Check (Round 21 — New)**

- **FR-169** (P1 — HIGH): The canary's liveness MUST be externally verifiable beyond CloudWatch-internal mechanisms. FR-126's heartbeat alarm (`treat_missing_data=breaching`) detects a stopped canary when CloudWatch Alarms service is healthy. FR-136's cross-region SNS detects failures when the canary runs and discovers problems. Neither mechanism detects the double-failure scenario: canary stops running AND primary-region CloudWatch Alarms are degraded. The canary Lambda MUST emit a heartbeat signal to an external monitoring endpoint on every successful invocation: (a) a cross-region CloudWatch metric in the SAME region as FR-136's cross-region SNS topic (NOT the primary region), published via direct `cloudwatch.put_metric_data()` to the backup region endpoint, OR (b) a POST to an external uptime monitoring service (PagerDuty heartbeat, OpsGenie heartbeat, or equivalent) that alerts when heartbeats stop. The external heartbeat MUST be fire-and-forget (best-effort, no error propagation to canary logic) because the canary's primary function (FR-145) must not be disrupted by heartbeat delivery failures. This external heartbeat closes the last remaining gap in the meta-observability chain: X-Ray → canary detects → CloudWatch + cross-region SNS alerts → external heartbeat verifies canary is alive. Source: AWS Well-Architected REL-13 — "monitor the health of your monitoring systems across regions"; PagerDuty heartbeat monitoring pattern.

**PutTraceSegments TPS Quota Correction and Monitoring (Round 21 — Amendment)**

- **FR-170** (P1 — HIGH): The spec's assumption of "2,600 segments/second/region" as the X-Ray throughput limit MUST be corrected to "~2,500 TPS default" (actual value varies by region). This quota IS adjustable via AWS Service Quotas (quota code `L-AB6D2D9B`). The spec MUST: (a) correct all references from "2,600" to "~2,500 (adjustable)" — specifically the Round 3 assumption and any edge case referencing this limit; (b) add a CloudWatch alarm on the `AWS/XRay` service quota utilization metric or a custom metric derived from `PutTraceSegments` API response parsing for `UnprocessedTraceSegments` count; (c) document that exceeding the quota returns `ThrottledException` and affected segments are listed in `UnprocessedTraceSegments` — data is PERMANENTLY lost (not queued or retried by the X-Ray service). At current projected traffic (<1M requests/month), this limit is not a concern. At 10x traffic growth, with 100% sampling and ~45 spans per SSE invocation, the system would produce ~520 segments/sec at peak — well within quota. However, burst traffic during incidents could spike higher. The Service Quotas alarm provides early warning before data loss occurs. Source: AWS X-Ray Service Quotas — quota code `L-AB6D2D9B`; AWS Service Quotas documentation — quota utilization alarms.

**OTLP Exporter Retry-Induced BSP Worker Block (Round 21 — New)**

- **FR-171** (P2 — MEDIUM): The spec MUST document that the OTLP HTTP exporter's retry behavior can block the BSP worker thread for up to 10 seconds per failed batch export. The exporter retries up to `_MAX_RETRYS = 6` times with exponential backoff (`2^retry_num * random.uniform(0.8, 1.2)` seconds) on HTTP 408 or 5xx responses. During retry sleep (`self._shutdown_in_progress.wait(backoff_seconds)`), the BSP worker thread is blocked and cannot process other batches. Spans continue accumulating in the deque from `emit()` calls. With `schedule_delay_millis=1000` and retry blocking, the effective export interval degrades from 1 second to 11+ seconds under Extension degradation, causing deque overflow and silent span drops (FR-166). This is NOT a bug — the OTel spec mandates retry for transient failures. However, it amplifies span loss beyond FR-074's ~30% edge case during ADOT Extension degradation scenarios. FR-114's `OTEL_EXPORTER_OTLP_TRACES_TIMEOUT=2` caps individual request timeout but does NOT cap total retry duration. Mitigation: the retry loop checks `deadline` and aborts when total timeout exceeds `self._timeout` (default 10s). Note: HTTP 429 (rate limit) is NOT retried by the OTLP HTTP exporter — this deviates from the OTel spec recommendation. Source: OTel Python SDK `trace_exporter/__init__.py:174-208` — export retry loop; `_common/__init__.py:29-34` — `_is_retryable()`; opentelemetry-python — retry policy implementation.

**X-Ray Groups and Sampling Rules Quota Tracking (Round 21 — New)**

- **FR-172** (P2 — MEDIUM): The spec MUST track X-Ray Groups and Sampling Rules against their per-region quotas. Both quotas default to 25 per region and are adjustable via Service Quotas. Current spec creates at minimum: (a) 1 error/fault Group (FR-013), (b) 1 `sse-reconnections` Group (FR-141), (c) 1 centralized sampling rule for Function URL (FR-161), plus the default Group and default sampling rule. This leaves ~21 Groups and ~22 sampling rules available. Future additions (per-Lambda Groups, per-environment sampling rules, per-customer trace Groups) could exhaust quotas without warning. The Terraform configuration MUST: (a) include a comment documenting the current Group and Sampling Rule count against the 25-per-region quota; (b) be validated by a CI check that counts `aws_xray_group` and `aws_xray_sampling_rule` resources across all Terraform configurations and warns when count exceeds 20 (80% of default quota). Source: AWS X-Ray Service Quotas — Groups quota code `L-F895D63C`, Sampling Rules quota code `L-B5F5E1ED`; both adjustable.

**X-Ray Encryption-at-Rest Documentation (Round 21 — New)**

- **FR-173** (P2 — MEDIUM): The spec MUST document X-Ray's encryption-at-rest configuration. X-Ray encrypts ALL trace data at rest by default using an AWS-managed key (zero customer configuration required). Optionally, a customer-managed CMK can be configured via `PutEncryptionConfig` API per-region. FR-155 prohibits PII in trace data; encryption-at-rest provides defense-in-depth for data that inadvertently contains sensitive content during the 30-day retention window. The spec MUST: (a) document that default encryption is active and sufficient for the current threat model; (b) note that CMK configuration is available if the organization's compliance posture requires customer-controlled encryption keys; (c) NOT mandate CMK by default (adds KMS cost and key management complexity without security benefit beyond what AWS-managed encryption provides). This FR is informational — it documents the existing security posture rather than requiring changes. Source: AWS X-Ray Developer Guide — "Data protection in AWS X-Ray"; `PutEncryptionConfig` API reference.

**GetTraceSummaries Time Window Constraint (Round 21 — Amendment)**

- **FR-174** (P2 — MEDIUM): FR-163's trace archival procedure MUST account for the `GetTraceSummaries` API's 6-hour maximum time range window per request. For incidents spanning more than 6 hours, the archival script MUST issue multiple requests with sliding 6-hour time windows to cover the full incident duration. This compounding constraint — combined with the 30 TPS rate limit and 100-result pagination — means archiving a 24-hour incident with 100,000 traces requires: (a) 4 time windows × multiple paginated requests to enumerate trace IDs, (b) 20,000 `BatchGetTraces` requests (5 traces/request) at 5 TPS = ~67 minutes for export. Total archival time for a 24-hour incident: ~70-90 minutes depending on trace distribution across time windows. FR-163's runbook MUST include this time estimation formula and recommend starting archival immediately upon incident declaration (not waiting for post-mortem). Source: AWS X-Ray API Reference — `GetTraceSummaries` parameters; confirmed 6-hour maximum time range window, 30 TPS rate limit, 100 results per page.

**Centralized Sampling Rule Scope Documentation (Round 21 — New)**

- **FR-175** (P2 — MEDIUM): The spec MUST explicitly document the interaction between `parentbased_always_on` sampler (FR-056) and centralized X-Ray sampling rules (FR-161). Centralized sampling rules ONLY apply at trace roots — spans with no parent context. They do NOT override parent sampling decisions propagated via `X-Amzn-Trace-Id` header. For this system: (a) API Gateway-fronted Lambdas (Dashboard, Ingestion, Analysis, Notification) receive parent sampling decisions FROM API Gateway — centralized rules have NO effect on these Lambdas; (b) Function URL Lambda (SSE Streaming) receives no parent context from Function URL — centralized rules ARE the sampling decision; (c) EventBridge-triggered Lambda (Canary) receives no parent context — centralized rules apply; (d) the Metrics Lambda receives no parent context (EventBridge Scheduler) — centralized rules apply. FR-161's cost guard therefore controls SSE + Canary + Metrics Lambda sampling rates. API Gateway Lambdas are always sampled at whatever rate API Gateway decides (typically 100% for first request/sec + 5% thereafter per default rule). Implementers MUST understand this distinction to avoid expecting centralized rules to reduce API Gateway Lambda trace volume. Source: OTel SDK `sampling.py:349-367` — `ParentBased` delegation logic; X-Ray centralized sampling specification; AWS API Gateway X-Ray sampling behavior.

**Lambda Function URL Service Map Limitation (Round 21 — New)**

- **FR-176** (P2 — MEDIUM): The spec MUST document that Lambda Function URL invocations are INVISIBLE as a separate node in the X-Ray service map. Unlike API Gateway (which creates its own X-Ray segment and appears as a gateway node), Function URL acts as a passthrough — the trace shows the Lambda function directly with no intermediary. Operators lose gateway-level latency breakdown (authorization time, TLS negotiation, integration latency) that API Gateway provides. The SSE Lambda (accessed via Function URL) appears in the service map as a Lambda node with downstream dependencies (DynamoDB, CloudWatch) but no upstream gateway node. For debugging SSE connection establishment latency, operators MUST use Lambda `REPORT` log lines (`Init Duration` for cold start, `Duration` for handler) rather than X-Ray gateway-level metrics. FR-138's Function URL `Url5xxError` alarm partially compensates for the missing gateway-level error visibility. Source: AWS Lambda Function URL documentation — no X-Ray segment creation; AWS Lambda X-Ray tracing documentation — segment created by Lambda service, not Function URL.

**ADOT Version Upgrade Runbook (Round 21 — New)**

- **FR-177** (P2 — MEDIUM): An operational runbook MUST document the ADOT version upgrade procedure for container-based Lambda deployments (FR-069/FR-070). The procedure MUST include: (a) check ADOT release notes for breaking changes in collector config schema, exporter behavior, or resource detector output; (b) update Dockerfile ADOT image digest (`COPY --from=public.ecr.aws/aws-observability/aws-otel-lambda-extension-amd64:NEW_VERSION@sha256:NEW_DIGEST`); (c) run CI collector config validation (FR-116/SC-066) against the new ADOT binary; (d) deploy to preprod and re-measure cold start latency (FR-158 gate); (e) run FR-160's streaming span retention verification (≥95%); (f) run FR-117's automated trace validation (segment counts, annotation keys); (g) verify ADOT Extension logs show `"Successfully fetched sampling rules"` (FR-159/SC-109 IAM verification); (h) if all gates pass, update production Dockerfile and deploy. The procedure MUST be cross-referenced from FR-070 (digest pinning) and FR-110 (pre-ADOT baseline image retention). ADOT upgrades MUST NOT be bundled with application code changes — they are independently deployable and independently rollbackable (revert Dockerfile digest). Source: ADOT release process — aws-otel.github.io; FR-070 digest pinning rationale; FR-110 rollback image retention.

### Key Entities

- **X-Ray Trace**: A distributed trace spanning one or more service components, identified by a trace ID in the format `1-{hex_timestamp}-{24_hex_digits}`. Propagated via the `X-Amzn-Trace-Id` header.
- **X-Ray Subsegment**: A named, timed span within a trace representing a discrete operation (e.g., a DynamoDB query, a connection acquisition). Can carry annotations (indexed, searchable key-value pairs) and metadata (non-indexed structured data).
- **X-Ray Annotation**: An indexed key-value pair attached to a subsegment. Annotations are searchable in the X-Ray console and API, making them the primary mechanism for filtering traces by business attributes (e.g., `event_type = "bucket_update"`, `cache_hit_rate = 0.85`).
- **X-Ray Canary**: A scheduled probe that validates X-Ray ingestion health and data integrity by submitting and querying test traces, reporting results via a separate (non-X-Ray) channel.
- **X-Ray Group**: A filtered view of traces defined by a filter expression (e.g., `fault = true`). Groups automatically generate CloudWatch metrics, enabling alarm-based monitoring from trace data without manual metric emission.
- **X-Ray Sampling Rule**: A server-side configuration that controls which requests are traced. Rules specify a reservoir (guaranteed minimum per second) and a fixed rate (percentage of additional requests). Rules are evaluated by priority (lowest number first).

## Assumptions

- ~~The X-Ray SDK auto-patches HTTP client libraries including the one used by the SendGrid SDK.~~ **INVALIDATED in Round 2**: The SendGrid SDK uses `python-http-client` which uses Python's stdlib `urllib.request`. The X-Ray SDK does NOT auto-patch `urllib`. SendGrid HTTP calls require explicit X-Ray subsegments (see FR-028).
- The existing CloudWatch RUM configuration (`enable_xray = true`) already generates browser-side X-Ray trace contexts. The frontend needs to extract and propagate these, not generate new ones. CloudWatch RUM's web client auto-injects `X-Amzn-Trace-Id` headers on `fetch()` calls when `addXRayTraceIdHeader: true` is configured in the http telemetry options.
- When both the publishing and subscribing Lambda functions have Active X-Ray tracing enabled, AWS automatically propagates trace context through SNS via the `AWSTraceHeader` system attribute. No manual MessageAttributes configuration is needed for cross-Lambda trace linking.
- Auto-instrumentation patches all supported AWS SDK calls (DynamoDB, SNS, CloudWatch, etc.) transparently. Explicit subsegments are needed only for business logic operations not covered by auto-patching.
- The X-Ray canary's query latency window (time between trace submission and retrievability) is typically 5-30 seconds due to X-Ray's eventual consistency model.
- Standard AWS managed policies for X-Ray provide sufficient permissions for Lambda trace emission and telemetry reporting.
- ~~The X-Ray SDK's `xray_recorder.begin_segment()` API can create independent segments outside the Lambda runtime's auto-created segment. These segments can carry a specified trace ID to link them to the original invocation, appearing as part of the same distributed trace.~~ **INVALIDATED in Round 3**: `begin_segment()` is a documented no-op in Lambda. The SDK's `LambdaContext.put_segment()` silently discards segments with a log warning (source: `aws_xray_sdk/core/lambda_launcher.py:55-59`). The `FacadeSegment` raises `FacadeSegmentMutationException` on all mutation operations including `close()`, `put_annotation()`, `put_metadata()`, `set_aws()`, `add_exception()`, and `serialize()`. Additionally, the X-Ray daemon within the Lambda execution environment may begin shutting down after the handler returns in `RESPONSE_STREAM` mode, making even raw UDP emission unreliable. **Replacement**: The SSE Lambda uses a Lambda Extension-based tracing mechanism (e.g., ADOT — AWS Distro for OpenTelemetry) that runs as a separate process with its own lifecycle. Lambda Extensions receive INIT, INVOKE, and SHUTDOWN lifecycle events and continue running after the handler returns. The extension accepts trace spans via local OTLP endpoint and exports to X-Ray, providing trace data emission during response streaming without depending on the X-Ray SDK's LambdaContext.
- **(Round 2)** The X-Ray SDK supports `str`, `int`, `float`, and `bool` annotation types. All annotations defined in this spec use these types. `None` values are silently dropped, so annotations MUST use explicit sentinel values or be omitted when the value is unavailable.
- **(Round 2)** Adding Powertools Tracer as a dependency to all non-streaming Lambda containers adds approximately 5-10MB to the deployment package and 50-100ms to cold start. This is acceptable given that the Lambdas already include the aws-xray-sdk dependency.
- **(Round 3)** The `EventSource` browser API does not support custom HTTP headers. This is defined in the WHATWG HTML Living Standard, Section 9.2. The constructor signature is `EventSource(url, eventSourceInitDict)` where `eventSourceInitDict` only supports `withCredentials: boolean`. CloudWatch RUM's automatic trace header injection patches `window.fetch()` and `XMLHttpRequest.prototype.open` but does NOT patch `EventSource`. Trace propagation for SSE connections requires using `fetch()` + `ReadableStream` instead.
- **(Round 3)** Adding the ADOT Lambda Extension to the SSE Lambda adds approximately 40-60MB to the execution environment and 50-200ms to cold start (runs concurrently with Lambda INIT, partially masking overhead). The extension provides an OTLP receiver on localhost that accepts trace spans independently of the X-Ray SDK lifecycle.
- **(Round 3)** X-Ray's region-level throughput limit is 2,600 segments per second. Beyond this, `PutTraceSegments` returns `ThrottledException` and affected segments are listed in `UnprocessedTraceSegments` — data is permanently lost, not queued. The X-Ray daemon provides batching and burst capacity but cannot sustain above-limit throughput.
- **(Round 3)** The default `threading.local()` context storage used by the X-Ray SDK correctly propagates across `asyncio.new_event_loop().run_until_complete()` boundaries when called from the same thread. This is because `run_until_complete()` executes coroutines on the calling thread. The X-Ray SDK's `AsyncContext` alternative uses `TaskLocalStorage`, which is scoped to `asyncio.Task` objects and does NOT propagate across event loop boundaries.
- **(Round 3)** At current traffic levels (<1M requests/month), 100% X-Ray sampling costs less than $5/month, well within the cost guard thresholds. The free tier covers the first 100,000 traces recorded per month.
- **(Round 4)** CloudFront is not in the current architecture — removed in Features 1203-1207. Browser-to-backend trace propagation via `X-Amzn-Trace-Id` headers reaches API Gateway and Lambda Function URLs directly without intermediary header manipulation. CloudFront treats `X-Amzn-Trace-Id` as a restricted header and replaces client-sent values with its own; re-introducing CloudFront would break browser-originated trace propagation (see edge case).
- **(Round 4)** ADOT Lambda Extension in "sidecar-only" mode (OTLP receiver without auto-instrumentation) does not conflict with Powertools Tracer or the X-Ray SDK. Conflicts arise only when ADOT auto-instrumentation is enabled via the `AWS_LAMBDA_EXEC_WRAPPER` environment variable, which activates handler wrapping (`AwsLambdaInstrumentor`) and botocore patching (`BotocoreInstrumentor`).
- **(Round 4)** CloudWatch metric ingestion delay (time between `put_metric_data` and metric availability via `get_metric_statistics`) is typically 60-120 seconds. The canary's CloudWatch health check query window must account for this delay. Source: AWS CloudWatch API documentation.
- **(Round 4)** At current traffic levels with 100% X-Ray sampling, the gap between X-Ray trace availability and CloudWatch metric availability for error detection is effectively zero — every error has both an X-Ray trace and a CloudWatch metric. The dual-instrumentation requirement for silent failure paths (FR-043) provides insurance for the future when production sampling may be reduced below 100%.
- **(Round 5)** The OTel `AwsXRayLambdaPropagator` reads the `_X_AMZN_TRACE_ID` Lambda environment variable and extracts `Root` (trace ID), `Parent` (span ID), and `Sampled` (sampling decision) into the OTel context. This provides the bridge between the Lambda runtime's X-Ray trace and the OTel SDK's span hierarchy. When combined with `parentbased_always_on` sampler, the OTel SDK honors the Lambda runtime's sampling decision. Source: `opentelemetry-propagator-aws-xray` package, PyPI.
- ~~**(Round 5)** The OTel `BatchSpanProcessor`'s default batch timeout of 200ms means streaming-phase spans are exported to the ADOT Extension incrementally during the 15-second streaming lifecycle. The `force_flush()` call at end of generator handles only the final partial batch (spans created in the last <200ms before generator exhaustion). The flush target is localhost (ADOT Extension), not remote X-Ray API.~~ **CORRECTED in Round 9**: The actual `BatchSpanProcessor` `schedule_delay_millis` default is **5000ms** (5 seconds), NOT 200ms. FR-073 overrides to 1000ms, making the corrected statement: "With FR-073's tuned 1000ms schedule delay, streaming-phase spans are exported approximately every 1 second. The `force_flush()` call at end of generator handles the final partial batch (spans created in the last <1000ms before generator exhaustion)." The original 200ms figure was incorrect and caused downstream reasoning errors about the flush window. Source: OTel Python SDK v1.39.1 — `BatchSpanProcessor` default `schedule_delay_millis=5000`; OTel Specification, BatchSpanProcessor section.
- **(Round 5)** The ADOT Extension layer in sidecar-only mode (no `AWS_LAMBDA_EXEC_WRAPPER`, OTLP receiver only) is approximately 35-45MB unzipped. The additional OTel Python SDK packages (FR-057) add approximately 5-10MB. Combined with the SSE Lambda's existing deployment package, the total remains well within the 250MB Lambda deployment limit.
- **(Round 5)** The X-Ray `PutTraceSegments` API rejects segment documents exceeding 64KB. The X-Ray SDK and OTel SDK both mitigate this by emitting completed subsegments/spans as independent documents, but individual documents with large metadata can still exceed the limit. The 50-annotation-per-subsegment limit (2,048 chars each) produces ~100KB of annotation data at maximum, which exceeds 64KB — in practice, annotations are small (typically <50 chars each), keeping documents well under the limit.
- **(Round 6)** The `_X_AMZN_TRACE_ID` environment variable is updated by the Lambda runtime at the start of each invocation and remains stable for the duration of that invocation. `AwsXRayLambdaPropagator.extract()` reads `os.environ.get("_X_AMZN_TRACE_ID")` on each call with no internal caching. Module-level extraction captures only the INIT-phase value, which becomes stale on warm invocations.
- ~~**(Round 6)** The SSE Lambda's current memory allocation of 512MB provides sufficient headroom for the ADOT Extension (~40-60MB) plus OTel SDK packages (~5-10MB). No memory increase is required. Source: Terraform `infrastructure/terraform/main.tf` — `memory_size = 512`.~~ **INVALIDATED in Round 12**: See FR-102. Memory increase to 1024MB required. ADOT Extension (~50MB) + OTel SDK (~30MB) + runtime (~100MB) + application (~150MB) = ~330MB baseline leaves insufficient headroom at 512MB. Source: AWS Lambda performance benchmarks.
- ~~**(Round 6)** The Lambda RESPONSE_STREAM runtime calls the generator's `.close()` method or propagates `BrokenPipeError` when the client disconnects mid-stream, ensuring the generator's `finally` block executes reliably. The `finally` block is NOT deferred to garbage collection.~~ **PARTIALLY INVALIDATED in Round 11**: This assumption is CORRECT for client disconnect (BrokenPipeError is raised, generator `.close()` is called, `finally` block executes) but INCORRECT for Lambda timeout. Lambda does NOT send SIGTERM to the custom runtime process on timeout — the process is killed directly (SIGKILL after Extension Shutdown phase). Python `finally` blocks do NOT execute on SIGKILL. FR-055's `force_flush()` in the `finally` block handles normal completion and client disconnect but NOT Lambda timeout. FR-093 adds proactive deadline-aware flush to cover the timeout path. Source: AWS Lambda Runtime Environment Lifecycle — Extensions receive Shutdown event with `shutdownReason: "TIMEOUT"`; runtime process is terminated.
- **(Round 6)** Powertools Tracer's `auto_patch=True` patches `botocore.client.BaseClient._make_api_call` at module import time. These patches are global (not scoped to the handler phase) and remain active during the streaming phase. The X-Ray daemon's ability to accept subsegments created by auto-patched calls during RESPONSE_STREAM streaming is undocumented by AWS and may be unreliable — the daemon process lifecycle after handler return is not guaranteed.
- **(Round 7)** The ADOT Lambda Extension's default collector configuration for Lambda includes an OTLP HTTP receiver on port 4318 and an AWS X-Ray exporter pipeline. In sidecar-only mode (no `AWS_LAMBDA_EXEC_WRAPPER`), the collector receives OTel spans via OTLP and exports them to X-Ray without any auto-instrumentation. If the default config includes extraneous components, a custom `collector.yaml` is required. Source: ADOT Lambda Extension GitHub repository — `adot/collector/config.yaml`.
- ~~**(Round 7)** The SSE Lambda currently uses zero Lambda layers (S3-packaged deployment). Adding 1 ADOT Extension layer is well within the 5-layer Lambda limit. The combined unzipped size (Lambda package + ADOT collector-only layer ~35MB) remains within the 250MB Lambda deployment limit. Source: AWS Lambda quotas — 5 layers per function, 250MB unzipped deployment limit.~~ **INVALIDATED in Round 8**: The SSE Lambda is container-based (ECR image with `python:3.13-slim` + custom bootstrap), NOT ZIP-deployed. Lambda container images CANNOT use Lambda Layers (source: AWS Lambda Container Image docs — "You cannot add Lambda layers to container images"). ADOT is embedded in the container image via multi-stage Dockerfile build. Container images have a 10GB size limit, not 250MB. The 5-layer limit is irrelevant.
- **(Round 7)** The X-Ray daemon process (~16MB) and ADOT Lambda Extension (~40-60MB) coexist within the SSE Lambda's 512MB memory allocation. Combined tracing infrastructure overhead is ~76MB (15% of total), leaving ~436MB for the Python runtime and application code. This is validated against the current SSE Lambda memory usage. Source: Terraform `infrastructure/terraform/main.tf` — SSE Lambda `memory_size = 512`, zero layers currently.
- **(Round 7)** The OTel Python SDK's `BatchSpanProcessor` spawns one daemon thread per instantiation. Module-level `TracerProvider` instantiation creates exactly one thread for the lifetime of the execution environment. Repeated instantiation without `shutdown()` orphans daemon threads (~1MB each), causing monotonic memory growth. Source: OTel Python SDK — `BatchSpanProcessor.__init__()` starts `threading.Thread(daemon=True)`.
- **(Round 7)** The `set_global_textmap()` function in the OTel Python SDK unconditionally overwrites the global propagator, including any propagator loaded from the `OTEL_PROPAGATORS` environment variable at import time. The env var sets the initial default; `set_global_textmap()` is effective at any subsequent call. Source: OTel Python SDK v1.39.1 — `opentelemetry/propagate/__init__.py`.
- **(Round 8)** The SSE Lambda is container-based, using `python:3.13-slim` as its base image with a custom `bootstrap` script that implements the Lambda Runtime API for RESPONSE_STREAM mode. Lambda Extensions (including ADOT) are Lambda service-level features, not runtime-level — they are discovered by scanning `/opt/extensions/` during INIT and launched as separate processes. The ADOT Extension registers with the Extensions API (`/2020-01-01/extension/register`) independently of the custom runtime. No bootstrap modifications are needed. Source: AWS Lambda Extensions API documentation.
- **(Round 8)** AWS publishes ADOT Lambda Extension images on the public ECR gallery at `public.ecr.aws/aws-observability/`. The multi-stage Dockerfile pattern `COPY --from=adot-image /opt/extensions/ /opt/extensions/` extracts the collector binary. Image tags must be digest-pinned (`@sha256:...`) for reproducible builds. Source: ECR Public Gallery — AWS Observability.
- **(Round 8)** The OTel `AwsLambdaResourceDetector` (from `opentelemetry-sdk-extension-aws`) reads `AWS_LAMBDA_FUNCTION_NAME`, `AWS_LAMBDA_FUNCTION_VERSION`, `AWS_REGION`, `AWS_LAMBDA_LOG_STREAM_NAME`, and `AWS_LAMBDA_FUNCTION_MEMORY_SIZE` environment variables. These are set by the Lambda service regardless of base image. The detector sets `cloud.provider`, `cloud.platform`, `cloud.region`, `faas.name`, `faas.version`, `faas.instance`, and `faas.max_memory` — but does NOT set `service.name`. Without explicit `OTEL_SERVICE_NAME`, the default is `unknown_service`. Source: `opentelemetry-python-contrib` — `sdk-extension/opentelemetry-sdk-extension-aws/src/opentelemetry/sdk/extension/aws/resource/_lambda.py`.
- **(Round 8)** The OTel `BatchSpanProcessor` default configuration (`schedule_delay_millis=5000`, `max_queue_size=2048`, `max_export_batch_size=512`) is designed for long-running server processes, not Lambda. For the SSE Lambda (900s streaming), the 5s delay is functionally acceptable since `force_flush()` drains the entire queue synchronously. However, reduced defaults (`schedule_delay_millis=1000`, `max_queue_size=256`, `max_export_batch_size=64`) consume less memory and export more frequently, reducing the risk of data loss if `force_flush()` is not reached. Source: OTel Python SDK v1.39.1 — `BatchSpanProcessor` defaults and `OTEL_BSP_*` env vars.
- **(Round 9)** The ADOT Lambda Extension's decouple processor (Lambda-specific batch processor) decouples span reception from export, preventing the Extension from signaling invocation completion until all queued spans are exported to the X-Ray backend. This is the accepted mitigation for the known span drop race condition (aws-otel-lambda#886). The decouple processor is included in the ADOT Extension's default Lambda configuration. Source: https://github.com/open-telemetry/opentelemetry-lambda/blob/main/collector/processor/decoupleprocessor/README.md
- **(Round 9)** The `TracerProvider(shutdown_on_exit=False)` parameter prevents Python `atexit` handler registration. In Lambda, the `atexit` handler is redundant because FR-055 mandates `force_flush()` at end of each streaming session, and the ADOT Extension has its own SHUTDOWN phase for final export. Disabling `atexit` eliminates the thread pool shutdown race (opentelemetry-python#4461) with zero functional impact.
- **(Round 9)** X-Ray trace propagation delay (time between `PutTraceSegments` and availability via `BatchGetTraces`) is documented as "generally 30 seconds" in the AWS FAQ. This is eventual consistency, NOT a deterministic SLA. Under regional load or during X-Ray service degradation, delay may exceed 30 seconds. The canary's retry-with-backoff (30s then 60s per FR-078) accounts for this variability.
- **(Round 9)** CloudWatch `put_metric_data` error codes fall into two categories: (a) transient/retryable: `ThrottlingException`, `InternalFailure`, `ServiceUnavailable`, `InternalServiceError`; (b) permanent/IAM: `AccessDeniedException`, `NotAuthorized`, `InvalidClientTokenId`. The boto3 SDK raises `botocore.exceptions.ClientError` with error code accessible via `error.response['Error']['Code']`. Source: https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/CommonErrors.html
- **(Round 9)** The SSE Lambda's event stream format uses `id:` fields in SSE frames, enabling `Last-Event-ID` based resumption per the SSE protocol specification. If the current SSE Lambda does not emit `id:` fields, FR-081's server-side support requires adding them. The `id:` value must be monotonically increasing or otherwise orderable to support "resume from position" semantics.
- **(Round 9)** The `ReadableStreamDefaultReader.closed` Promise is supported in all modern browsers (Chrome 78+, Firefox 65+, Safari 10.1+) and is the canonical mechanism for detecting stream close type. Source: MDN Web Docs — ReadableStreamDefaultReader.closed.
- ~~**(Round 5)** The OTel `BatchSpanProcessor`'s default batch timeout is 200ms.~~ **CORRECTED in Round 9 (FR-084)**: The actual default `schedule_delay_millis` is 5000ms (5 seconds). FR-073 overrides to 1000ms. The original 200ms figure was incorrect. Source: OTel Python SDK v1.39.1 source code; OTel Specification, BatchSpanProcessor defaults.
- **(Round 10)** AWS is rolling out a new Lambda X-Ray segment format that eliminates the Invocation subsegment and uses `aws.responseLatency`/`aws.responseDuration` annotations on the function segment. Customer subsegments attach directly to `AWS::Lambda::Function` segment instead of the Invocation subsegment. OTel span parenting via `AwsXRayLambdaPropagator` is format-independent because it reads `_X_AMZN_TRACE_ID` which provides trace context regardless of segment format. Powertools Tracer subsegments may attach to a different parent under the new format, but this affects only handler-phase cosmetics in the X-Ray console — not trace correctness. No spec changes required; this assumption documents awareness. Source: https://docs.aws.amazon.com/lambda/latest/dg/services-xray.html
- **(Round 10)** The SSE Lambda MUST NOT be deployed in a VPC when using Function URL response streaming. AWS docs confirm: "Function URLs do not support response streaming in VPC environments, but invoking functions using the InvokeWithResponseStream API is supported." If deployed in a VPC, responses are silently buffered instead of streamed — no error, no warning, just degraded behavior. The current architecture uses DynamoDB (public endpoint via AWS SDK), so VPC is not needed. If VPC access is required in the future (e.g., VPC-only RDS or ElastiCache), the SSE Lambda must either (a) remain non-VPC and access VPC resources via VPC endpoints or PrivateLink, or (b) be invoked via SDK `InvokeWithResponseStream` from a non-VPC intermediary Lambda with a Function URL. Source: https://docs.aws.amazon.com/lambda/latest/dg/configuration-response-streaming.html
- **(Round 10)** The project's span-per-poll-cycle convention for SSE streaming instrumentation (FR-025/FR-026: one parent span per DynamoDB poll cycle, child spans for db query + event dispatch + metric emit) is project-specific and NOT aligned with any OTel semantic convention — because none exists. The OTel specification's section on streaming instrumentation for GenAI spans explicitly says "TODO" (no span-per-event vs span-per-connection convention defined). When OTel publishes streaming semantic conventions, the project SHOULD evaluate alignment and migrate if feasible. Until then, the current convention is the best-available approach. Source: https://opentelemetry.io/docs/specs/semconv/gen-ai/gen-ai-spans/
- **(Round 10)** AWS Lambda response streaming supports only Node.js managed runtimes and custom runtimes. The Python managed runtime (`python3.x`) does NOT support response streaming — `InvokeMode: RESPONSE_STREAM` returns buffered responses identical to `BUFFERED` mode. The SSE Lambda uses `python:3.13-slim` with a custom `bootstrap` script, which IS a custom runtime. This is the architectural reason the SSE Lambda requires container-based deployment with a custom bootstrap (not just for ADOT embedding). Source: https://docs.aws.amazon.com/lambda/latest/dg/configuration-response-streaming.html — "Response streaming currently supports the Node.js managed runtimes and custom runtimes."
- **(Round 8 AMENDED in Round 10)** ~~FR-073's `max_queue_size=256` provides sufficient headroom for the SSE Lambda's span throughput.~~ **CORRECTED in Round 10 (FR-086)**: The original `max_queue_size=512` (updated from 256 in an earlier round) is INSUFFICIENT. Recalculation: 15s stream × 15 polls/s × 3 spans/poll = ~675 spans/invocation. FR-086 amends to `max_queue_size=1500` (>= 2x expected throughput). Source: OTel Python SDK — `BatchSpanProcessor.on_end()` silent drop on queue full.
- **(Round 11)** The OTel Python SDK (v1.39.1) is fully compatible with Python 3.13. All 4 FR-057 packages (`opentelemetry-sdk`, `opentelemetry-exporter-otlp-proto-http`, `opentelemetry-sdk-extension-aws`, `opentelemetry-propagator-aws-xray`) have explicit Python 3.13 PyPI classifiers, zero deprecation warnings, and work correctly with the standard GIL-enabled Python 3.13 build. Free-threading (PEP 703) is NOT enabled in `python:3.13-slim`. Source: PyPI package metadata classifiers; runtime import verification.
- **(Round 11)** API Gateway with X-Ray tracing enabled makes its own sampling decision regardless of client-supplied `Sampled=1` in the incoming `X-Amzn-Trace-Id` header. Lambda Function URLs do NOT have this behavior — they honor the incoming `Sampled` field directly. The asymmetry means FR-035's server-side sampling override works for API Gateway paths but not for Function URL paths. Source: API Gateway X-Ray integration documentation.
- **(Round 11)** The ADOT Lambda Extension's embedded default configuration (used when `OPENTELEMETRY_COLLECTOR_CONFIG_FILE` is not set) includes the decouple processor, OTLP HTTP receiver on port 4318, and AWS X-Ray exporter. However, it also includes unnecessary components (gRPC receiver, potentially metrics pipeline) that consume ~15MB of extra memory. The Extension does NOT auto-discover config files at filesystem paths — it reads the path exclusively from the `OPENTELEMETRY_COLLECTOR_CONFIG_FILE` environment variable. Source: ADOT Lambda Extension GitHub repository — `collector/lambdacomponents/default.go`.
- **(Round 11)** The SSE Lambda's custom bootstrap at `src/lambdas/sse_streaming/bootstrap` reads `Lambda-Runtime-Aws-Request-Id` from the Runtime API `/runtime/invocation/next` response but does NOT read `Lambda-Runtime-Trace-Id`. On cold start, the Lambda service sets `_X_AMZN_TRACE_ID` in the process environment. On warm invocations, the env var retains the cold start value unless the bootstrap explicitly updates it. This is confirmed by code inspection (lines 39-51 of the bootstrap). Source: AWS Lambda Custom Runtime Contract; project source code.
- ~~**(Round 6)** The SSE Lambda's current memory allocation of 512MB provides sufficient headroom for the ADOT Extension (~40-60MB) plus OTel SDK packages (~5-10MB). No memory increase is required. Source: Terraform `infrastructure/terraform/main.tf` — `memory_size = 512`.~~ **INVALIDATED in Round 12**: ADOT Extension consumes ~50MB resident memory, OTel SDK packages add ~30MB import overhead, Python 3.13 runtime requires ~100MB, and application code + dependencies require ~100-150MB. Total baseline of ~350-400MB leaves only ~110-160MB headroom at 512MB — insufficient for burst allocations. Additionally, Lambda CPU scales linearly with memory; at 512MB, cold start is ~1200ms vs ~800ms at 1024MB. FR-102 increases memory to 1024MB. Source: AWS Lambda performance benchmarks; ADOT Lambda Extension documentation.
- **(Round 12)** Concurrent Lambda invocations each get their own isolated execution environment (Firecracker microVM) with their own ADOT Extension process. Lambda guarantees at most one concurrent invocation per execution environment. The OTLP receiver on `localhost:4318` is per-environment (each has its own network namespace). Zero risk of cross-invocation span corruption from concurrency. Sequential warm invocations on the same environment share the Extension process but are serialized — never concurrent. Source: AWS Lambda Execution Environment lifecycle documentation.
- **(Round 12)** The ADOT Extension receives a Shutdown event on Lambda timeout with `shutdownReason: "timeout"` and a 2000ms grace period, even though the runtime process is SIGKILL'd. This means already-received spans (those that completed Hop 1: SDK→Extension) CAN be exported to X-Ray during the Extension's Shutdown phase. Spans still in the BSP queue (Hop 1 not started) are permanently lost because the runtime is dead. FR-093's proactive flush ensures Hop 1 completes before timeout. FR-100's graceful termination converts the timeout into a normal completion, providing an even more reliable Shutdown. Source: AWS Lambda Extensions API — Shutdown event lifecycle.
- **(Round 12)** OTel Python core packages enforce lockstep versioning via internal `==` dependency pins. `opentelemetry-sdk` hard-pins `opentelemetry-api==<exact-version>`. The pip resolver prevents installation of mismatched core packages (build fails with `ResolutionImpossible`). AWS contrib packages (`opentelemetry-propagator-aws-xray`, `opentelemetry-sdk-extension-aws`) use wide `~=1.12` pins against core, allowing any core version >= 1.12. Source: PyPI package metadata; pip resolver behavior.
- **(Round 12)** The OTLP v1 protocol (proto path: `opentelemetry.proto.collector.trace.v1`) is stable and backward-compatible between OTel Python SDK 1.25+ through 1.39.1 and ADOT Collector versions since v0.21.0 (mid-2022). Version skew between the OTel SDK OTLP exporter and the ADOT collector at the protocol level is not a risk. Source: OTel proto v1.0.0 specification.
- **(Round 12)** SSE Lambda memory increase from 512MB to 1024MB costs approximately $2-3/month additional at current invocation patterns (~8,000 invocations/month × 15s average duration). This is well within the FR-038 cost alarm threshold of $10. Source: AWS Lambda pricing — $0.0000166667/GB-second.
- **(Round 12)** The SSE Lambda's custom bootstrap currently passes `None` for the Lambda context parameter: `handler(event_data, None)`. The standard `context.get_remaining_time_in_millis()` API is therefore unavailable to the handler. FR-103 addresses this by reading `Lambda-Runtime-Deadline-Ms` and making it accessible. Source: Project source code — `src/lambdas/sse_streaming/bootstrap` line 157.
- **(Round 13)** The `OTEL_SDK_DISABLED` environment variable is part of the OTel specification and has been supported in `opentelemetry-python` since v1.24.0 (released mid-2024). When set to `true`, the SDK returns no-op implementations for all telemetry APIs (tracers, meters, loggers). The variable is read at SDK initialization time (module-level `TracerProvider` creation per FR-065); changing it after initialization has no effect until the Lambda execution environment is recycled (idle timeout ~5-15 minutes). In Lambda, this means the kill switch takes effect within minutes as execution environments are recycled. Source: OTel Environment Variable Specification — `OTEL_SDK_DISABLED`.
- **(Round 13)** X-Ray Groups require `insights_enabled = true` in the `InsightsConfiguration` to emit CloudWatch metrics. Without this setting, Groups are purely query-time filters — they match traces when operators search, but they do NOT push any metrics to CloudWatch. The `insights_enabled` attribute is independent of X-Ray Insights pricing tier — enabling it on a Group does not incur Insights charges for root cause analysis or impact detection; it only enables basic Group-derived CloudWatch metrics (fault count, error count, response time statistics). Source: AWS X-Ray API Reference — `CreateGroup` API `InsightsConfiguration` parameter; AWS X-Ray pricing page.
- **(Round 13)** EventBridge scheduled rules become active immediately upon creation. There is no built-in delay, "pending" state, or activation delay. If the rule's target (Lambda function) is not yet invocable due to missing `aws_lambda_permission`, the first invocation fails with `AccessDeniedException`. Terraform's `depends_on` meta-argument ensures deterministic resource creation ordering within a single `terraform apply`. Without `depends_on`, Terraform creates independent resources in parallel, making the ordering non-deterministic. Source: AWS EventBridge documentation — rule state lifecycle; Terraform resource ordering documentation.
- **(Round 13)** AWS ECR image lifecycle policies support tag-based exclusion rules. Images tagged with specific prefixes (e.g., `pre-adot-baseline`) can be exempted from automatic cleanup. ECR repositories support up to 10,000 images. The `pre-adot-baseline` image for rollback purposes is a single image (~500MB for the SSE Lambda container) with negligible storage cost (~$0.05/month at ECR pricing of $0.10/GB/month). Source: AWS ECR Lifecycle Policy documentation — `tagPrefixList` filter.
- **(Round 14)** The `OTEL_EXPORTER_OTLP_TRACES_TIMEOUT` environment variable is supported by the `opentelemetry-exporter-otlp-proto-http` package and controls per-request timeout for the OTLP HTTP exporter. Default is 10 seconds. Setting to 2 seconds caps worst-case block during ADOT Extension failure while still allowing normal localhost round-trips (sub-millisecond). Source: OTel Python SDK OTLP HTTP exporter — `DEFAULT_TIMEOUT` and environment variable override.
- **(Round 14)** The ADOT collector binary's `validate` subcommand is available in ADOT v0.40.0+ and produces deterministic exit codes (0 for valid, non-zero for invalid). Validates component references, pipeline wiring, and config schema without starting the collector. The `validate` subcommand from `otelcol-contrib` MUST NOT be used because ADOT bundles different components than contrib. Source: OTel Collector documentation — `validate` subcommand.
- **(Round 14)** Browser CORS preflight cache respects `Access-Control-Max-Age` reliably across Chrome (max 7200s), Firefox (max 86400s), and Safari (max 600s). Cache key is origin + path + method. Source: Fetch Living Standard — CORS preflight cache.
- **(Round 14)** The 50-annotation-per-segment X-Ray limit is sufficient for this feature's instrumentation given the annotation budget (max ~25 per span per FR-120). The 10-annotation reserve provides headroom for diagnostic annotations. Source: X-Ray API documentation — PutTraceSegments annotation limits.
- **(Round 14)** The `event: done` sentinel event pattern (FR-118) is compatible with the frontend's custom SSE parser. Adding a new event type (`done`) requires parser extension but does not conflict with existing types (`data`, `retry`, `deadline` per FR-100). Source: WHATWG HTML Living Standard §9.2.
- **(Round 14)** OTel Python SDK `force_flush()` return value (`True`/`False`) is NOT a reliable indicator of export success. Returns `True` when queue is drained, `False` only when processor is shut down. Export failures during flush do not affect return value. Code MUST NOT use the return value as a health signal. Source: OTel Python SDK `_shared_internal/__init__.py` lines 237-243; issue #4568.
- **(Round 14)** The `treat_missing_data` classification in FR-121 relies on the distinction between "zero-value data points" (metric published with value 0) and "missing data points" (no metric published). AWS-native Lambda metrics publish zero-value points during idle. Custom metric filters with `default_value="0"` emit zeros only when the log group receives non-matching events — NOT when completely silent. Source: AWS CloudWatch Developer Guide — metric filter default values.
- **(Round 15)** CloudWatch Logs metric filters and CloudWatch Metrics use separate API endpoints and subsystems; a Metrics API degradation does not affect Logs metric filter processing. This enables Logs metric filters as a reliable out-of-band backup for put_metric_data failures. Source: AWS regional degradation post-incident reports (us-east-1, 2023-02).
- **(Round 15)** The `python_http_client` library used by SendGrid SDK calls `urllib.request.urlopen()` internally, which resolves to `http.client.HTTPSConnection`. X-Ray's `httplib` patch and OTel's `URLLibInstrumentor` both capture this call chain. Source: python_http_client source code on GitHub.
- **(Round 15)** CloudWatch `put_metric_data` API pricing at $0.01 per 1,000 requests makes the additional metric emission from 5 silent failure paths and 1 heartbeat negligible (estimated ~$2.70/month total for all new custom metrics). Source: CloudWatch pricing page.
- **(Round 15)** Composite alarm `ActionsSuppressor` (added 2022) can prevent alert storms during maintenance windows without silencing the underlying individual alarms. Source: AWS CloudWatch composite alarm documentation.
- **(Round 15)** Lambda error rate math expression `IF(invocations > 10, (errors / invocations) * 100, 0)` requires CloudWatch metric math in alarm definition, which is supported in Terraform via `metric_query` blocks. The minimum invocation guard of 10 prevents division artifacts at low traffic.
- **(Round 15)** Work order files (fix-*.md) were last synchronized around Round 6 (FR-060 range). Rounds 7-14 added FR-062 through FR-123, mapped only in HL doc table. Task 5 has the largest gap: 36 FRs in HL but not in work order file. Source: Manual audit of fix-sse-subsegments.md vs HL doc.
- **(Round 15)** The `analysis_latency_high` alarm threshold at 25,000ms (42% of 60s timeout) is for an async SNS-triggered Lambda where cold starts add 1-5s jitter. AWS recommends 80% of timeout (48,000ms) for async Lambdas to reduce cold-start false positives. Dashboard Lambda's 1,000ms threshold is UX-driven (not timeout-driven) and correctly calibrated. Source: AWS Well-Architected PERF-04.
- **(Round 16)** CloudWatch RUM's `FetchPlugin` monkey-patches `window.fetch` by wrapping the function and injecting headers BEFORE the actual `fetch()` call via argument mutation. The `X-Amzn-Trace-Id` header is added to ALL `window.fetch()` calls that pass the `isUrlAllowed()` URL filter (`urlsToInclude`/`urlsToExclude`). Response body processing (including ReadableStream) is NOT intercepted — RUM only reads `response.status` and `Content-Length` header. Cross-origin requests require the target server to include `X-Amzn-Trace-Id` in CORS `Access-Control-Allow-Headers` for the header to be sent by the browser. If CORS preflight rejects the header, the browser silently drops it — fetch() succeeds but without the trace header. Source: aws-rum-web SDK `FetchPlugin.ts` source code; shimmer monkey-patching library.
- **(Round 16)** Cross-region SNS `publish()` from a Lambda in the primary region to a topic in a backup region has typical latency of 50-200ms. This is acceptable for canary alerting since canary detection latency is already ~10 minutes (2× FR-048 interval). The canary Lambda's execution role must include `sns:Publish` with the cross-region topic ARN — IAM policies are global and work cross-region. Source: AWS IAM documentation — global service.
- **(Round 16)** Python `threading.Thread(daemon=True)` combined with `thread.join(timeout=T)` correctly returns after T seconds if the thread has not completed. In Lambda, when the execution environment freezes (after handler returns), daemon threads are suspended but not cleaned up — they resume on the next warm invocation. The abandoned `force_flush()` thread may complete during freeze or on next invocation — this is acceptable because BSP handles concurrent access and `force_flush()` is idempotent. Source: Python 3.13 `threading` documentation; OTel Python SDK `BatchSpanProcessor` thread safety.
- **(Round 16)** API Gateway publishes `IntegrationLatency` and `Latency` metrics per-stage. Lambda Function URLs do NOT publish through API Gateway and use Lambda service metrics instead (`UrlRequestCount`, `Url4xxError`, `Url5xxError`). The SSE Streaming Lambda uses Function URLs (not API Gateway), so FR-138's API Gateway alarms apply only to the 4 API Gateway-fronted Lambdas (Dashboard, Ingestion, Analysis, Notification). The Metrics Lambda is internally triggered (EventBridge) and has neither API Gateway nor Function URL metrics — it is covered by Lambda-native metrics only. Source: AWS API Gateway Monitoring documentation; Lambda Function URL metrics.
- **(Round 16)** SNS `rawMessageDelivery` mode strips ALL system message attributes including `AWSTraceHeader`, breaking X-Ray trace propagation. SNS-to-Lambda subscriptions with `rawMessageDelivery: true` deliver the raw SNS message body without the SNS envelope or system attributes. The Ingestion→Analysis SNS topic subscription MUST NOT use `rawMessageDelivery` for trace propagation to work. Source: AWS SNS Developer Guide — "Amazon SNS raw message delivery."
- **(Round 16)** OTel Python SDK `BatchSpanProcessor.force_flush()` exports batches SEQUENTIALLY — each `exporter.export()` call blocks until the batch is exported or the per-request timeout expires. With `OTEL_EXPORTER_OTLP_TRACES_TIMEOUT=2` and 22 batches (1350 queue ÷ 64 batch size), worst-case total duration is 44 seconds if every batch times out. The `timeout_millis` parameter passed to `force_flush()` is accepted but NOT enforced (opentelemetry-python#4568, still open). Source: OTel Python SDK `BatchSpanProcessor` source; GitHub issue #4568; PR #4564 (merged June 2025).
- **(Round 17)** The ADOT X-Ray exporter's `makeCause()` function (in `opentelemetry-collector-contrib/exporter/awsxrayexporter/internal/translator/cause.go`) is gated on `span.Status().Code() == StatusCodeError`. Exception events recorded via `span.record_exception()` are processed by `makeCause()` to populate the X-Ray `cause` field (with exception type, message, stack), but ONLY when StatusCode is ERROR. When StatusCode is UNSET, exception events exist on the span but are NOT reflected in the X-Ray segment document — the segment has `fault: false` and `cause: null`. This is confirmed behavior in ADOT Collector v0.40+ and is a direct consequence of the X-Ray segment document schema where `fault`, `error`, and `throttle` are top-level booleans determined by the exporter, not by the presence of exception events. Source: ADOT X-Ray Exporter source code; X-Ray Segment Document API Reference.
- **(Round 17)** The canary Lambda is architecturally distinct from the 6 production Lambdas. It is the ONLY Lambda that: (a) queries X-Ray APIs (GetTraceSummaries, BatchGetTraces), (b) performs cross-region SNS publish (FR-136), (c) is exempt from FR-018 fail-fast on X-Ray initialization, (d) has an EventBridge scheduled trigger (not API Gateway or Function URL). Its Powertools Tracer instrumentation is wrapped in FR-145's error handling, meaning instrumentation failures produce structured logs and metrics rather than Lambda crashes.
- **(Round 17)** OTel SpanKind determines X-Ray segment type via the ADOT exporter's `MakeSegment()` function: `SpanKind.SERVER` and `SpanKind.CONSUMER` → X-Ray top-level segment; `SpanKind.CLIENT`, `SpanKind.PRODUCER`, `SpanKind.INTERNAL` → X-Ray subsegment. For the SSE Lambda's manually-created spans: DynamoDB and CloudWatch calls should use `SpanKind.CLIENT` (renders as remote dependency in service map), internal processing logic should use `SpanKind.INTERNAL` (renders within the Lambda node). Incorrect SpanKind produces correct traces but incorrect service map topology. Source: ADOT X-Ray Exporter `segment.go`.
- **(Round 18)** CloudWatch RUM's FetchPlugin uses the `shimmer` library to monkey-patch `window.fetch` SYNCHRONOUSLY — once the AwsRum constructor completes, all subsequent `fetch()` calls are intercepted without additional async steps. However, the AwsRum constructor itself is typically loaded via a CDN `<script>` snippet which may be `async` or `defer`, meaning the constructor execution timing is non-deterministic relative to application JavaScript. The FetchPlugin does NOT expose a readiness event, callback, or promise — detection of patching completion requires checking for side effects (e.g., `window.fetch.toString()` containing shimmer wrapper code, or presence of `window.cwr` global). Source: aws-rum-web SDK FetchPlugin.ts source code; shimmer library documentation.
- **(Round 18)** OTel Python SDK's `start_as_current_span()` decorator/context manager defaults to `record_exception=True` and `set_status_on_exception=True` since v0.16b0. These defaults cause the context manager to automatically call `span.record_exception(exception)` and `span.set_status(StatusCode.ERROR)` when an exception propagates OUT of the `with` block (i.e., is not caught by inner try/except). When inner code catches an exception without re-raising, the context manager's `__exit__` receives `exc_type=None` and does NOT invoke automatic error handling. This means: (a) for spans with no inner try/except, FR-144 compliance is automatic; (b) for spans wrapping the 7 silent failure try/except blocks, FR-144 compliance requires explicit manual dual-call. Source: OTel Python SDK source `opentelemetry/trace/__init__.py` — `use_span()` implementation; confirmed behavior since v0.16b0 (December 2020).
- **(Round 18)** Python module-level code (including `TracerProvider()` constructor, `set_tracer_provider()`, and `BatchSpanProcessor()` initialization) executes exactly ONCE per process — on the first import. Lambda's warm invocation model reuses the same process, so module-level code is NOT re-executed on warm invocations. If module-level initialization fails (exception caught by FR-145), the module-level variables retain their failed/partial state for all subsequent warm invocations. Re-initialization requires handler-level code that explicitly re-creates the objects. This is standard Python semantics but is a critical architectural consideration for the canary's FR-145 resilience requirement. Source: Python Language Reference — the import system; AWS Lambda execution environment reuse documentation.
- **(Round 20)** ~~The `BatchSpanProcessor` deadlock documented in opentelemetry-python#3886 affects Python 3.11+ runtimes and has been reproduced under high concurrency conditions (multiple threads calling `on_end()` while the worker thread is blocked in `export()`). The deadlock is a known open issue as of 2026-03-03.~~ **CORRECTED in Round 21**: opentelemetry-python#3886 is FIXED in SDK v1.33.0+. The project's pinned SDK v1.39.1 uses the rewritten `BatchProcessor` (`opentelemetry/sdk/_shared_internal/__init__.py`) with `collections.deque` + `threading.Event` pattern — no nested locks. FR-139's thread-based hard timeout wrapper remains architecturally valuable for ADOT Extension hang detection but BSP deadlock is no longer a possible cause. See FR-165. Source: OTel Python SDK `_shared_internal/__init__.py` — `BatchProcessor` class v1.39.1; opentelemetry-python#3886 — fix merged in v1.33.0.
- **(Round 20)** The ADOT Lambda Extension remains alive and accepts OTLP spans during the ENTIRE streaming phase of a Lambda RESPONSE_STREAM invocation. **This is an UNVERIFIED assumption** — no AWS documentation explicitly addresses Extension lifecycle during response streaming. AWS Lambda Extensions documentation states "After receiving the function response from the runtime, Lambda returns the response to the client, even if extensions are still running" but was written for buffered responses. For streaming, the "function response" is continuous. FR-160 mandates preprod verification before production deployment. If this assumption is FALSE, the SSE Lambda's OTel instrumentation architecture requires redesign. Source: Assumption based on Extension being an external process with independent lifecycle; NO canonical source confirms this for streaming mode.
- **(Round 20)** Lambda Function URL invocations have no parent X-Ray sampling context because they bypass API Gateway entirely. When the OTel sampler is `parentbased_always_on` (FR-056) and there is no parent context, the sampler delegates to the configured root sampler which is `always_on` — meaning 100% of Function URL invocations are sampled as root spans. This is correct for dev/preprod (per FR-034) but creates unbounded cost risk in production at scale. Source: OTel SDK sampler specification — `parentbased_always_on` delegates to root sampler when no parent exists; OTel default root sampler is `always_on`.
- **(Round 20)** The ADOT Extension's `awsproxy` component proxies X-Ray `GetSamplingRules` and `GetSamplingTargets` API calls to enable centralized sampling. If the Lambda execution role lacks `xray:GetSamplingRules` permission, the awsproxy returns an error and the OTel sampler falls back to `TraceIDRatioBased` with a default of 1 trace/sec. This fallback is SILENT — no CloudWatch alarm fires, no Lambda error is raised, no structured log is emitted by default. The only observable symptom is dramatically reduced trace volume in X-Ray. Source: ADOT Remote Sampling documentation — aws-otel.github.io/docs/getting-started/remote-sampling/; OTel sampler fallback behavior specification.
- **(Round 20)** The ADOT Extension's localhost OTLP endpoint (port 4318) accepts trace data from ANY process within the Lambda execution environment without authentication. This is by design — Lambda's execution environment provides network isolation at the sandbox level. A compromised dependency running in the same Lambda could inject arbitrary trace data into X-Ray via the local OTLP endpoint. The practical risk is low (Lambda sandbox isolation) but the security model should be documented. FR-155's PII prohibition applies to data sent BY the application; it does not protect against injected trace data from compromised dependencies. Source: ADOT permissions documentation — "The proxy extension allows any application with network connectivity to the collector to send requests to X-Ray without authentication."

- **(Round 21)** OTel Python SDK v1.39.1 `BatchProcessor.emit()` uses `collections.deque([], max_queue_size)` for span queuing. When the deque is full, `appendleft()` silently evicts the oldest element — there is NO log entry, NO callback, NO metric emitted. The `emit()` method contains an explicit comment: "Do not add any logging.log statements to this function, they can be being routed back to this emit function, resulting in endless recursive calls" (opentelemetry-python#4261). This means span drops are fundamentally undetectable from within the SDK. Detection requires external comparison of created vs exported span counts. Source: OTel Python SDK `_shared_internal/__init__.py:200-208` — `emit()` method.
- **(Round 21)** ADOT Lambda Extension processes are started ONCE during Lambda INIT and are NOT restarted if they crash (OOM, segfault). A crashed Extension is dead for all subsequent warm invocations on that execution environment. The OTLP HTTP exporter's `requests.Session` creates new TCP connections per request, so if the Extension were to restart (which it doesn't), the exporter would auto-reconnect without TracerProvider recreation. Source: AWS Lambda Extensions API lifecycle documentation; OTel Python SDK `trace_exporter/__init__.py:156-172`.
- **(Round 21)** The OTLP HTTP exporter retries failed exports up to 6 times with exponential backoff (`2^retry_num * random.uniform(0.8, 1.2)` seconds). Retryable conditions: HTTP 408 or any 5xx status. Non-retryable: all 4xx except 408 — notably HTTP 429 (rate limit) is NOT retried, deviating from OTel spec recommendation. Total retry duration capped at `self._timeout` (default 10s). During retry sleep, the BSP worker thread is blocked via `_export_lock`, preventing other batches from being exported. Source: OTel Python SDK `trace_exporter/__init__.py:174-208`; `_common/__init__.py:29-34`.
- **(Round 21)** X-Ray PutTraceSegments TPS quota is ~2,500/region default (NOT 2,600 as previously stated), adjustable via AWS Service Quotas (quota code `L-AB6D2D9B`). X-Ray Groups quota is 25/region (adjustable, quota code `L-F895D63C`). X-Ray Sampling Rules quota is 25/region (adjustable, quota code `L-B5F5E1ED`). All three quotas were previously assumed to be hard/non-adjustable. Source: AWS X-Ray Service Quotas documentation.
- **(Round 21)** X-Ray encrypts ALL trace data at rest by default using an AWS-managed key. Optional customer-managed CMK available via `PutEncryptionConfig` API per-region using envelope encryption. No customer action required for default encryption. Source: AWS X-Ray Developer Guide — "Data protection in AWS X-Ray."
- **(Round 21)** `GetTraceSummaries` API has a 6-hour maximum time range window per request. For broader searches, multiple requests with sliding 6-hour windows are required. Rate limit: 30 TPS. Pagination: 100 trace summaries per response with `NextToken`. Source: AWS X-Ray API Reference — `GetTraceSummaries` parameters.
- **(Round 21)** Centralized X-Ray sampling rules only apply at trace roots (no parent context). They do NOT override parent sampling decisions propagated via `X-Amzn-Trace-Id`. For `parentbased_always_on` (FR-056), the ParentBased wrapper delegates to the root sampler only when no parent exists. API Gateway-fronted Lambdas inherit parent sampling decisions; Function URL Lambdas, EventBridge-triggered Lambdas, and the canary create root spans governed by centralized rules. Source: OTel SDK `sampling.py:349-367` — `ParentBased` delegation logic; AWS X-Ray centralized sampling specification.
- **(Round 21)** Lambda Function URL does NOT create its own X-Ray segment — there is no separate "Function URL" node in the X-Ray service map. The Lambda service creates the segment. Operators lose gateway-level latency breakdown (authorization, WAF, integration latency) compared to API Gateway. Source: AWS Lambda Function URL documentation; Lambda X-Ray tracing documentation.
- **(Round 21)** CloudWatch RUM DOES inject `X-Amzn-Trace-Id` headers into browser `fetch()` calls when `enableXRay: true` and `addXRayTraceIdHeader: true` are configured. RUM creates its own traces on the client side using the X-Ray SDK for JavaScript. Backend segments become children of the RUM-originated trace, creating end-to-end browser-to-backend traces. Requires CORS `Access-Control-Allow-Headers: X-Amzn-Trace-Id` on the backend. Source: CloudWatch RUM Developer Guide — "CloudWatch RUM with X-Ray."
- **(Round 21)** OTel `AwsLambdaResourceDetector` reads `os.environ` on every `detect()` call (no caching in the detector). However, `Resource` objects are created once during TracerProvider initialization and are immutable. On warm invocations, the same `TracerProvider` (and same `Resource`) is reused. Environment variables like `AWS_LAMBDA_LOG_STREAM_NAME` that change between cold starts retain their cold-start values in the cached Resource. Source: OTel Python SDK `extension/aws/resource/_lambda.py:35-62`.
- **(Round 21)** OTel span attribute defaults: 128 attributes per span (`_DEFAULT_OTEL_SPAN_ATTRIBUTE_COUNT_LIMIT`), no default value length limit. Excess attributes are silently dropped via `BoundedAttributes.popitem(last=False)` with `self.dropped` counter incremented — no log emitted. String value truncation (when `OTEL_SPAN_ATTRIBUTE_VALUE_LENGTH_LIMIT` is set) uses `value[:limit]` with no log. Source: OTel Python SDK `trace/__init__.py:567-714`; `attributes/__init__.py:239-323`.
- **(Round 21)** ADOT Extension warm invocation overhead is effectively zero on the hot path — span creation via `emit()` is non-blocking (appends to `deque`), and BSP export runs asynchronously on a worker thread every 1000ms. No per-invocation synchronous OTLP call occurs. The Extension is already resident on warm invocations. Source: OTel Python SDK BSP implementation; ADOT Extension lifecycle — Extension persists across warm invocations.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: For any sampled user request, an operator can trace it from browser through API Gateway through Lambda through downstream services (DynamoDB, SNS, SendGrid) as a single continuous trace — with zero tool-switching required. Dev/preprod environments sample at 100%; production sampling is configured to balance cost with debuggability.
- **SC-002**: An operator can filter traces by error/fault status and immediately identify which specific operation failed (circuit breaker, audit trail, notification publish, fanout, self-healing) — without grepping logs.
- **SC-003**: 100% of Lambda functions (6 of 6) have auto-instrumented service call subsegments, up from current 5 of 6 (Metrics Lambda missing).
- **SC-004**: 100% of silent failure paths have X-Ray subsegments marked as error when the failure occurs. The 7 paths are: (1) circuit breaker load, (2) circuit breaker save, (3) audit trail persistence, (4) downstream notification SNS publish, (5) time-series fanout partial batch write, (6) self-healing item fetch, (7) parallel fetcher error aggregation.
- **SC-005**: SSE streaming latency and cache performance data is queryable via trace annotations — operators can filter traces by latency thresholds or cache hit rate directly, without separate log query tools.
- **SC-006**: The custom correlation ID system (`get_correlation_id`, `generate_correlation_id`) is fully removed, with zero remaining call sites.
- **SC-007**: The X-Ray canary detects simulated tracing ingestion failure within 2 consecutive canary intervals and fires a non-X-Ray alarm.
- **SC-008**: Cross-Lambda traces through SNS (Ingestion -> Analysis) appear under a single trace ID with the SNS hop visible as a connecting segment, enabling cross-service debugging without log correlation.
- **SC-009**: The system contains exactly one non-X-Ray tracing mechanism: the X-Ray canary. All custom tracing logs and custom correlation IDs previously identified in audit section 9.1 are consolidated onto X-Ray. Standard operational logging and CloudWatch metrics remain unchanged.
- **SC-010** [Round 3 — Revised]: SSE streaming operations (DynamoDB polls, event dispatch, CloudWatch metrics) that occur AFTER the Lambda handler returns the generator are captured in traced spans that appear in X-Ray linked to the original invocation trace ID. Zero orphaned subsegments, zero `SegmentNotFoundException` errors, zero silently discarded segments from `LambdaContext.put_segment()`. The tracing mechanism has an independent lifecycle that survives handler return.
- **SC-011** (Round 2): SendGrid email API calls in the Notification Lambda appear as explicitly instrumented subsegments with HTTP status and duration, not as untraced gaps in the service map.
- **SC-012** (Round 2): All non-streaming Lambda functions use a single, consistent tracing approach. Exceptions raised inside traced functions are automatically captured as subsegment errors. Zero Lambdas use dual/conflicting patching mechanisms. The SSE Lambda uses a distinct approach necessitated by the RESPONSE_STREAM lifecycle constraint, but its traces appear in the same X-Ray console.
- **SC-013** (Round 3): An X-Ray Group with error/fault filter generates CloudWatch metrics for errored traces. Operators can set alarms on error rates derived directly from trace data.
- **SC-014** (Round 3): The X-Ray canary detects partial trace data loss (throttling, daemon failure) by tracking the ratio of submitted-to-retrieved test traces. When data loss exceeds the configured threshold, a dedicated CloudWatch alarm fires.
- **SC-015** (Round 3): The frontend SSE client uses `fetch()` + `ReadableStream` with automatic reconnection logic, propagating `X-Amzn-Trace-Id` headers on every connection — matching or exceeding the reliability of the replaced `EventSource` implementation.
- **SC-016** (Round 3): X-Ray billing alarms fire when monthly costs exceed $10, $25, or $50 thresholds, preventing cost surprises from sampling configuration changes or traffic growth.
- **SC-017** (Round 4): 6/6 Lambda functions have both CloudWatch error alarms and CloudWatch latency alarms configured. Zero Lambda functions exist without operational alerting.
- **SC-018** (Round 4): All custom metrics previously emitted without alarms (7 metrics from audit Section 4.2) now have CloudWatch alarms with documented thresholds. Zero metrics exist in "emitted but unalarmed" state.
- **SC-019** (Round 4): All 7 silent failure paths emit both X-Ray subsegments (for trace context, per SC-004) and CloudWatch metrics (for 100% alarm coverage). An error on any silent failure path triggers both a trace annotation and a CloudWatch alarm — no silent failure is invisible to operators regardless of X-Ray sampling configuration.
- **SC-020** (Round 4): SSE reconnection traces are queryable by `session_id` annotation, returning all traces for a logical SSE session across reconnections. Operators can follow the reconnection chain via `previous_trace_id` annotations.
- **SC-021** (Round 4): The canary detects both X-Ray ingestion failure AND CloudWatch metric emission pipeline failure within 2 consecutive canary intervals. Meta-observability failures are reported via an out-of-band channel independent of CloudWatch alarms.
- **SC-022** (Round 4): The CloudWatch dashboard alarm status widget displays ALL configured alarms. Operators viewing the dashboard see complete alarm status without hidden blind spots.
- **SC-023** (Round 5): OTel spans created during SSE streaming phase carry the SAME trace ID as the Lambda runtime's X-Ray facade segment. Zero disconnected traces from trace ID mismatch. Zero orphaned OTel spans from sampling decision mismatch. Handler-phase subsegments (Powertools) and streaming-phase spans (OTel/ADOT) appear as a unified trace in the X-Ray console.
- **SC-024** (Round 5): The SSE Lambda's streaming generator calls `force_flush()` before the execution environment freezes. Zero spans are lost between invocations due to unflushed `BatchSpanProcessor` buffers. Zero stale spans from previous invocations appear in current invocation traces.
- **SC-025** (Round 5): All X-Ray subsegment/span metadata payloads are bounded. Zero segment documents are rejected by the 64KB `PutTraceSegments` limit. Error messages are truncated to 2,048 characters. No HTTP response bodies are attached as metadata.
- **SC-026** (Round 6): On warm invocations, all OTel streaming-phase spans link to the CURRENT invocation's trace ID — not a stale trace ID from a previous invocation. Verified by triggering 2+ sequential invocations on the same warm execution environment and confirming each invocation's spans carry distinct, correct trace IDs. Zero trace ID carryover between invocations.
- **SC-027** (Round 7): The SSE Lambda's OTel `TracerProvider` is instantiated exactly once per execution environment (at INIT). On 10+ consecutive warm invocations, no new `BatchSpanProcessor` daemon threads are created and memory usage remains stable (no monotonic growth). Zero `TracerProvider` instances are created inside the handler function.
- **SC-028** (Round 7): Streaming-phase DynamoDB and CloudWatch spans appear as correctly-typed subsegments in the X-Ray service map — DynamoDB calls render as DynamoDB nodes, CloudWatch calls render as CloudWatch nodes. Zero "unknown remote" or generic nodes from streaming-phase operations. Verified by querying the X-Ray service map API after a traced SSE streaming invocation with at least one DynamoDB query and one CloudWatch `put_metric_data` call.
- **SC-029** (Round 8): The SSE Lambda's container image includes the ADOT collector binary at `/opt/extensions/collector` and the default collector configuration at `/opt/collector-config/config.yaml`. The ADOT Extension process starts during Lambda INIT, registers with the Extensions API, and accepts OTLP spans on `localhost:4318`. Verified by inspecting the container image layers and confirming the Extension appears in Lambda INIT logs.
- **SC-030** (Round 8): The SSE Lambda appears as a correctly-named node in the X-Ray service map (matching `POWERTOOLS_SERVICE_NAME`). Zero `unknown_service` nodes exist in the service map for any environment where the SSE Lambda has been invoked. Verified by querying the X-Ray service map API and confirming the SSE Lambda node name matches the configured `OTEL_SERVICE_NAME`.
- **SC-031** (Round 9): The ADOT Extension's collector pipeline includes the decouple processor. Verified by inspecting the collector configuration and confirming the pipeline order includes `decouple` as the last processor before the exporter.
- **SC-032** (Round 9): The SSE Lambda's `TracerProvider` is instantiated with `shutdown_on_exit=False`. Zero `RuntimeError` exceptions from `ConcurrentMultiSpanProcessor` shutdown race conditions on execution environment recycling. Verified by running 100+ invocations across multiple execution environment recycling events.
- **SC-033** (Round 9): Zero `BotocoreInstrumentor` imports or `.instrument()` calls exist in the SSE Lambda's codebase. Verified by code search.
- **SC-034** (Round 9): The X-Ray canary retries trace retrieval at least once with backoff (30s then 60s) before reporting trace loss. Verified by simulating X-Ray propagation delay >30s and confirming the canary does not false-alarm on the first query.
- **SC-035** (Round 9): The canary immediately escalates IAM/credential errors (`AccessDeniedException`, `NotAuthorized`) via out-of-band channel (FR-050) with zero retries. Transient errors (`ThrottlingException`, `InternalFailure`) are retried up to 3 times before escalation. Verified by simulating both error types and confirming differential handling.
- **SC-036** (Round 9): The SSE Lambda server either (a) checks `Last-Event-ID` header and resumes from that position, or (b) responds with `X-SSE-Resume-Supported: false` header. Zero scenarios exist where the client sends `Last-Event-ID` and the server silently ignores it.
- **SC-037** (Round 9): The SSE Lambda Function URL CORS configuration includes `x-amzn-trace-id` in `Access-Control-Expose-Headers`. Browser JavaScript can read the `X-Amzn-Trace-Id` response header for FR-048's `previous_trace_id` capture.
- **SC-038** (Round 9): The frontend SSE client reconnects with short delay (1-3s) on graceful server close and exponential backoff on network error. Zero instances of 30s+ reconnection delays after expected server-initiated closes (e.g., Lambda timeout).
- **SC-039** (Round 10): When an SSE Lambda streaming client disconnects mid-stream, OTel spans emitted after the disconnect carry `client.disconnected=true` annotation. Verified by disconnecting a client mid-stream and querying X-Ray for traces with the `client.disconnected` annotation. The span status is `OK` (not `ERROR`), confirming client disconnect is not misclassified as a server fault.
- **SC-040** (Round 10): The SSE Lambda's `BatchSpanProcessor` `max_queue_size` is >= 1,350 (2x expected span throughput of ~675 spans per 15-second invocation). Zero spans are silently dropped due to queue overflow during normal streaming operations (15 polls/second, 3 spans/poll). Verified by running a 15-second streaming session with `OTEL_LOG_LEVEL=WARNING` and confirming zero "Queue is full" log entries.
- **SC-041** (Round 10): The X-Ray canary reports partial traces (`IsPartial=true`) as degraded rather than successful. When a test trace has `IsPartial=true` after all retry attempts, the canary emits `canary.trace.partial` CloudWatch metric with value 1 and does NOT count the trace as a fully successful verification. Verified by simulating delayed ADOT span delivery and confirming the canary reports degraded status.
- **SC-042** (Round 10): The SSE Lambda emits `retry: 3000\n` as the first field in each SSE connection. The frontend client parses this value and uses 3000ms as the reconnection delay on graceful server close. Verified by inspecting SSE stream output and measuring client reconnection timing after Lambda timeout.
- **SC-043** (Round 10): The SSE Lambda is deployed WITHOUT VPC configuration — no `vpc_config` block in the Terraform `aws_lambda_function` resource. Verified by inspecting Terraform configuration and confirming zero `subnet_ids` or `security_group_ids` on the SSE Lambda.
- **SC-044** (Round 10): The custom ADOT collector YAML explicitly includes `decouple` in the `service.pipelines.traces.processors` list, in the order `[decouple, batch]`. Verified by inspecting the collector configuration file in the container image at `/opt/collector-config/config.yaml`.
- **SC-045** (Round 11): The SSE Lambda's custom bootstrap reads `Lambda-Runtime-Trace-Id` from the Runtime API response and sets `os.environ['_X_AMZN_TRACE_ID']` before calling the handler on EVERY invocation. On 10+ consecutive warm invocations, each invocation's OTel spans carry the CURRENT invocation's trace ID (not the first invocation's). Verified by comparing trace IDs between the Runtime API response header and the OTel spans emitted during each invocation.
- **SC-046** (Round 11): The SSE Lambda's streaming generator calls `force_flush()` proactively when remaining Lambda execution time falls below 3000ms, BEFORE the Lambda timeout kills the process. On a Lambda invocation that times out, spans created up to the proactive flush point are exported to ADOT and appear in X-Ray. Zero span loss due to `finally` block non-execution on timeout. Verified by setting a short Lambda timeout (5s) and confirming spans from the first 2 seconds appear in X-Ray.
- **SC-047** (Round 11): The SSE Lambda's Terraform configuration includes `OPENTELEMETRY_COLLECTOR_CONFIG_FILE = "/opt/collector-config/config.yaml"` in the environment variables block. The ADOT Extension uses the custom config (not the embedded default). Verified by inspecting Lambda configuration in the AWS console and confirming the ADOT Extension logs reference the custom config file path on startup.
- **SC-048** (Round 11): The observability canary runs every 5 minutes via EventBridge scheduled rule. Detection latency for X-Ray or CloudWatch failures is <= 10 minutes (2 consecutive 5-minute intervals per FR-020). The canary Lambda timeout is 60 seconds. CloudWatch metric health checks use two-phase architecture (each invocation queries PREVIOUS invocation's test metric). Verified by checking EventBridge rule configuration and canary invocation logs.
- **SC-049** (Round 11): All FR-043 silent failure metrics use the `SentimentAnalyzer/Reliability` namespace with `FunctionName` and `FailurePath` dimensions. All 6 metrics are queryable in a single CloudWatch namespace. The `HighLatencyAlert` metric uses `SentimentAnalyzer/Reliability` namespace. Verified by querying CloudWatch `list-metrics` for the namespace.
- **SC-050** (Round 11): The SSE Lambda's CloudWatch Log Group has a metric filter matching `"Exporting failed. Dropping data."`. The filter emits `SentimentAnalyzer/Reliability/ADOTExportFailure` metric. A CloudWatch alarm on this metric fires within 5 minutes of an ADOT export failure. Verified by simulating an export failure and confirming the alarm transitions to ALARM.
- **SC-051** (Round 11): The frontend SSE client uses `new TextDecoder('utf-8', {stream: true})` for ReadableStream chunk decoding. Multi-byte UTF-8 characters that span chunk boundaries are correctly reconstructed without U+FFFD replacement. Verified by sending SSE events containing multi-byte characters and confirming the client receives them intact.
- **SC-052** (Round 12): After the proactive flush (FR-093) fires, the streaming generator yields `event: deadline` and terminates normally. The Lambda invocation completes as a normal return (not a timeout). The ADOT Extension receives Shutdown with `shutdownReason: "spindown"` (not `"timeout"`). Verified by setting a short Lambda timeout (10s), streaming for 7+ seconds, confirming the `event: deadline` SSE event is received by the client, and confirming all spans up to the flush point appear in X-Ray.
- **SC-053** (Round 12): The SSE Lambda's requirements file pins OTel core packages to identical versions (e.g., `opentelemetry-api==1.39.1`, `opentelemetry-sdk==1.39.1`, `opentelemetry-exporter-otlp-proto-http==1.39.1`). `pip install` produces zero version-conflict warnings. Docker builds on different dates produce identical OTel package versions. Verified by running `pip check` and confirming zero incompatibilities.
- **SC-054** (Round 12): The SSE Lambda memory allocation is 1024MB. Cold start (including ADOT Extension + OTel SDK initialization) completes in < 1500ms. Memory utilization under normal 15-second streaming stays below 80% (< 820MB). Verified by inspecting Lambda `REPORT` log lines for `Init Duration` and `Max Memory Used`.
- **SC-055** (Round 12): The SSE Lambda's custom bootstrap reads `Lambda-Runtime-Deadline-Ms` from the Runtime API response and passes the deadline timestamp to the handler. The handler (or streaming generator) can compute remaining execution time as `deadline_ms - current_time_ms` without using the Lambda context object. Verified by logging the deadline value at handler entry and comparing with the actual Lambda timeout.
- **SC-056** (Round 12): All 6 Lambda functions have CloudWatch memory utilization alarms at 85% threshold. A Lambda invocation that uses > 85% of configured memory triggers the alarm. Verified by simulating high-memory workload and confirming alarm transition.
- **SC-057** (Round 12): FR-098's CloudWatch Logs metric filter matches both `"Exporting failed. Dropping data."` and `"Exporting failed. No more retries left. Dropping data."` log variants. Verified by injecting both log patterns and confirming the metric filter emits for both.
- **SC-058** (Round 13): Remediation tasks are deployed in phase order per FR-107: Phase 1 (IAM) → Phase 2 (Instrumentation) → Phase 3 (Frontend) → Phase 4 (Logging Removal, after 2-week verification per FR-109) → Phase 5 (Monitoring) → Phase 6 (Validation). Zero logging removal tasks (6/7/9) are deployed before instrumentation tasks (2-5) are verified. Zero canary deployments (Task 11) occur before at least one instrumentation task is live and emitting traces. Verified by deployment runbook checklist, PR merge timestamps, and canary health status.
- **SC-059** (Round 13): All ADOT-instrumented Lambda functions include `OTEL_SDK_DISABLED=false` in their Terraform environment variables block. Setting `OTEL_SDK_DISABLED=true` via Terraform and applying immediately disables all OTel span creation on the next execution environment initialization, with zero container rebuilds required. Reverting to `false` re-enables tracing. Verified by inspecting Terraform configuration and toggling the variable in a non-production environment.
- **SC-060** (Round 13): The SSE Lambda's ECR repository contains a tagged `pre-adot-baseline` image that matches the current production image without ADOT components. Reverting the Terraform `image_uri` to the `pre-adot-baseline` image and applying restores pre-ADOT behavior within 5 minutes. The rollback image passes all existing integration tests. Verified by deploying the rollback image in a test environment and confirming the Lambda functions normally without OTel traces.
- **SC-061** (Round 13): All X-Ray Groups created for this feature have `insights_configuration { insights_enabled = true }`. CloudWatch metrics derived from X-Ray Group filter expressions are visible in the CloudWatch Metrics console under the `AWS/XRay` namespace within 5 minutes of traced requests matching the Group filter. Verified by creating a Group with error filter, triggering an errored request, and confirming the `FaultCount` metric appears in CloudWatch.
- **SC-062** (Round 13): The canary EventBridge rule's Terraform resource includes explicit `depends_on` referencing the `aws_lambda_permission` resource. A fresh `terraform apply` from empty state creates the Lambda permission before the EventBridge rule activates. Zero `AccessDeniedException` invocation failures occur during initial deployment. Verified by `terraform plan` output ordering and monitoring the first canary invocation after deployment.
- **SC-063** (Round 13): The canary emits `canary.trace.completeness_ratio` metric on every invocation. Over a 1-hour window with 12 canary invocations (5-minute interval per FR-048), the completeness ratio stays >= 0.95 (at least 11 of 12 test traces fully verified). A simulated 20% systemic trace loss (e.g., by temporarily misconfiguring ADOT collector pipeline) triggers the completeness alarm within 30 minutes. Verified by querying the CloudWatch metric and simulating trace loss in a test environment.
- **SC-064** (Round 14, amended Round 17, **SUPERSEDED Round 20**): This SC has been split into SC-064a (port-unreachable) and SC-064b (hung Extension) in Round 20. The original SC tested two distinct failure modes with different code paths in a single success criterion, creating ambiguous pass/fail semantics. SC-064a verifies fast failure (ECONNREFUSED, < 100ms, no thread wrapper needed). SC-064b verifies hung failure (TCP accept, no response, FR-139 thread wrapper fires at 2500ms). The `OTEL_EXPORTER_OTLP_TRACES_TIMEOUT=2` env var requirement remains in both sub-criteria.
- **SC-065** (Round 14): Every SSE event emitted during streaming includes a `trace_id` field in the JSON data payload in X-Ray format (`1-{hex_timestamp}-{24_hex_digits}`). An operator can paste this trace_id into the X-Ray console and retrieve the exact span that produced the event. Verified by receiving an SSE event, extracting trace_id, and querying X-Ray.
- **SC-066** (Round 14): CI includes an ADOT collector config validation step before Docker image build. A deliberately malformed config (invalid YAML, unknown component, broken pipeline) causes CI failure with clear error. Validation uses the ADOT-specific binary, not `otelcol-contrib`. Verified by introducing a config error and confirming CI rejection.
- **SC-067** (Round 14): FR-109's four verification gates are executable: gate (a) automated semantic trace comparison, gate (b) annotation key set diff, gate (c) X-Ray service map API query, gate (d) automated 100-trace spot-check. All produce pass/fail with detailed output. Daily spot-check results persisted to S3 for 2-week audit trail. Verified by running gate scripts in test environment.
- **SC-068** (Round 14): The frontend SSE client correctly classifies 4 error types: graceful close (sentinel) → immediate reconnect, abnormal termination (no sentinel) → short delay, network error (TypeError) → exponential backoff, intentional cancellation (AbortError) → no reconnect. SSE Lambda sends `event: done` before every intentional close. Verified by simulating each scenario and measuring reconnection behavior.
- **SC-069** (Round 14): All CORS configurations including `X-Amzn-Trace-Id` in Allow-Headers also set `Access-Control-Max-Age: 7200`. First fetch() triggers preflight; subsequent requests within 2 hours do not. Verified via browser DevTools Network tab.
- **SC-070** (Round 14): No OTel span on the SSE Lambda exceeds 25 annotations (half the 50-annotation X-Ray limit). Annotation budget is documented with 10 reserved slots. Verified by exporting traces and counting annotations per span.
- **SC-071** (Round 14): All canary/heartbeat alarms use `treat_missing_data = "breaching"`. All custom error metric filter alarms use `"breaching"`. AWS-native Lambda metric alarms use `"notBreaching"`. Simulated canary failure (no metric emission for 2 periods) triggers alarm. Verified by inspecting Terraform and simulating metric absence.
- **SC-072** (Round 14): Kill switch mitigation (`OTEL_SDK_DISABLED=true`) applies within 5 minutes. Full image rollback to `pre-adot-baseline` completes within 15 minutes. Post-rollback SSE Lambda error rate < 0.1%, cold start p99 < 1500ms within 5 minutes. Verified by rollback drill in preprod.
- **SC-073** (Round 14): SSE Lambda CloudWatch Log Group has metric filter for ADOT collector startup failures. Deliberately broken config triggers `ADOTCollectorStartupFailure` metric and alarm within first invocation. Verified by deploying malformed config to test environment.
- **SC-074**: All 5 identified silent failure paths (circuit breaker persistence, audit writes, notification delivery, self-healing fetch, fanout partial writes) emit a `SilentFailure/Count` metric with `FailurePath` dimension AND log a structured fallback line containing `metric_fallback: true` on metric emission failure.
- **SC-075**: Each Lambda log group has a CloudWatch Logs metric filter matching `{ $.metric_fallback = true }` that generates a backup metric, with an alarm using `treat_missing_data = "breaching"`.
- **SC-076**: The canary `EmissionHeartbeat` metric is included in the same `put_metric_data` API call as production metrics — not a separate call — and its alarm uses `treat_missing_data = "breaching"` with 3×60s evaluation periods.
- **SC-077**: All Lambda error alarms use percentage-based CloudWatch math expressions with a minimum invocation guard (>10); no Lambda error alarm uses absolute count thresholds.
- **SC-078**: Ingestion Lambda has P95 latency alarm < 48,000ms; Notification Lambda has P95 latency alarm < 24,000ms; SSE Streaming Lambda has P99 connection setup alarm < 5,000ms; Analysis Lambda has WARNING at 36,000ms and CRITICAL at 48,000ms.
- **SC-079**: CloudWatch dashboard displays all alarms across 3 severity-tiered widgets using alarm name prefix patterns (not ARN lists); a composite alarm aggregating all CRITICAL alarms is visible in the dashboard header.
- **SC-080**: An integration test confirms that a SendGrid API call produces an X-Ray subsegment or OTel span with destination URL containing `api.sendgrid.com`, verifying the httplib/urllib patching chain.
- **SC-081**: All CloudWatch alarms on application-emitted custom metrics use `treat_missing_data = "missing"`; canary/heartbeat alarms use `breaching`; only AWS-native metric alarms use `notBreaching`.
- **SC-082**: A validation check detects any FR present in the HL remediation checklist task table but absent from the corresponding fix-*.md work order file, with zero synchronization gaps.
- **SC-083**: Collision rate alarm evaluation periods are ≤2 periods for high collision and ≤3 periods for low collision; error rate alarm thresholds on all Lambdas are percentage-based.
- **SC-084** (Round 16): Every `FailurePath` dimension value of the `SilentFailure/Count` metric has an individual CloudWatch alarm. An aggregate composite alarm across all FailurePath-specific alarms is in the CRITICAL tier of the dashboard. Simulated silent failure (e.g., injected DynamoDB throttle on circuit breaker table) triggers the path-specific alarm within 1 evaluation period (1 minute).
- **SC-085** (Round 16): A Playwright test confirms `X-Amzn-Trace-Id` header is present on outbound `fetch()` requests to API Gateway (same-origin) and Lambda Function URL SSE endpoint (cross-origin). The trace ID matches X-Ray format (`Root=1-{hex_timestamp}-{24_hex_digits}`). The test runs as a Phase 3 deployment gate and fails if any monitored fetch() call lacks the header.
- **SC-086** (Round 16): The canary publishes failure alerts to a cross-region SNS topic via direct `sns.publish()`. Simulated primary-region CloudWatch Metrics API degradation does NOT prevent the alert from reaching the SNS topic. The topic has at least one email and one webhook subscription confirmed by `sns:ListSubscriptionsByTopic`.
- **SC-087** (Round 16): A test trace from Ingestion Lambda through SNS to Analysis Lambda shows both Lambda segments and the SNS connecting segment under a single X-Ray trace ID in the service map. The verification gate is automated, uses retry-with-backoff (30s then 60s), and blocks Phase 2 deployment on failure.
- **SC-088** (Round 16): API Gateway-fronted Lambdas have `IntegrationLatency` P99 alarms and `5XXError` rate alarms. The SSE Lambda has equivalent Lambda Function URL metric alarms (`Url5xxError`). A simulated 6-second Lambda response triggers the IntegrationLatency alarm within 2 evaluation periods.
- **SC-089** (Round 16): `force_flush()` completes or aborts within 2,500ms regardless of ADOT Extension state. A hung Extension (TCP accept but no OTLP response) causes the flush to abort after 2,500ms — NOT block for 44+ seconds. The generator still yields `event: deadline` and terminates gracefully. Zero Lambda timeouts caused by runaway `force_flush()`.
- **SC-090** (Round 16): ADOT Extension unavailability during SSE streaming produces an `ADOTExtensionUnavailable` CloudWatch metric within the same Lambda invocation. Simulated Extension crash during streaming results in metric emission and alarm transition within 2 minutes — not waiting for the 10-minute canary cycle.
- **SC-091** (Round 16): X-Ray query `annotation.session_id = "{test_session_id}"` returns all traces for an SSE session across multiple reconnections. The `sse-reconnections` X-Ray Group generates a CloudWatch metric for reconnection frequency. Reconnection rate alarm fires when rate exceeds 10/minute system-wide.
- **SC-092** (Round 16): FR-124's `FailurePath` dimension has exactly 7 distinct values: `circuit_breaker_load`, `circuit_breaker_save`, `audit_trail`, `notification_delivery`, `self_healing_fetch`, `fanout_partial_write`, `parallel_fetcher_aggregate`. Each value produces a separate alarm (FR-134).
- **SC-093** (Round 16): The `metric_fallback` structured log line is JSON-formatted across all 6 Lambda functions. FR-125's CloudWatch Logs metric filter `{ $.metric_fallback = true }` matches fallback lines from every Lambda. Deliberately emitting a fallback line (by mocking `put_metric_data` to fail) in each Lambda's log group produces a metric filter match confirmed by `get-metric-data`.
- **SC-094** (Round 17): Every manually-created OTel span on the SSE Lambda that encounters an exception has BOTH `StatusCode.ERROR` set AND the exception recorded. An X-Ray query with `fault = true` on the error X-Ray Group (FR-034) returns spans from ALL error paths — including DynamoDB failures, internal processing errors, and BrokenPipeError disconnects (FR-085). Zero exceptions are invisible to X-Ray error filtering. Verified by triggering each of the 7 silent failure paths (SC-004) during streaming and confirming every resulting span has `fault=true` and populated `cause` field in the X-Ray segment document.
- **SC-095** (Round 17): The canary Lambda starts successfully and completes its health check cycle even when X-Ray SDK/OTel initialization fails. Simulated X-Ray service outage (ADOT Extension not running + X-Ray API returning 500) does NOT crash the canary. The canary emits `CanaryInitFailure` CloudWatch metric and still publishes to cross-region SNS (FR-136). Verified by deploying canary without ADOT Extension and confirming health check completes with degraded status.
- **SC-096** (Round 17): When X-Ray is healthy, the canary Lambda's own operations (X-Ray API calls, CloudWatch API calls, SNS publish) appear as traced subsegments in X-Ray. The canary's Powertools Tracer instrumentation produces the same trace structure as other non-streaming Lambdas. Verified by querying X-Ray for canary traces during normal operation.
- **SC-097** (Round 18): FR-124's inline text states "seven distinct silent failure paths" (not "five"). The 7 FailurePath dimension values match FR-142's enumeration and FR-134's alarm definitions exactly. Zero dangling alarms reference non-existent metrics. Verified by text inspection of FR-124 and cross-reference with FR-142 enumeration and FR-134 alarm ARN list.
- **SC-098** (Round 18): The frontend application does NOT issue any fetch() call to instrumented backend endpoints before CloudWatch RUM FetchPlugin initialization completes. A Playwright test simulates a cold page load with network throttling (Slow 3G) and verifies that the first outbound fetch() to the API carries `X-Amzn-Trace-Id` header. A second test simulates RUM CDN failure (blocked by CSP) and verifies the application proceeds after the 5-second timeout with a structured warning logged to the browser console.
- **SC-099** (Round 18): After FR-093's proactive flush fires, zero new OTel spans are created before the generator returns. A test triggers proactive flush by simulating remaining time <= 3000ms, then verifies the BSP queue is empty at generator return. Any span-creating code path executed after flush_fired=True logs via structured JSON instead. The test counts spans exported after the flush timestamp and asserts zero.
- **SC-100** (Round 18): Every caught exception in the SSE Lambda's 7 silent failure paths (FR-142 enumeration) has explicit `span.record_exception(exception)` AND `span.set_status(StatusCode.ERROR, str(exception))` as the first two statements in the except block. Code review gate: a grep-based CI check verifies that every `except` block within an OTel span context contains both calls. X-Ray query for `fault = true` on the error Group returns spans from ALL 7 failure paths when triggered.
- **SC-101** (Round 18): The canary Lambda attempts TracerProvider re-initialization on warm invocations when cold-start initialization failed. A test deploys the canary without ADOT Extension (causing cold-start failure per FR-145), invokes it twice (warm invocation), then starts ADOT Extension and invokes a third time. The third invocation's traces appear in X-Ray, confirming successful re-initialization. The `canary_reinit_attempt` structured log is emitted on each warm invocation while `_tracer_init_failed` is True.
- **SC-102** (Round 18): FR-117's automated trace validation script is the authoritative deployment gate for trace structure verification. The script defines minimum expected segment counts per Lambda function type and validates 100 traces per deployment. FR-109 gate (d) is documented as a one-time acceptance test. The automated script runs in FR-107 Phase 5 and blocks promotion on segment count violations.
- **SC-103** (Round 18): FR-018's scope statement explicitly names all covered Lambda functions and all exempted Lambda functions (with FR references). A new Lambda function added to the system without an explicit exemption FR defaults to fail-fast behavior. Verified by spec text inspection and by deploying a hypothetical 8th Lambda without FR-018 exemption — it crashes on X-Ray initialization failure.
- **SC-104** (Round 18): FR-132's staleness detection creates a GitHub issue within 1 hour of detecting HL document staleness. The next `/speckit.specify` round cannot proceed until the staleness issue is resolved or explicitly deferred. A test runs FR-132's audit against the current HL document and verifies the issue creation mechanism fires when gaps are detected. The issue body enumerates specific missing rounds, risk entries, and SC entries.
- **SC-105** (Round 18): Zero X-Ray span annotations or metadata contain PII (email, username, IP, sentiment text, API keys). A code review gate searches all `set_attribute()`, `add_annotation()`, and metadata assignment calls for variable names matching PII patterns (email, user, name, address, text, input, token, key, secret). X-Ray trace data from a production-like test run is queried and spot-checked for PII content. The PII check runs as part of FR-107 Phase 4 code review gate.
- **SC-064a** (Round 20, split from SC-064): Port-unreachable ADOT failure mode — stopping the ADOT Extension process causes `force_flush()` to fail fast (< 100ms) via ECONNREFUSED on localhost:4318. The SSE Lambda emits a structured log with `flush_timeout_cause: "extension_unavailable"` and `otlp_connection: "refused"`. FR-139's thread wrapper is NOT required for this code path — ECONNREFUSED returns immediately. Verified independently from SC-064b by killing the Extension process during a streaming invocation and measuring `force_flush()` duration.
- **SC-064b** (Round 20, split from SC-064, **amended Round 21**): Hung ADOT Extension failure mode — a mock TCP server on port 4318 that accepts connections but never sends an OTLP response causes `force_flush()` to hang until FR-139's 2500ms thread wrapper fires and aborts the call. The SSE Lambda emits a structured log with `flush_timeout_cause: "extension_hang"` and `otlp_connection: "established"` (label amended from `"bsp_or_extension_hang"` per FR-165 — BSP deadlock eliminated in SDK v1.33.0+). The generator yields `event: deadline` and terminates gracefully within 500ms of thread wrapper abort. Zero Lambda timeouts caused by this failure mode. Verified independently from SC-064a by deploying a mock TCP server during a streaming invocation.
- **SC-106** (Round 20, **amended Round 21**): Force flush completes or aborts within 2500ms (per FR-139) regardless of whether the timeout cause is ADOT Extension hang or any other blocking condition. BSP deadlock (opentelemetry-python#3886) is no longer a possible cause at SDK v1.39.1 (FR-165). When the flush times out, a structured log line differentiates the cause: `flush_timeout_cause` is `"extension_unavailable"` (ECONNREFUSED, fast failure) or `"extension_hang"` (connection established but no response). Verified by both SC-064a and SC-064b failure scenarios.
- **SC-107** (Round 20): A GitHub issue titled "HL Document Staleness: Missing Round 18" exists in the repository, created as part of Round 20 acceptance. The issue enumerates: 9 missing FRs (FR-147 through FR-155), 9 missing SCs (SC-097 through SC-105), missing Round 18 Progress Log entry, and missing Round 18 Risk Assessment entries. The issue is labeled `doc-staleness` and `priority:high`. HL document synchronization MUST be completed before any task implementation begins.
- **SC-108** (Round 20): SSE Lambda cold start P95 latency is measured in preprod with the ADOT-embedded Docker image at 1024MB memory and is < 2000ms. The measurement is repeated after any ADOT version, OTel SDK version, or memory allocation change. The cold start measurement is documented in the HL document's Risk Assessment. If measured cold start exceeds 2000ms, the Lambda memory allocation is increased until the budget is met.
- **SC-109** (Round 20): The Lambda execution role for ADOT-instrumented Lambdas (SSE Lambda, canary Lambda) includes all 5 IAM actions: `xray:PutTraceSegments`, `xray:PutTelemetryRecords`, `xray:GetSamplingRules`, `xray:GetSamplingTargets`, `xray:GetSamplingStatisticSummaries`. A Terraform plan shows all 5 actions in the policy statement. ADOT Extension logs show `"Successfully fetched sampling rules"` on cold start, confirming centralized sampling is operational.
- **SC-110** (Round 20): A preprod test deploys the ADOT-instrumented SSE Lambda, initiates a 15-second streaming invocation, creates OTel spans at 1-second intervals throughout the streaming phase (14+ spans), and verifies ≥95% of streaming-phase spans appear in X-Ray within 60 seconds of invocation completion. The test runs 10 invocations and reports aggregate span retention rate. The test blocks FR-107 Phase 2 progression on failure.
- **SC-111** (Round 20): An X-Ray centralized sampling rule exists for the SSE Lambda Function URL with a configurable sampling rate (`FixedRate`). FR-038's cost alarms include a daily anomaly detection alarm that fires when X-Ray daily cost exceeds 200% of the trailing 7-day average. In dev/preprod, the sampling rule rate is 1.0 (100%). Production sampling rate is configurable via the X-Ray console without code deployment.
- **SC-112** (Round 20): Every CloudWatch alarm in the system has an explicit `treat_missing_data` value that matches the documented classification: `breaching` for canary heartbeat and `no_new_items` alarms; `missing` for all application metric alarms, Lambda error/latency alarms, and API Gateway alarms; `notBreaching` for cost/billing alarms only. A Terraform validation check confirms all `aws_cloudwatch_metric_alarm` resources have `treat_missing_data` explicitly set (not relying on provider defaults). Zero alarms omit the setting.
- **SC-113** (Round 20): An operational runbook section documents the manual X-Ray trace archival procedure: (a) identify incident time window, (b) estimate trace volume via `GetTraceSummaries` count, (c) export traces via `BatchGetTraces` to S3 bucket with 1-year retention, (d) record S3 path in post-mortem document. The procedure is cross-referenced from FR-122's rollback section. The procedure includes rate limit awareness (5 traces/request, 5 requests/sec) and estimated archival duration per trace volume.
- **SC-114** (Round 20): Every FR listed in the HL document's Work Order task table is present in the corresponding fix-*.md file with round annotation (e.g., "FR-100 (Round 12)"). FR-132's automated validation check reports zero synchronization gaps. Verified after the HL document synchronization mandated by FR-157, and re-verified after each subsequent specification round.
- **SC-115** (Round 21): FR-156's structured log label `"bsp_or_extension_hang"` is replaced with `"extension_hang"` across all code paths and documentation. The spec's catalogued span-loss vectors enumerate exactly TWO vectors: (1) ADOT Extension drop race (FR-074), (2) ADOT Extension hang (FR-139). FR-156's BSP deadlock vector is documented as RETIRED with the caveat that OTel SDK downgrade below v1.33.0 would re-expose it. Verified by text search for `"bsp_or_extension_hang"` in codebase and spec — zero occurrences remain.
- **SC-116** (Round 21): SC-040's verification of BSP queue non-overflow uses a span-count comparison method: a test `SpanProcessor` shim counts total `on_end()` calls (spans created), a test `SpanExporter` shim counts total `export()` spans received (spans exported), and the delta is zero during a standard 15-second streaming session. The old verification method ("confirming zero 'Queue is full' log entries") is documented as INVALID for SDK v1.39.1+. Verified by running a 15-second streaming session with the test shims and asserting created == exported.
- **SC-117** (Round 21): ADOT Extension crash during SSE streaming produces `ADOTExtensionUnavailable` CloudWatch metric (per FR-140) on the SAME invocation AND on ALL subsequent warm invocations on the same execution environment. A test kills the Extension process mid-stream, confirms metric emission on the current invocation, then triggers 3 additional warm invocations and confirms each emits the metric. Recovery occurs only on execution environment recycling (new cold start). Verified by monitoring Lambda invocation logs and CloudWatch metrics across warm invocations after Extension crash.
- **SC-118** (Round 21): An AWS Config rule or CloudTrail EventBridge rule monitors IAM policy changes on ADOT-instrumented Lambda execution roles. When any of the 5 required X-Ray IAM actions (FR-159) are removed from the role policy, a `SentimentAnalyzer/Reliability/IAMPolicyDrift` CloudWatch metric is emitted with `FunctionName` dimension within 15 minutes of the IAM change. A CloudWatch alarm fires on any non-zero value. Verified by removing `xray:GetSamplingRules` from the role and confirming the alarm transitions to ALARM state.
- **SC-119** (Round 21): The canary Lambda emits a heartbeat signal to an external monitoring endpoint (cross-region CloudWatch or third-party uptime service) on every successful invocation. Simulated double-failure (canary stops running + primary-region CloudWatch Alarms degraded) triggers the external heartbeat monitor's alert within 15 minutes. The external heartbeat is fire-and-forget — delivery failure does NOT affect canary execution. Verified by stopping the canary EventBridge rule and disabling primary-region alarm actions, then confirming the external monitor alerts.
- **SC-120** (Round 21): X-Ray PutTraceSegments TPS quota utilization is monitored. A CloudWatch alarm fires when `UnprocessedTraceSegments` count in PutTraceSegments API responses exceeds zero, indicating segments were permanently lost due to throttling. The alarm references AWS Service Quotas quota code `L-AB6D2D9B` with current region default (~2,500 TPS). Verified by inspecting Terraform alarm configuration and confirming the metric source.
- **SC-121** (Round 21): FR-163's trace archival runbook accounts for the 6-hour maximum `GetTraceSummaries` time window per request. The runbook includes a time estimation formula: `archival_minutes = ceil(incident_hours / 6) * (trace_count / 500) / 60 + trace_count / (5 * 5) / 60`. For a 24-hour incident with 100,000 traces, the estimated archival time is documented as ~70-90 minutes. Verified by runbook text inspection.
- **SC-122** (Round 21): Terraform configuration for X-Ray Groups and Sampling Rules includes a comment documenting current resource count against the 25-per-region quota. A CI validation check counts `aws_xray_group` and `aws_xray_sampling_rule` resources and warns when count exceeds 20 (80% of default quota). Verified by running the CI check and confirming the count is below threshold.
- **SC-123** (Round 21): The spec documents that X-Ray encryption at rest is active by default using an AWS-managed key. The operational runbook notes CMK configuration availability via `PutEncryptionConfig` for organizations requiring customer-controlled encryption. No action is required for default encryption — this SC is verified by AWS X-Ray documentation review.
- **SC-124** (Round 21): The spec explicitly documents that centralized X-Ray sampling rules control Function URL Lambda (SSE), EventBridge Lambdas (Canary, Metrics) sampling rates, but have NO effect on API Gateway-fronted Lambdas (Dashboard, Ingestion, Analysis, Notification) which inherit parent sampling decisions. Implementers can verify by changing the centralized rule rate in preprod and confirming only Function URL/EventBridge Lambda trace volumes change.
- **SC-125** (Round 21): An ADOT version upgrade runbook exists, cross-referenced from FR-070 and FR-110. The runbook enumerates 8 verification gates (release notes review, Dockerfile update, CI config validation, cold start re-measurement, streaming span retention, trace validation, IAM verification, production deployment). The runbook mandates ADOT upgrades be deployed independently of application code changes. Verified by runbook text inspection and cross-reference check.
