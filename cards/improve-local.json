{
  "id": "improve-local",
  "subject": "improve-local: inbox and factoring pass for this repo's session-governing rules",
  "status": "pending",
  "description": "The dump target for instructions that governed a session in this repo and exist in no committed file (the improve-local role, created by name -- no allocator anywhere; card 166 in ~/dotfiles is the record). Contributors append via improve-drop.sh local; this repo's Helios batch-incorporates with improve-collect.py local. A candidate becomes a committed line only when it passes the five gates of ~/dotfiles/cards/133.json: recurrence, durability, mechanism first, payment, placement. Dumping is free; factoring is the work.",
  "candidates": [
    "`pytest` here collects from `tests/` alone, so anything written under any other directory is dead on arrival and its green-ness is not evidence. `scripts/tests/test_pi_carryover.py` and `test_pi_carryover_rotate.py` (211 lines, shipped by PR #1036) were never executed by any runner in the 40 days they existed; they were deleted with the code they covered at f05fd7d5.\n\nCitation: pyproject.toml:138 `testpaths = [\"tests\"]`; no Makefile, .github/workflows or .pre-commit-config.yaml target names scripts/tests (grep, 2026-09-22, zero hits).\n\nMechanism: a new suite lands under `tests/`, or the writer states which runner reaches it. Cheap check before claiming a suite passes: `pytest --collect-only <path> -q | tail -1` returns \"no tests ran\" for a path outside testpaths."
  ]
}
