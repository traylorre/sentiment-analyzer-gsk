# .env.example - Template for .env.local
# Generated by Feature 1194-workspace-bootstrap
#
# Copy this file to .env.local and fill in values:
#   cp .env.example .env.local
#
# Or run the bootstrap script to auto-populate from AWS Secrets Manager:
#   ./scripts/bootstrap-workspace.sh
#
# DO NOT COMMIT .env.local - it is gitignored

# AWS Configuration
AWS_REGION=us-east-1

# API Keys from AWS Secrets Manager (dev/sentiment-analyzer/*)
DASHBOARD_API_KEY=
TIINGO_API_KEY=
FINNHUB_API_KEY=
SENDGRID_API_KEY=
HCAPTCHA_SECRET_KEY=
HCAPTCHA_SITE_KEY=

# Test Configuration
# This secret is used for magic link authentication during testing
# Must be at least 32 characters long
MAGIC_LINK_SECRET=local-dev-secret-at-least-32-characters-long

# Optional: Stripe (for payment testing)
STRIPE_WEBHOOK_SECRET=

# Cache metadata (populated by bootstrap script)
# SECRETS_CACHE_PATH=~/.config/sentiment-analyzer/secrets.cache.age
# SECRETS_FETCHED_AT=
