# Development & Testing Dependencies
# Sentiment Analyzer - Demo 1 (001-interactive-dashboard-demo)
#
# Install with: pip install -r requirements-dev.txt
# This includes production deps: pip install -r requirements.txt
#
# For On-Call Engineers:
# - CI/CD uses these exact versions
# - If tests fail locally but pass in CI, check version mismatch
#
# For Developers:
# - moto mocks AWS services (DynamoDB, SNS, Secrets Manager)
# - responses mocks HTTP requests (NewsAPI)
# - Run tests with: pytest

# Include production dependencies
-r requirements.txt

# Testing Framework
pytest==7.4.3
pytest-cov==4.1.0       # Coverage reporting

# AWS Mocking - Critical for unit tests
# Note: moto version must support all AWS services we use
# moto 5.0+ required for unified mock_aws decorator
moto[all]==5.0.0

# HTTP Mocking - For NewsAPI adapter tests
responses==0.24.0

# Code Quality
black==23.11.0          # Code formatter
ruff==0.1.6             # Fast linter (replaces flake8, isort)
pre-commit==3.6.0       # Git hooks framework

# Type Checking (optional but recommended)
mypy==1.7.1
boto3-stubs==1.34.0     # Type stubs for boto3

# Testing Utilities
httpx==0.25.2           # For FastAPI TestClient
freezegun==1.2.2        # Time mocking for TTL tests
